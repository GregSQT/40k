# Roadmap Index — Ordre global du travail

> **Source unique de priorité.** Ce fichier tranche l'ordre, les fichiers sujets tranchent le
> contenu. Historique d'un sujet : `archives/<sujet>.md`. `archives/ROADMAP.md` est un FOSSILE
> gelé (l'ancien fichier monolithique) : ne pas le lire en session, ne plus le mettre à jour.
>
> **Règles d'arbitrage :** Code > décision datée > priorité ici > tout autre doc.
> Conflit résiduel → demander à l'utilisateur.
>
> **Discipline.** Ouvrir = ajouter une ligne ici D'ABORD. Livrer = marquer ✅ ici + vider/archiver
> dans le fichier sujet, dans la même livraison.
>
> **Outillage.** `python3 scripts/check_doc_references.py` contrôle ce fichier, les fichiers
> sujets et les deux contrats permanents (renvois, liens, valeurs recopiées, ancres, sortes),
> et vérifie qu'aucun chantier ouvert de `Documentation/Chantiers/backlog/` n'est devenu
> inatteignable depuis ce fichier — un document que plus aucun fichier sujet ne cite n'est plus
> priorisé, il est seulement stocké.
> La porte de fusion `scripts/check_roadmap_declared.py` (hook `prepare-commit-msg`, versionné
> dans `.githooks/`) : une fusion dans `main` est refusée quand **2** chantiers ont été livrés
> sans que ce fichier bouge. Se débloquer : écrire la ligne du chantier puis `git add` (l'index
> vaut déclaration) ; fusion hors chantier : `ROADMAP_GATE=off git commit`.
>
> **Contrats permanents** (jamais archivés, hors roadmap) :
> `Documentation/Chantiers/Replay.md` (contrat `step.log`, pipeline replay) et
> `Documentation/Chantiers/analyzer_couverture.md` (matrice règle → contrôle → champs de
> log) — relus à chaque livraison qui touche le journal.
>
> **Exceptions actées** (numérotées ici et nulle part ailleurs) : `Bot_refactor.md` vit à la racine de `Documentation/Chantiers/` au lieu de `backlog/` (chantier vivant, chemin demandé) ; `archives/v11.md` porte l'historique du programme V11 entier et sert d'archive au sujet `v11_chemin_critique.md`.
>
> **Pendant entraînement.** `⚡` = peut démarrer sans interrompre un run (ne touche pas
> `config/**/*.json`, ne modifie pas le moteur). `🚫` = ne pas démarrer : risque de biaiser le
> run ou conflit direct avec le processus d'entraînement.

---

## Direction — le fil

**CAP : la démo de financement** — un agent RL crédible sur les deux rosters retenus (Space
Marines / Orks, décision 2026-07-19), prouvé par une mesure quantitative (win-rate `x1_long`
contre le panel) et une validation qualitative par un joueur externe.

Tout chantier sert un jalon ci-dessous, ou attend. Les jalons sont séquentiels ; le soutien
(analyzer, security, infra, hygiène doc) avance en parallèle quand un jalon le réclame.

| Jalon | Contenu | Critère de sortie |
|---|---|---|
| **J1 — Pipeline prouvé, ligne de base** ✅ | Run `x1_long --new` terminé le 2026-08-20 ; ligne de base REJOUÉE le 2026-08-21 sur `robust_0.8463` (post-rupture §12.15) : combined agent `0,8567`, pire bot `attrition = 0,810` ; reference bots mesurés bot-contre-bot (balanced `0,168`, denial `0,155`, reactive `0,139`) ; `benchmark_floor` remis à `0,90` le 2026-08-20 (le `0,049` mélangeait les sémantiques) — gate RETIRÉ depuis, le 2026-08-22 | Critères de [training.md#run-verif](training.md#run-verif) verts ; ligne de base panel rejouée ✅ |
| **J2 — Le gym décide tout** ✅ | P3-5, P3-6, P3-8, P4, P5 livrés ; `TOTAL_ACTION_SIZE` 1159→1389, `obs_size` 16671→16703 ; ré-entraînement `--new` nécessaire. P3-6 rouvert puis soldé le 2026-09-09 : son « constaté implémenté » du 2026-08-19 décrivait la règle jouée, pas la décision rendue — `move_after_shooting` est désormais une décision d'agent, `reactive_move` reste auto (porteurs absents du régime d'entraînement, cf. [v11_chemin_critique.md#p3-6](v11_chemin_critique.md#p3-6)). Deux correctifs le 2026-09-09 : la décision `move_after_shooting` étant armée sur le drapeau GLOBAL `gym_training_mode`, le BOT adverse y répondait au tirage — sa baseline bougeait en même temps que l'agent mesuré, `bot_action_for_pending_choice` lui rend `CHOICE_0` ; puis le scoring de la case occupée est fusionné avec celui des destinations (un seul passage de distances). Chaîne vérifiée sur le chemin de production — vrai moteur, vrai décodeur, vrai bot —, et aucun run n'a tourné dans la fenêtre de 78 min où la baseline était aléatoire (artefacts de `ai/models/` inchangés depuis 05:49) | Plus aucune décision de jeu jouée par une heuristique à la place de l'agent, hors optionnels statués par mesure de regret ✅ |
| **J3 — Mesure de référence** | Curriculum R1→R3 (URGENCE), puis `x1_long` (~6 h) | LE chiffre officiel du projet — solde §0.14, §0.67 et le critère T6 (via §10.6) |
| **J4 — Dépasser la mesure** | Capacités 06, É9 second scénario — priorisés selon ce que la mesure révèle | Win-rate au-dessus de la mesure de référence, reproductible |
| **J5 — Démo** | Validation qualitative §10.6 volet 2 (joueur externe), validations navigateur front soldées | Un externe joue contre l'agent et le trouve crédible ; le front tient la partie de bout en bout |

---

## ✅ URGENCE — Curriculum : adversaires et étalons (décision 2026-08-21)

**R0a/R0b livrés. R1→R3 absorbés par le curriculum `--etape` (décision 2026-08-30)** : le fix
aliasing obs Phase 3 (2026-08-29) invalide toute baseline antérieure ; le run `--etape P2`
post-fix tient lieu de R1 (ratio_mb0=1.0, EV→0.85). Le curriculum P0→P10 intègre les leviers
R2 (self-play) et R3 (récompense, si D.4 le justifie) séquentiellement — mesurer en standalone
n'apporterait que la décomposition du gain par levier. Fermé sans dette.
Détail : `Documentation/Chantiers/backlog/curriculum_adversaires_etalons.md`.

**Régime d'entraînement révisé le 2026-09-06 — P2 est à relancer depuis P1.** Trois changements
livrés ensemble, les deux premiers mesurés sur le run x1_long du 2026-09-06 (~64 000 épisodes) :
le déploiement `auto` pose désormais **les deux camps** (il ne posait que l'agent, face à un
adversaire qui se déployait avec sa politique — 0.304 de win-rate en `auto` contre 0.684 en
`active`, différentiel d'objectifs -0.76 contre +0.19) ; et la part de bots passe à **30 % en P1,
20 % en P2, 15 % de P3 à P10** (les six bots sont saturés entre 0.86 et 1.00, quand le champion du
pool est à 0.502 et P0 à 0.626). P1 n'est PAS rejoué — sa config est homogénéisée et porte la
mesure de ce qu'il a réellement joué. ⚠️ `s_win_rate_deploy_auto` **change de définition** : ses
valeurs antérieures au 2026-09-06 ne se comparent pas aux suivantes. Enfin `bot_eval_intermediate`
de `x1_long` passe à **100** épisodes par bot pour la précision des points intermédiaires
(erreur-type 9,1 → 5,0 points) ; l'évaluation intermédiaire du run à relancer monte donc de 180 à
600 épisodes, coût horloge non rechronométré, et `x5_long` reste à 30.
Détail : [training.md#regime-2026-09-06](training.md#regime-2026-09-06).

**✅ Régime de lignée livré le 2026-09-07 (option A) — P2 reste à lancer.** Trois runs P2
successifs avaient échoué en faisant varier des hyperparamètres étape par étape ; l'option A
supprime cette possibilité au lieu de chercher les bonnes valeurs pour chacune. Un bloc
profil **`x1_lineage`** (`learning_rate` 0.001 et `ent_coef` 0.03 **scalaires**, `n_steps` 32640,
`vf_coef` 0.15, `agent_seat_p2_ratio` 0.6, le reste **hérité** de `x1_long` par
`"extends"` — dont `batch_size` 1020, le 4080 initial ayant fait mourir P2 à sa première update
par saturation de VRAM, cf. correctifs hors chantier du 2026-09-07) s'applique à toute étape `init: "from:"` — learners comme exploiteurs ; les vingt
rampes `decay_fraction` et les surcharges de P2/P3 disparaissent, une étape reprise ne déclare
plus que `total_episodes`. Le curriculum ne porte que les deux **noms** (`training_configs`), et
un `--training-config` qui ne correspond pas à la nature de l'étape est refusé au lancement. `P00` est supprimée (`P0`
passe en `init: "new"`, E1/E2/E3 en `from:P0`). Un **verrou de parité** arrête le run quand la
baseline d'ouverture contre l'archive reprise sort de `[0.40, 0.60]` — elle vaut 0.50 par
identité, et deux nuits d'entraînement ont déjà été payées sur une baseline aberrante lue comme
une mesure. Il ne se pose qu'à l'**épisode 0 de l'étape** : après un `--resume-from` de reprise
sur crash, le modèle a déjà progressé et n'est plus l'archive source. Toutes les décisions
(promotion, arrêt pour destruction, gate de fin) passent sur la **moyenne des `probe_window`
dernières sondes** (3 aujourd'hui ; le tag TensorBoard `pool_eval/vs_<tag>_<n>ep` porte la
fenêtre réelle) et sur le pool ENTIER, plus seulement le champion. Un verdict **`destroy` est
souverain** : l'étape est refusée sans que le gate la mesure — sous `save_best_robust` le zip
canonique est un instantané antérieur, donc un autre modèle que celui jugé, et le gate pouvait
l'accepter et le promouvoir. Un seuil de promotion **sous** le plancher du gate est refusé au
chargement du curriculum.
⚠️ `base_seed` de `evaluate_against_checkpoints` est désormais **tiré au hasard** : un gate n'est
plus reproductible à l'identique, et les scores de `curriculum.log` antérieurs portent un
échantillon unique et figé.
**Le rollout ×4 rendait la VRAM intenable — corrigé le 2026-09-07 hors chantier.** Les 3,16 Gio
d'observations du rollout 32640 étaient résidents à la fois en RAM et sur la carte ; ils n'y
partent plus que par mini-lot (3,437 → 0,275 Gio de VRAM mesurés, +3,24 s par update). Sans cela
`n_steps` 32640 n'était pas tenable sur une carte de 8 Go, quel que soit le `batch_size`.
Détail : [infra.md#perf-entrainement](infra.md#perf-entrainement).
**Six défauts de cette livraison fermés le 2026-09-07**, dont un qui rendait la chaîne P3→P10
impossible à démarrer : le LR scalaire du régime était sauvegardé dans le zip promu sous forme
d'objet sérialisé, et l'étape suivante levait au contrôle de continuité.
**Un verdict `promote` publie les poids VIVANTS** (option A) : l'early-stop juge les poids
courants contre tout le pool, mais sous `save_best_robust` le canonique restait l'instantané
robuste — choisi sur le score contre les bots, jamais dégradé — donc le gate mesurait un autre
modèle et pouvait refuser une étape déjà arrêtée, budget perdu. Le seuil de score robuste est
effacé au passage : il survivait à l'étape et aurait bloqué la republication du canonique par la
suivante.
**Cadence des sondes dédoublée** dans la foulée : le champion à chaque sonde, les autres membres
un tour sur `full_pool_probe_every` (3). Sonder tout le pool à chaque fois coûtait 117 000
épisodes d'évaluation bloquants à P10 pour 300 000 entraînés — il en reste ~48 600. Le verdict
lit toujours le pool entier, avec la dernière moyenne connue des membres non sondés.
Détail : [training.md#regime-lignee-2026-09-07](training.md#regime-lignee-2026-09-07).

---

## J3 — Mesure de référence

| # | Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|---|
| 7 | training | Mesure de référence — `--test-only --step` sur champion final P10 (~8 min) | [v11_chemin_critique.md#mesure](v11_chemin_critique.md#mesure) | 🚫 |

---

## J4 — Capacités

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| moteur | ✅ **20.01 — la déclaration de réserves passe AVANT le déploiement** — source : `engine/phase_handlers/deployment_handlers.py` (`SQUAD_ACTION_WAIT`, `reserves_declaration`, `CHOICE_i`, `deploy_commit`). `SQUAD_ACTION_WAIT` portait la mise en réserves pendant le tour de déploiement de chaque unité ; mesuré en déploiement actif sur `scenario_training_armageddon1.json` (`deployment_type`), le joueur 2 gardait le slot ouvert avec **quatre unités adverses déjà posées**, alors que 20.01 situe la déclaration à l'étape Declare Battle Formations, antérieure à toute mise en place. File alternée figée au reset, une question par unité déclarable, type de décision `reserves_declaration` répondu par `CHOICE_i` ; `deploy_commit` refusé tant que l'étape est ouverte. **`obs_size` et `TOTAL_ACTION_SIZE` INCHANGÉS** (slot de type réservé) — aucun `--new` imposé par la taille, mais la politique de déploiement change et chaque épisode gagne une décision par unité déclarable : **les win-rates d'avant ne sont plus comparables**. Bots et déploiement `auto` déclinent, par la même source unique. Livré le 2026-09-10 | [moteur.md#declaration-reserves-2001](moteur.md#declaration-reserves-2001) | 🚫 |
| training | ✅ **Obs — canaux « zone obscurante » et « exposition »** (`GRID_CHANNELS` 9→11, porté à **12** depuis par la verticalité du move gym) — **mergé le 2026-09-08** (`dd8a24be`) ; `obs_size` ne bouge pas, la grille étant fournie à part, mais la forme d'entrée du CNN change : **`--new` obligatoire** pour tout modèle antérieur | [training.md#canaux-obscurant-exposition](training.md#canaux-obscurant-exposition) | 🚫 |
| training | ✅ **Obs — `hidden` 13.09 sur toutes les entités + porte de détection** — `los_can_see` = visible ET détectable ; mergé le 2026-09-08, `obs_size` inchangé (16811), aucun `--new` propre | [training.md#hidden-detection-obs](training.md#hidden-detection-obs) | 🚫 |
| moteur | ✅ **13.06 — le move gym peut FINIR EN HAUTEUR** — déclaration de montée sur les `CHOICE_*` existants, niveau par figurine, coût vertical facturé à celle qui monte ; livré le 2026-09-09. `obs_size` 16971 → **17055** et `GRID_CHANNELS` 11 → **12** : **`--new` obligatoire**. Rendement mesuré : 0,8 % des cellules masquées | [moteur.md#verticalite-move-gym](moteur.md#verticalite-move-gym) | 🚫 |
| training | ✅ **Obs — CONTEXTE des points d'arrêt à deux temps** — sélection d'arme de mêlée (§0.69) et sous-état CIBLE du tir fractionné (P3-8) demandaient un choix dont la moitié déjà fixée — la cible pour l'un, l'arme pour l'autre — n'était PAS observée : deux états ne différant que par elle rendaient des observations identiques (écart mesuré 0.0). `fight_target_selected` par entité + `shoot_weapon_selected` par profil d'arme, plus les assignations déjà faites du tir fractionné (10 activations sur 12 empilaient plusieurs armes sur la même cible, mesuré) — d'abord comptées par `n_weapons_assigned`, champ depuis remplacé par les dix `split_assigned_w<i>` (ligne ci-dessous) ; livré le 2026-09-09. `obs_size` 17091 → **17795** : **`--new` obligatoire** | [training.md#contexte-points-arret-obs](training.md#contexte-points-arret-obs) | 🚫 |
| training | ✅ **Obs — rôle et PV par figurine (retrait de cohérence 03.03)** — `COHERENCY_SLOT_i` désigne la ligne `i` de `self_models_*`, scorée par un produit scalaire nu sans biais de slot : le rôle n'existait qu'agrégé PAR TYPE et les PV courants n'étaient plus observés par figurine depuis §9.4, si bien que 67 paires de figurines de valeur différente sur 67 portaient une ligne identique (mesuré sur 16 épisodes gym, 8 points d'arrêt) — le choix entre le Warboss et un Boy était un tirage au sort. One-hot de rôle + `wounded` + `hp_ratio` par figurine, et `coherency_removal_pending` (obs identique avec/sans le pending, écart 0.0) ; livré le 2026-09-09. `obs_size` 17795 → **17916** : **`--new` obligatoire**, déjà exigé par les lots du même jour | [training.md#role-pv-figurine](training.md#role-pv-figurine) | 🚫 |
| training | ✅ **Obs — seuil et déclenchement du Battle-shock** — `leadership` (Ld le plus bas des figurines vivantes, 01.06) et `battle_shock_test_due` (le prédicat exact de 08.03 : choquée OU à/sous demi-effectif) par entité, +64. **Maillon à part : il ne comble aucun écart d'observation**, et c'est mesuré — sur les 31 compositions de tous les rosters de config/agents/ (agent, adversaire, benchmarks), aucune paire ne partage sa signature observée avec un Ld effectif différent, et le Ld n'a varié dans aucune des 66 escouades suivies sur 6 épisodes gym (11 jets, 3 échecs, Ld 5 à 8 = 17 % à 58 %). Ce qu'il achète est la validité HORS corpus : la table profil → Ld tient en 26 lignes et se périme à la première faction ajoutée, et à force de départ 1 l'appendice 25 mesure les PV, pas les figurines. Livré le 2026-09-09 sur décision de l'utilisateur, contre la recommandation de ne rien ajouter. `obs_size` 18205 → **18269** : **`--new` obligatoire**, déjà exigé par les lots du même jour | [training.md#leadership-battle-shock](training.md#leadership-battle-shock) | 🚫 |
| training | ✅ **Obs — couples arme→cible du tir fractionné** — une fois un couple commité, l'agent choisissait l'arme suivante puis sa cible sans voir ce qu'il avait déjà envoyé et sur qui : les dix bits mis à 0, deux états ne différant que par la cible déjà assignée rendent des observations identiques sur les 28 clés, aux DEUX sous-états, et le masque ne le disait pas non plus (une cible prise reste éligible). `split_assigned_w0..9` par entité (+320) **en remplacement** du comptage `n_weapons_assigned` (−32), dont ils sont le `popcount` et qui perdait l'appariement — 04.03 cumule les dés des armes à attaques identiques sur une même cible, et 55 % des escouades Armageddon portent ≥ 3 profils de tir (75 % de celles capables de split-fire, jusqu'à 6). Coût réseau mesuré : +1 152 paramètres. Livré le 2026-09-09. `obs_size` 17916 → **18204** : **`--new` obligatoire**, aucun artefact du disque n'était déjà compatible | [training.md#couples-arme-cible-split-fire](training.md#couples-arme-cible-split-fire) | 🚫 |
| training | ✅ **Obs — siège premier / second joueur** — `is_my_turn` disait qui a la main, jamais si l'adversaire rejoue APRÈS moi dans ce round : aucun des onze registres ne nommait le joueur (tous égocentriques) et le seul signal restant était un proxy géométrique qui se dégrade quand les unités ont traversé la carte, soit précisément au round 5 — où le premier joueur a déjà marqué son primaire (command phase) quand le second le marquera après sa fight phase. `i_play_first` dans `GLOBAL_BIN_FIELDS`, posé depuis l'OBSERVATEUR et non `current_player` ; sondé sur 217 points d'arrêt du chemin de production, 0 désaccord. Écart de siège mesuré, non encore corrigé : 0,707 contre 0,586. Livré le 2026-09-09. `obs_size` 18204 → **18205** : **`--new` obligatoire**, déjà acquis — aucun `.zip` du disque ne se rechargeait | [training.md#siege-premier-joueur-obs](training.md#siege-premier-joueur-obs) | 🚫 |
| training | ✅ **Obs — candidats de décision DISCERNABLES** — `decision_options_cont` était rempli par le moteur mais lu par AUCUN réseau : cinq types de décision sur neuf présentaient des candidats à embedding identique (écart mesuré 0.0), donc un tirage au sort sous la tête pointeur. Câblage + registre porté à 8 colonnes nommées + les cinq types alimentés ; mergé le 2026-09-09 (`0049f9ab`). `obs_size` 17055 → **17091** : **`--new` obligatoire** | [training.md#candidats-decision-discernables](training.md#candidats-decision-discernables) | 🚫 |
| moteur | ✅ **13.09 — statut « caché » rafraîchi à chaque perte ET à chaque tir** — option C livrée le 2026-09-08 aux deux choke-points, un par membre de la règle ; change les parties jouées, `--new` obligatoire pour tout modèle pré-diff | [moteur.md#hidden-fraicheur](moteur.md#hidden-fraicheur) | 🚫 |

Autres tâches J4 archivées → `archives/doc.md#hygiene-correctifs-ponctuels`

---

## Suspendus — ne pas commencer avant leur jalon

| Sujets | Chantier | Fichier | Jalon | ⚡/🚫 |
|---|---|---|---|---|
| moteur | **T7** Unification validation déploiement | [moteur.md#t7](moteur.md#t7) | Fix faux — re-analyser avant | 🚫 |
| moteur+training | **Force dispositions & missions** — home obj, VP scoring 4 missions, obs mission, reward conditionné, sampling, métriques | [missions.md](missions.md) | Terrains 4 missions créés par utilisateur | 🚫 |
| moteur | **Phase B** Observation des niveaux | [moteur.md#phase-b](moteur.md#phase-b) | Phase A' validée + LoS 3D complet | 🚫 |
| training+bot | **É9** Second siège + second scénario — levier `agent_seat_p2_ratio` livré 2026-08-28, plus deux correctifs de suivi le même jour : `_resolve_seat_p2_ratio` et `get_seat_stats` retournent `None` en modes de siège fixes au lieu d'un `0.5` interne trompeur (`ai/env_wrappers.py`, `ai/train.py`, tests rouge→vert). Le second SCÉNARIO reste ouvert | [training.md#e9](training.md#e9) | J4 — entraînement bot satisfaisant | 🚫 |
| training+bot | Validation qualitative §10.6 volet 2 | [bot.md#validation-externe](bot.md#validation-externe) | J5 — requis pour la démo | ⚡ |
| bot | **Recherche (MCTS) — siège A, inférence de l'agent** : plan B anti-coups-absurdes, ET la mesure qui commande les deux autres sièges (gate G1). Spec unique re-écrite sur mesures le 2026-09-08, `Documentation/Chantiers/backlog/mcts.md` — elle remplace `mcts_adversaire.md` et couvre aussi la ligne infra ci-dessous | [bot.md#mcts-inference](bot.md#mcts-inference) | Après J3, seulement si la démo l'exige | 🚫 |

---

## ✅ Hygiène — correctifs ponctuels

Correctifs ponctuels livrés → `archives/doc.md#hygiene-correctifs-ponctuels`

Non-régression, trois axes (2026-09-09) : `worktree-non-regression-C-D-A`. **(C)** La couche B de `scripts/front_test_all.sh` était **rouge par construction** — `npx vitest run` collectait `frontend/tests/e2e/smoke.spec.ts`, un fichier Playwright, et rendait `1 failed | 36 passed` quel que soit l'état du code ; frontière déclarée dans `frontend/vite.config.ts`, verrouillée par `tests/unit/scripts/test_vitest_collect_scope.py`, et `npx vitest run` (4,0 s, 430 tests) entre dans la vérification large de CLAUDE.md. La couche C **a été exécutée pour la première fois le 2026-09-09** (Playwright installé) : **6 min 42, 13 rouges et 1 skippé sur 14 — aucun vert**, elle reste donc dehors. Quatre défauts la rendaient inexécutable, trois corrigés (`__dirname` en module ES, proxy `/api` figé sur 5001 au lieu du backend de test 5098, artefacts non ignorés dont `.auth/` porteur d'un cookie de session) ; le quatrième était un **serveur fantôme** et non un défaut d'API : `npx vite` laissait un `node …/vite` orphelin, et le run suivant, port pris, laissait Playwright piloter le serveur du run précédent avec son ancien proxy — corrigé par `setsid` + kill de groupe + refus sur port occupé. Bilan FINAL sur un harnais assaini : **14 verts sur 14 en 45 s**, et la couche C **entre dans la vérification large** — avec un pré-vol qui distingue « prérequis absent » (🟠, sans effet sur le code de sortie) d'un vrai échec, et la baseline visuelle désormais VERSIONNÉE — et non « 13 rouges en 6 min 42 », chiffre issu des runs pollués. ⚠️ Les 2 rouges sont les **invariants de parité front/back**, qui passaient jusqu'ici SANS RIEN COMPARER : 401 silencieux faute d'en-tête CSRF, `return` sans assertion, et pool lu au niveau racine alors que l'API l'enveloppe dans `game_state` (∅ ⊆ tout). Les trois causes corrigées, plus une quatrième — ils ne construisaient pas la phase move qu'ils prétendent vérifier : le test l'atteint désormais PAR L'INTERFACE, en répondant d'abord aux deux modales que le jeu ouvre au démarrage (décision Waaagh! 08.04, dialogue d'enregistrement du replay), dont le fond interceptait tous les clics. **Onze défauts au total, tous corrigés** — dont un test qui se skippait à chaque run faute d'avoir navigué avant d'interroger `window`. Un 5ᵉ défaut corrigé au passage : le test appelait l'API sans l'en-tête `X-W40K-Client`, exigé sur toute requête authentifiée par cookie, et recevait 401. Son entrée dans la vérification large reste à trancher. Cf. `Documentation/Roadmap/front.md#couche-c`. **(D)** `ai/training_contract.py` : un modèle ne reprend plus si l'observation, les familles d'actions ou les **clés** de récompense ont changé depuis son apprentissage — le trou que ni `check_model_lifecycle`, ni le verrou de parité, ni `check_for_correct_spaces` de SB3 ne voient (dérive à taille constante). Les **valeurs** de récompense ne sont pas comparées : ce sont des réglages. **Correctif du 2026-09-09** (`check_ai_rules`) : le contrat enregistré était lu par `.get(clé, {})`, donc un fichier **abîmé** (section absente, vide, du mauvais type, version non entière) se comparait comme s'il était vide et sortait en « le contrat a changé » avec les 17 registres du code annoncés « nouveaux » — un diff faux dont la seule sortie affichée est `--new`. Il **lève** maintenant, en nommant la section et le fichier ; 3 tests ajoutés pour ce chemin, 3 de plus pour les deux findings de la revue (version `"1"` textuelle qui sautait le contrôle, section présente mais vide). **(A)** `scripts/mutation_ciblee.py` : mutation testing sur les seules lignes du diff, qui automatise la preuve rouge/vert que T4 exige à la main une fois. ✅ **Hook durci le 2026-09-09** : `deny-verif-large.sh` protège désormais `vitest` (hors `vitest list`), `playwright test` (mais PAS `playwright install`, qui doit rester possible), `scripts/front_test_all.sh`, et les **scripts npm** — `npm run test:run` lançait toute la suite vitest sans jamais écrire son nom, donc la protection était contournable par une ligne ; la liste des scripts larges est lue du CONTENU de `frontend/package.json`, un fichier illisible étant refusé et non ignoré. 24 formes ajoutées aux listes paramétrées de `test_hooks_garde_fous.py` (197 verts). **Coût mesuré le 2026-09-09** (40 à 60 appels par cellule, processus complet) : le hook coûte ~14 à 16 ms par commande Bash ; lui faire lire `frontend/package.json` — nécessaire parce que `npm run test:run` lance vitest sans jamais écrire son nom — a un coût **sous le bruit de mesure**. Le chiffre « +0,25 ms » d'une première passe n'était pas un signal : deux séries du MÊME hook diffèrent de −0,23 ms, et l'écart-type intra-série va de 0,64 à 1,51 ms. Ce qui est établi est une borne, pas une valeur : **moins d'une milliseconde**. ⚠️ À condition de **n'ajouter aucun import** : une première esquisse avec `import pathlib` coûtait **+4,11 ms**, et c'est l'import qui payait, pas la lecture. `open()` + `json`/`re`, déjà présents, suffisent. La liste figée de noms de scripts, envisagée comme repli, n'est donc pas nécessaire. Revue `/code-review` de la livraison : **6 findings, tous corrigés dans la foulée** — `--resume-from` cassait systématiquement (le contrat était écarté sans être reposé), le contrat empreintait la table de `--agent` au lieu de celle du run, deux `--new` d'une même seconde levaient `FileExistsError`, `purge_pycache` balayait `.venv` (539 caches), `TimeoutExpired` n'était pas capturé, et le garde « test orphelin » ignorait les `*.spec.ts` de `src/`.

Correctifs hors chantier (2026-09-08, suite 17) : `worktree-oath-hors-table` — **le run `--etape P1` mourait sur `ValueError: Oath of Moment : designation en attente mais aucun slot ouvert`, et la cause était un prédicat de trop peu.** `def oath_selectable_enemy_ids` (`engine/phase_handlers/command_handlers.py`) ne testait que `is_unit_alive`, alors que la désignation n'est exprimable que par un `OATH_SLOT` et que `def _refresh_enemy_slot_mapping` (`engine/phase_handlers/shared_utils.py`) n'accorde de slot qu'aux escouades SUR LA TABLE. Une escouade en réserves stratégiques (20.01) reste vivante dans `units_cache` avec la sentinelle `(-1,-1)` : l'armement passait, le mapping ne rendait rien, le masque levait. **État atteignable sans rien forcer** — l'adversaire garde ≤ 50 % de sa liste en réserve, on détruit ce qu'il a déployé, et sa command phase suivante trouve une armée entièrement hors table. Reproduit par exécution avant correction (`selectable=['2']`, `pending_oath=1`, `enemy_slot_mapping=[None×6]`, crash identique au log), puis re-joué vert. **Trois jumeaux du même motif « vivant confondu avec sur la table », chacun mesuré avant d'être fermé** : `def player_has_squads_on_board` rendait `True` pour un joueur entièrement en réserve, armant Waaagh! ET Oath sur une ancre hors table ; `def _observer_squad_for_pending_decision` (`engine/w40k_core.py`) prenait cette ancre-là quand une escouade posée existait ; `def set_oath_target` (`engine/game_state.py`), écrivain unique, acceptait une cible que le masque n'ouvre plus. Les deux libellés d'erreur qui DÉCRIVENT la garde amont (`engine/action_decoder.py`, `def _select_ai_oath_target`) ont suivi. **`/code-review` a trouvé un quatrième site et une régression front, tous deux fermés** : le repli du pool vide de `def _build_observation_and_mask` — le chemin qu'emprunte justement l'Oath, qui n'est pas un `pending_agent_decision` — choisissait encore une escouade en réserve (ancre `col=-1` mesurée) ; et `oathTargets` (`frontend/src/components/BoardWithAPI.tsx`) filtrait toujours sur `HP_CUR > 0`, si bien que le durcissement de `set_oath_target` bloquait la phase sur un clic — extrait dans `frontend/src/utils/oathTargetSelection.ts` avec test rouge→vert. Correctif de fond plutôt qu'un cinquième filtre recopié : les trois prédicats dérivent désormais des helpers nommés du dépôt (`deployed_friendly_squad_ids`, `enemy_entries_on_battlefield`) et les deux replis d'ancre passent par `def _first_squad_on_board`, source unique — c'est la duplication qui avait produit la divergence. **Écart de règle assumé** : « select one unit from your opponent's army » (`Armageddon/OathOfMoment.txt`) ne restreint pas aux unités posées, mais une escouade hors table ne peut être ni ciblée ni chargée, et lui ouvrir un slot casserait l'invariant D1 partagé avec le tir, la charge et la mêlée. Verrous : 6 tests moteur + 10 vitest, chaque défaut réintroduit puis constaté rouge (`__pycache__` purgé à chaque mutation). Non-régression : 471 tests verts (capacités de faction, réserves stratégiques, observation d'Oath, `env_wrappers`, décisions d'agent, PvP command/invariants/deploy).

Correctifs hors chantier (2026-09-08, suite 18) : `worktree-refactor-00critical-metrics` — **deux courbes de santé PPO ajoutées au dashboard critique, deux courbes de jeu retirées, et le tableau de `metriques.md` réaligné sur le code.** `00_critical/l_approx_kl_max` (source `train/approx_kl_max`, publiée par `ai/patched_ppo.py`) et `00_critical/m_explained_var` portent la valeur **BRUTE** de l'update, écrites depuis `def log_training_metrics` : la KL maximale est celle qui déclenche l'early-stop de PPO à `1.5 × target_kl`, donc un lissage sur 20 updates effacerait exactement le pic recherché, et `m` est la jumelle non lissée de `h_explained_variance`. `d_win_rate` et `e_episode_reward_smooth` quittent le namespace — `game_critical/win_rate` est la **jumelle exacte** de l'ancienne `d` (même source `all_episode_wins`, même `_emit_windowed`), le lissage de `e` n'en a aucune. Les lettres `m` à `t` décalent en `n` à `u` (12 fichiers de code, 6 de doc). **Verrous** : deux tests neufs sur les courbes brutes, dont un qui interdit tout repli sur `train/approx_kl` quand le PPO patché est absent (rouge prouvé par mutation, `__pycache__` purgé) ; le test de fenêtre de `test_metrics_single_writer.py` et celui de cadence de `test_metrics_tracker_utils.py`, qui sondaient tous deux les courbes supprimées, sont réancrés sur `game_critical/win_rate` et `o_intent_zone_steps` — la seule courbe par-épisode que `log_critical_dashboard` écrive encore hors garde PPO. **Doc auditée ligne à ligne contre le code** : le tableau porte 25 lignes pour 25 tags émis, tri alphabétique vérifié par script ; `ckpt_mean`/`ckpt_min` étaient inversés, la ligne `m` décrivait « convergence du critic (value_loss) » pour une métrique de myopie, le bloc « Organisation » annonçait `g–j` pour cinq courbes `g–k`, et `entrainement.md` listait trois lettres fausses (`g_approx_kl`, `h_entropy_loss`, `e_explained_variance`) **déjà périmées avant ce chantier**. **Anomalie documentée, non corrigée** : `o_intent_zone_steps` et `o_robust_current_score` partagent le préfixe `o_`, ce qui insère le critère de sélection entre les deux métriques de la tête d'intent — collision antérieure au décalage, dont la correction casserait la comparabilité avec les runs archivés. 229 tests verts. Reste 4 findings `/code-review` ouverts (docstring citant un `log_ppo_update` inexistant, paragraphe « deux tags en `o_` » renommé mécaniquement, commentaire de cadence de `_emit_truncation_curve`, cellules « Idem `p_` » du tableau).

Correctifs hors chantier (2026-09-08, suite 19) : `worktree-obs-unit-rules-vocab` — **le chantier 06 avait registré 14 `obs_id` que l'observation ne pouvait porter, donc 14 règles vives que l'agent subissait sans les percevoir.** `def unit_ability_obs_ids` (`engine/observation_builder.py`) ne construit sa table QUE sur `UNIT_RULE_EFFECT_IDS` (`engine/observation_entities.py`) ; les passes 3 à 5 avaient posé leurs `obs_id` dans `config/unit_rules.json` sans ajouter la règle au tuple, et rien ne contrôlait cette direction du contrat — le chargeur n'exigeait que l'inverse. Les 14 : `deadly_demise` (§24.08, `def destroy_model`), les six règles d'arme accordées de la primitive B (`def build_weapon_attack_profile`, `def _manual_roll_intent`), `melee_attacks_bonus_while_waaagh`, les deux FNP conditionnels (`def _collect_fnp_thresholds_mortal`), les deux blessures mortelles (`def _manual_roll_fight_intent`, `def _check_and_trigger_exhortation_de_rage`), `secure_objective_on_control` et `oc_bonus` (`def unit_effective_oc`, source unique de l'OC du contrôle 14.02). **Vivacité de chacune vérifiée par lecture du site qui l'applique**, pas de sa déclaration, et les deux rollers manuels sont atteints depuis `w40k_core` — chemin IA autant que PvP. Vocabulaire **24 → 38**, `obs_size` **inchangé** : le canal est un embedding d'identifiants, pas des bits positionnels. Verrou de contrat posé dans l'autre sens — **tout `obs_id` déclaré est un `obs_id` observé**, sans liste d'exceptions : une règle inerte ne doit porter aucun `obs_id`. `UNIT_ABILITY_SLOTS = 8` cesse d'être une projection : mesure sur les quatre rosters d'entraînement chargés par le chemin du moteur (`agent_roster_ref` → fold 19.04, aucune reconstitution) → **6 au maximum** (`Boyz + Warboss + PainBoy`), marge 2. Registres jumeaux vérifiés : 0 `obs_id` orphelin côté `config/weapon_rules.json` et `config/unit_statuses.json`. 4 rouges constatés en remettant le défaut (`__pycache__` purgé), 19 verts après ; 115 verts sur les fichiers voisins du canal `ability_ids`. ⚠️ **L'observation change de CONTENU à taille constante** : un modèle entraîné avant cette livraison lit des identifiants qu'il n'a jamais vus. Détail : [archives/capacites.md](archives/capacites.md#armageddon-06).

Correctifs hors chantier (2026-09-08, suite 20) : `worktree-early-stop-merge-validation` + `worktree-simplify-early-stop-tests` — **la surcharge `early_stop` du profil d'entraînement était validée AVANT fusion et jamais après, et son cas le plus dangereux passait en silence.** Dans `def _run_main` (`ai/train.py`), la garde `if _early_stop_cfg is not None and _tc_early_stop` faisait qu'un profil déclarant `early_stop` sans bloc correspondant dans le curriculum ni dans l'étape courante voyait sa surcharge **ignorée sans un mot** : le réglage était écrit, lu, et sans effet. Elle lève désormais en nommant le profil et les deux remèdes. Les clés individuelles étaient bien confrontées à `_EARLY_STOP_REQUIRED_KEYS`, mais le bloc **résultat de la fusion** ne l'était pas — `validate_early_stop_block` est appelée dessus, et `_validate_early_stop_against_gate` (`ai/curriculum.py`) le confronte au bloc `gate` de l'étape dès qu'une surcharge a eu lieu. Cette dernière porte l'invariant qui manquait : `promote_score_vs_champion` et `promote_score_vs_others` ne peuvent pas descendre SOUS `gate.min_score_vs_champion` / `min_score_vs_others`, sans quoi le run s'arrête en se déclarant promu sur un score que le gate de fin d'étape refusera, et le budget non dépensé est perdu avec l'étape — or une surcharge PARTIELLE est exactement ce qui produit ce couple incohérent. Le helper de test `_gate_block` omettait `eval_repeats`, donc la confrontation au gate qu'il servait était elle-même incomplète. Le second worktree paramètre les tests de surcharge et ramène le bloc `gate` des tests à une source unique.

Correctifs hors chantier (2026-09-08, suite 21) : `worktree-obs-oc-live-secured` — **+20 scalaires dans le contexte global, `obs_size` 16 791 → 16 811, `retrain --new` obligatoire.** L'agent voyait `objective_control_{i}` — la vérité 14.02 de **FRONTIÈRE**, relue d'`objective_controllers` et donc figée jusqu'à la fin de la phase — et rien d'autre : pendant tout son mouvement, il ne pouvait pas savoir ce que ses propres déplacements faisaient au contrôle qu'il jouait. Dix continus `objective_my_oc_{0..4}` / `objective_enemy_oc_{0..4}` (`GLOBAL_CONT_FIELDS`) portent l'OC **LIVE**, calculé sur les positions COURANTES par `sum_objective_control_oc_multi` (`engine/game_state.py`) et non lu de l'état de frontière — les deux sémantiques coexistent délibérément et sont documentées côte à côte. Les figurines sous choc y contribuent 0, la fonction portant déjà la règle. Dix binaires `objective_secured_mine_{0..4}` / `objective_secured_enemy_{0..4}` (`GLOBAL_BIN_FIELDS`) portent le statut **secured 14.03**, qu'aucun champ n'exposait : un objectif sécurisé reste tenu sans OC présent tant que l'adversaire n'a pas strictement plus d'OC en fin de phase — sans ce bit, un objectif vide et un objectif sécurisé sont indiscernables dans l'observation. **Le placement en BIN n'est pas cosmétique** : VecNormalize ne touche que `GLOBAL_CONT`, et un champ 0/1 y aurait une variance ≈ 0, donc un écrasement au clip ±10 sur `--append`. **Coût mesuré plutôt que supposé**, `sum_objective_control_oc_multi` étant la fonction dominante : 500 steps avec, puis la même série la fonction patchée à zéro → **3,8 % de surcoût**, sous le seuil de 5 % au-delà duquel la mémoïsation au point d'invalidation était prévue — elle n'est donc pas posée. Verrous : `tests/unit/engine/test_squad_obs_objective_oc.py` (6), dont celui qui sépare les deux sémantiques — après un mouvement ennemi sur la zone et AVANT la frontière, `objective_my_oc_0` bouge et `objective_control_0` ne bouge pas. Rouge prouvé par mutation. Doc et config d'équerre dans la même livraison : indices de `observation_et_actions.md` décalés (waaagh 27 → 37, oath 31 → 41, `reserved_mission_bin_*` 35 → 45), `obs_size` des six profils de `ArmageddonAgent_x1` et la valeur recopiée d'`entrainement.md`. ⚠️ **La taille de l'observation change** : tout modèle antérieur est explicitement incompatible.

Correctifs hors chantier (2026-09-08, suite 22) : `worktree-fix-fallback-secured-objectives` + `worktree-bump-save-format-tl04` — **le repli `game_state.get("secured_objectives", {})` de `def build_squad_observation` (`engine/observation_builder.py`) est remplacé par `require_key`, et cette correction a mis au jour un contrat de save en retard de neuf clés.** La clé est posée à chaque reset d'épisode (`engine/w40k_core.py`) et lue en accès direct par les trois autres lecteurs de production (`engine/game_state.py`) : le repli était le dernier survivant, et il aurait publié un statut secured faux au lieu de lever. **Le second worktree ferme ce que le premier a révélé** : une save ne stocke que le MUTABLE du `game_state`, le décor et la config étant ré-attachés depuis le moteur vivant (`_GS_STATIC_KEYS`, `services/game_snapshots.py`), donc toute clé obligatoire ajoutée au reset après la frappe d'une magic manque aux saves antérieures — et `apply_live_state` écrase la partie en cours AVANT que le premier lecteur ne lève. Neuf clés étaient dans ce cas depuis TL03 (2026-08-04) : `secured_objectives`, `suppressed_squads`, `_reserves_placed`, `_reserves_deployed`, `_ingress_offered`, `_ingress_no_destination`, `_ingress_arrived`, `_pending_reserves_wasted`, `_restored_model_counter`. Magic **TL03 → TL04**, TL03 refusé explicitement avec son motif — même arbitrage que pour `command_points` en TL03, aucune migration, aucune réinjection. **La parade automatique a été écartée sur MESURE, pas par principe** : une vérification à l'exécution « clés du moteur vs clés de la save » est impossible, l'ensemble n'étant pas stable — dans un même fichier, à version et configuration identiques, deux points capturés à 35 s d'intervalle portent 133 puis 139 clés, et 162 clés distinctes sont écrites dans le `game_state` à travers le dépôt, la plupart nées paresseusement. Le verrou retenu est donc un TEST à configuration fixe, `tests/unit/services/test_save_format_key_contract.py`, qui épingle par magic l'empreinte des clés mutables posées par le reset (déterminisme vérifié sur trois processus) : ajouter une clé au reset sans bumper la magic le rend rouge, message à l'appui. **Portée annoncée plutôt que surestimée** : il ne voit ni les clés obligatoires créées hors reset, ni un changement de FORME à nom constant ; les neuf clés de la dérive observée sont, elles, toutes dans son périmètre — un test paramétré le prouve clé par clé. Les deux littéraux `b"W40KTL03"` en dur de `tests/unit/services/test_game_saves_restricted_unpickle.py` passent par la constante du module, pour que le prochain bump ne les casse plus. Rouge prouvé par mutation aux deux endroits (`__pycache__` purgé) : sonde de clé insérée dans le reset → rouge nommant la clé ; retour de la magic à TL03 → trois verrous rouges. 33 tests verts sur les fichiers touchés et les voisins de save. `Documentation/Chantiers/Replay.md` mis à jour dans la même livraison.

Correctifs hors chantier (2026-09-09, suite 23) : `worktree-ez-frozenset-fingerprint` — **les zones d'engagement sont PUBLIÉES immuables, et le fingerprint du cache spatial du move cesse de re-hacher leur contenu à chaque lecture.** `def _move_spatial_cache` (`engine/phase_handlers/shared_utils.py`) relit l'état plutôt qu'un compteur — c'est la garantie §0.18, un compteur y avait déjà servi un cache périmé. Mais relire le CONTENU des deux `enemy_adjacent_hexes_player_*` coûtait **162,4 µs sur les 184,2 µs de chaque appel (88 %)**, pour 15,5 appels par step : 137 µs de `hash(frozenset(...))` sur 7 942 hexes cumulés et 25,5 µs de `sorted(game_state.items())` sur 167 clés pour en retrouver deux. **Les positions des figurines, elles, ne pesaient que 13,4 µs (7,3 %)** — rendre LEUR hachage incrémental côté écrivains, la piste envisagée d'abord, aurait visé 7 % du poste en répartissant l'invariant §0.18 sur une dizaine de chemins d'écriture. La correction publie ces ensembles en `frozenset` : CPython calcule alors leur hash depuis leur contenu et le met en cache **sur l'objet** (0,03 µs contre 26 µs de reconstruction). Ce n'est pas le compteur interdit, et c'est **plus sûr que la relecture** — un ensemble immuable ne peut pas changer de contenu sans devenir un autre objet, donc aucun chemin d'écriture, présent ou futur, ne peut périmer l'empreinte. La branche de repli (`_hex_set_content_hash`) garde la relecture mot pour mot pour tout ensemble MUTABLE, si bien qu'une save antérieure au gel reste correcte et qu'**aucun bump de magic n'est requis** — `tests/unit/services/test_save_format_key_contract.py` ne couvre pas un changement de FORME à nom de clé constant, et `_safe_loads` accepte `frozenset` sans élargir `_ALLOWED_CLASSES` (types de base hors `find_class`). **Trois gardes de type touchées**, dont une SILENCIEUSE : `enemy_adj if isinstance(enemy_adj, set) else None` (`movement_handlers.py`) basculait un frozenset sur la branche de repli — bénigne (le repli relit la même clé pour le même joueur) mais non voulue ; les deux autres (`maybe_resolve_reactive_move`, l'override réactif) lèvent. 10 annotations élargies en `AbstractSet`, pyright vert sur les trois fichiers. **Mesure : 184,2 → 26,7 µs par appel (−85 %)**, sonde de 40 échantillons répartis sur 80 steps ; **le wall du bench ne résout pas ce gain** (variance machine ±30 % sur 3 répétitions de 300 steps, distributions superposées) — journal §6 de `perf_entrainement.md`. Verrous : `tests/unit/engine/test_ez_cache_frozen_fingerprint.py` (9), dont celui qui interdit un jeton d'identité déguisé (deux objets distincts de même contenu doivent GARDER le cache) et celui qui couvre la fenêtre de réaction — chemin qu'aucune mesure n'exerce (0 appel sur 80 steps) et que `test_reactive_move.py` ne couvrait pas non plus, puisqu'il pose lui-même ces clés en `set` nu. Cinq mutations rouges constatées, `__pycache__` purgé ; **la première rédaction du test d'état final restait VERTE sous la mutation du site d'ouverture de fenêtre** (la republication de sortie l'écrase) et a été réancrée sur ce que le fingerprint LIT pendant la fenêtre. 369 tests verts sur les fichiers touchés et leurs voisins.

Correctifs hors chantier (2026-09-09, suite 24) : `worktree-keyword-beasts-pluriel` — **le mot-clé officiel `BEASTS` n'était reconnu par aucune règle de terrain, et DEUX fautes se compensaient.** Le PDF `13 Terrain` l'écrit au PLURIEL aux trois endroits qui l'emploient — 13.06 « keywords: INFANTRY/**BEASTS**/SWARM/FLY/MONSTER », 13.08 et 13.09 « That model has the INFANTRY/**BEASTS**/SWARM keyword » — mais `_HIDEABLE_KEYWORDS` (`engine/game_state.py`) portait `"beast"` au SINGULIER, et `_FLOOR_CAPABLE_KEYWORDS` en dérive. Conséquence sur le chemin de production : `FenrisianWolf`, qui déclare la forme conforme, n'était ni hideable (13.08 Benefit of Cover, 13.09 Hidden) ni autorisée à finir un move hors rez-de-chaussée (13.06) — `compute_hideable` et `unit_can_occupy_upper_floor` rendaient `False` sur ses mots-clés réels. **La constante était la SEULE anomalie du moteur** : les onze autres mentions (docstring de `unit_can_occupy_upper_floor` incluse, `engine/terrain_utils.py`, `movement_handlers.py`, `shooting_handlers.py`, `deployment_handlers.py`, `geodesic_move.py`, `observation_builder.py`) écrivent toutes `BEASTS`, si bien que le code contredisait sa propre documentation. **Corriger la constante seule aurait cassé le hideable de `Mucolid`**, qui déclarait `"beast"` — forme non conforme au PDF, fonctionnelle uniquement parce que la constante portait la même faute : les deux moitiés bougent donc ensemble, `frontend/src/roster/tyranid/units/Mucolid.ts` passe à `"beasts"`. *(Rectifié le 2026-09-09, suite 25 : la compensation ne portait QUE sur 13.08/13.09 — `Mucolid` déclare aussi `fly`, donc `unit_can_occupy_upper_floor` lui rendait `True` quelle que soit l'orthographe. `FenrisianWolf`, lui, ne porte que `beasts`, `imperium` et `fenrisian wolves`, sans mot-clé de secours : il perdait bien les trois droits.)* `SWARM` est singulier dans le PDF et `RipperSwarm` le déclare ainsi — **pas de jumeau de ce défaut** côté SWARM, vérifié. Verrou : `tests/unit/engine/test_beasts_keyword_1306_conformity.py` (10), dont un contrat de données qui refuse toute datasheet déclarant une forme non officielle de la famille, et un bout-en-bout sur le registre RÉEL avec garde d'énumération non vide. 10 rouges constatés avant correction, 10 verts après ; 215 verts sur les fichiers voisins (hideable, hidden 13.09, couvert, étages, déploiement, observation) et 15 en intégration PvP tir. Grep jumeau `"beast"` singulier sur `engine/`, `ai/`, `services/`, `frontend/src/`, `config/`, `tests/` → **0 résidu**. **Portée annoncée plutôt que surestimée** : `FenrisianWolf` et `Mucolid` sont absents des rosters d'entraînement (`config/agents/_p2_rosters/`), donc aucun modèle n'est concerné et `obs_size` ne bouge pas — le défaut ne se manifestait qu'en PvP.

Correctifs hors chantier (2026-09-09, suite 25) : `worktree-conformite-mots-cles-terrain` — **le verrou de la suite 24 ne couvrait que BEASTS et par un prédicat de préfixe ; il porte maintenant sur les CINQ mots-clés de terrain, par distance d'édition.** Le PDF `13 Terrain` a été relu : « SETTING UP OR ENDING A MOVE » (sous-section de 13.06) écrit `INFANTRY/BEASTS/SWARM/FLY/MONSTER`, 13.08 et 13.09 écrivent `INFANTRY/BEASTS/SWARM` — les deux constantes moteur sont conformes, vérifié. **Deux contrôles là où il n'y en avait qu'un, et le PDF sert de référence aux DEUX côtés** : `test_engine_constants_match_pdf_spelling` fige `_HIDEABLE_KEYWORDS` et `_FLOOR_CAPABLE_KEYWORDS` sur les formes du PDF — c'est le contrôle qui aurait vu le défaut d'origine, et un attendu dérivé des datasheets l'aurait manqué puisque `Mucolid` portait alors la même faute ; `test_registry_terrain_keywords_use_official_spelling` fige la donnée sur ces mêmes formes. **Le prédicat passe de `startswith("beast")` à la distance de Levenshtein 1**, choix mesuré et non supposé : sur les 138 `keywordId` distincts des 161 datasheets, il produit **0 faux positif** (distance 2 aussi), et il attrape les **quatre** familles de fautes réellement présentes dans les rosters — `beast`/`beasts`, `grenade`(22)/`grenades`(34), `harverster`(1)/`harvester`(3), `jump pack`(7)/`jump_pack`(8) — là où le préfixe n'en voit que deux. Les trois dernières ne sont lues par aucun code (`grep` sur `engine/`, `ai/`, `services/` → 0 hit), donc cosmétiques à ce jour, mais elles **prouvent que la faute de frappe est un événement réel dans ce corpus**, pas une crainte théorique. Portée réelle des cinq racines, mesurée : `infantry` 146 porteurs, `fly` 22, `monster` 4, `beasts` 2, `swarm` 1 — aucune n'est un chemin mort. Corpus à **casse mixte** (`infantry` 125 / `INFANTRY` 21, `fly` 16 / `FLY` 6), absorbée par le `strip().lower()` des deux lecteurs `def unit_can_occupy_upper_floor` et `def compute_hideable` (`engine/game_state.py`) — pas de bug latent. **Risque résiduel nommé plutôt que tu** : un test d'orthographe ne voit pas un mot-clé ABSENT ; les 15 datasheets sans `infantry` ont donc été examinées une à une — toutes légitimement VEHICLE, WALKER, MOUNTED, BEASTS, MONSTER ou SWARM, aucun trou. Deux mutations rouges constatées, `__pycache__` purgé : `"beast"` dans la constante (10 rouges, dont le nouveau contrôle moteur) et `infantery` dans `AggressorBoltStorm.ts` (1 rouge nommant l'unité et la forme attendue) — cette seconde faute est invisible à l'ancien prédicat, `'infantery'.startswith('infantry')` étant `False`. 11 verts après rétablissement, 56 sur les fichiers voisins (fly 21.03, hidden 13.09, flags de terrain de l'observation). Fichier de test **non renommé** délibérément : son sujet s'élargit, mais le renommage n'est exigé par aucun critère T2 et l'origine BEASTS reste son ancrage.

Correctifs hors chantier (2026-09-09, suite 25) : `worktree-obs-keywords-categorie` — **+160 scalaires, `obs_size` 16 811 → 16 971, `retrain --new` obligatoire. L'observation disait quel mot-clé une arme [ANTI-X] VISE, sans jamais dire si la cible le portait.** Le keyword ciblé est exposé côté arme depuis toujours (`WEAPON_RULE_OBS_VOCABULARY = WEAPON_RULE_BITS + ANTI_RULE_IDS`, `engine/observation_weapon_profiles.py`, canal `wpn_rule_ids`) ; aucune entité n'exposait les siens, si bien que [ANTI-X] 24.03 était structurellement inapprenable — le réseau ne pouvait que corréler sur T/W. Même trou pour le volet MONSTER/VEHICLE de 10.06 (`shooting_handlers.py`) et pour les gates de terrain 13.06/13.08/13.09, dont les entités ENNEMIES ne portaient rien (`hidden`/`in_cover` sont actif-seul). Cinq bits dans `UNIT_BIN_FIELDS` : `kw_infantry`, `kw_vehicle`, `kw_monster`, `kw_fly`, `kw_psyker`. **La liste est close par la CONSOMMATION du moteur, pas par le livre de règles** — chaque bit cite le site qui le lit, et c'est sa condition d'entrée ; n'entrent donc pas `CHARACTER` (l'allocation 19.02/19.04 lit le RÔLE via `_is_character_role`, déjà observé par `MODEL_TYPE_BIN_FIELDS`, et le mot-clé n'est lu nulle part dans `engine/`), `BEASTS`/`SWARM` (aucune datasheet des rosters d'entraînement n'en porte — sur ce périmètre le bit vaudrait exactement `kw_infantry`), ni `BATTLELINE`/`WALKER`/`MOUNTED`/`GRENADES` (portés par des datasheets, lus par aucune règle). **Des bits positionnels et NON un troisième registre d'`obs_id`**, contre le précédent du chantier 01 : celui-ci a sorti les 13 bits `rule_*` parce que leur vocabulaire croît avec CHAQUE DATASHEET, alors que les mots-clés de catégorie sont fixés par le livre de règles de base — ajouter une faction n'en crée aucun, donc la cause est absente. Coût comparé avant de trancher, et il ne décide de rien : 4 slots d'ids auraient coûté 128 scalaires contre 160 pour les bits, soit **0,19 % de l'observation** ; ce qui décide, c'est qu'un `EmbeddingBag` n'a rien à compresser sur un vocabulaire de 5 et n'y ajouterait qu'une indirection plus un mode de défaillance (débordement) que des bits n'ont pas. Dimensionnement mesuré sur les 179 datasheets : **3 mots-clés simultanés au maximum** (`Neurotyrant`), **2** après union 19.03 sur les 70 paires `CAN_LEAD` légales. **Source de lecture = `unit_keywords_upper`** (`attack_sequence.py`), celle qu'exécute la résolution de [ANTI-X], et non `compute_hideable` : les deux ne normalisent pas pareil, et `_normalize_keyword` documente qu'une seconde normalisation concurrente a déjà pu faire diverger [ANTI] d'une clause interrogeant le même champ. `require_key(unit, "UNIT_KEYWORDS")` posé AVANT l'appel et non redondant — `unit_keywords_upper` rend sciemment un ensemble vide sur une unité sans la clé, juste pour [ANTI] mais masquant ici, où il écrirait « aucune catégorie » sans lever. Verrous : `tests/unit/engine/test_squad_obs_category_keywords.py` (21), dont le « allume SON bit et lui seul » qui attrape une permutation, l'émission sur entités ENNEMIES, et le contrat sur le registre RÉEL avec garde d'énumération non vide. **Deux mutations rouges constatées, `__pycache__` purgé** : source vidée (14 rouges) et permutation `kw_vehicle` ← INFANTRY (7 rouges, dont le cas `kw_infantry` — c'est le « lui seul » qui mord). **315 tests verts sur les consommateurs de l'observation** (schéma, contrat de déploiement, extracteur d'entités, tête pointeur, harnais de parité), **mesurés sur l'état FUSIONNÉ** : le « 231 » publié d'abord venait de la branche avant les merges concurrents du 2026-09-09 (`worktree-verticalite-move-gym`, `worktree-conformite-mots-cles-terrain`) et n'a jamais été obtenu sur l'état réellement livré — chiffre corrigé plutôt que laissé. Un rouge subsiste dans ce lot et il est SANS rapport avec cette livraison : `tests/unit/scripts/test_parity_harness.py::test_obs_scratch_buffer_is_zeroed_before_reuse`, que `git blame` impute à `c4dd34ace` — `def ObservationBuilder.__init__` (`engine/observation_builder.py`) y exige désormais `game_rules.plunging_fire_height` alors que ce test construit un builder sur `{"observation_params": {"obs_size": ...}}` seul. pyright vert sur les deux fichiers. Doc et config d'équerre dans la même livraison : indices `[s][17..25]` d'`observation_et_actions.md`, bloc « Vue d'ensemble » (total, `allies_bin` 21 → 26, `enemies_bin` 21 → 26), acquittement `_ACKNOWLEDGED_OBS_SIZE` et sa lignée, `obs_size` des six profils — dont la **justification était restée sur 16 735 pendant deux livraisons** et récupère les trois maillons manquants. ⚠️ **La taille de l'observation change : tout modèle antérieur est explicitement incompatible.**

Correctifs hors chantier (2026-09-09, suite 26) : `worktree-fix-fixtures-unit-keywords` — **la garde `UNIT_KEYWORDS` de la suite 25 cassait 8 tests, livrés rouges sous une annonce verte.** Le `require_key(unit, "UNIT_KEYWORDS")` posé dans `_encode_unit_entity` est correct et reste : les deux constructeurs du moteur (`create_unit`, `_build_enhanced_unit`) posent la clé sans repli, donc une unité de PRODUCTION la porte toujours — ce sont deux fixtures littérales qui décrivaient une unité impossible, déclarant `FACTION_KEYWORDS` et oubliant son jumeau. `tests/unit/engine/test_agent_decision_mechanism.py` (6) et `tests/unit/engine/test_model_value_per_figurine.py` (2) reçoivent `"UNIT_KEYWORDS": []`, la convention que porte déjà le fixture `_FULL_UNIT_CFG` de `tests/unit/engine/test_state_manager.py` (« une config d'entrée complète le déclare — comme `UNIT_KEYWORDS` », même vide). **La correction ne remonte PAS dans le socle** `unit_invariants()` (`tests/_state_invariants.py`) : celui-ci ne porte que les champs à valeur CONSTANTE, et exclut explicitement ceux qui dépendent du roster — dont `hideable`, dérivé de `UNIT_KEYWORDS`. **Deux des huit étaient MASQUÉS** derrière le rouge `game_rules` du chantier étages, qui lève dans `ObservationBuilder.__init__` avant d'atteindre l'encodeur : prouvés par sonde locale (ajout temporaire de `game_rules: {plunging_fire_height: 3}` → deux rouges `UNIT_KEYWORDS` apparus, sonde retirée), et c'est pourquoi le compte annoncé d'abord était 6. **L'angle mort de la recherche a été fermé, pas contourné** : le premier critère (fichiers citant `unitType` sans `UNIT_KEYWORDS`) ratait les fixtures bâties sur `unit_invariants()` et les tests atteignant l'observation par `.reset()`/`.step(` ; le critère élargi couvre **185 fichiers**, dont 4 restaient à vérifier — **162 verts, aucun rouge**. 223 tests verts au total sur les fichiers touchés et l'angle mort. ⚠️ **Trois rouges subsistent dans le dépôt et n'appartiennent pas à cette livraison** : `game_rules` manquant dans `test_model_value_per_figurine.py` (2) et `test_ez_metric_game_state_verrou.py` (1), imputés par `git blame` à `c4dd34ace` — `ObservationBuilder.__init__` y exige `game_rules.plunging_fire_height` alors que ces tests construisent un builder sur `{"observation_params": {…}}` seul.

Correctifs hors chantier (2026-09-09, suite 27) : `worktree-fix-obs-builder-plunging-height` — **les 5 rouges `game_rules` signalés par les suites 25 et 26 sont fermés ; le seuil de Plunging Fire est lu au POINT D'USAGE, comme son jumeau, et non plus au constructeur de l'observation.** `c4dd34ace` avait posé `require_key(require_key(config, "game_rules"), "plunging_fire_height")` dans `ObservationBuilder.__init__` pour une constante utilisée à UN seul endroit, `_encode_unit_entity` (échelle de `max_floor_height`) — un site qui reçoit `game_state`, donc la même config (`w40k_core.py` (`W40KEngine.__init__`), `game_state["config"] is self.config`). **Les fixtures n'ont PAS été complétées** : le contrat déclaré de `__init__` est qu'`obs_size` est le seul paramètre exigé (`test_observation_builder.py::test_valid_config_initializes`, qui tombait lui aussi), et le JUMEAU de cette lecture — la résolution du tir, `shared_utils.py` (`_manual_roll_intent`) — lit déjà `require_key(require_key(require_key(game_state, "config"), "game_rules"), "plunging_fire_height")` à son point d'usage, sous court-circuit. Exiger la clé à la construction faisait porter une config de jeu complète à tout builder unitaire et divergeait du jumeau ; la lecture est désormais dans la branche `if _alive_heights:` — un plateau sans étage n'a aucun seuil à exiger, exactement comme le court-circuit du tir. **Zéro fixture touchée : 5 rouges deviennent verts par le seul changement moteur** (`tests/unit/scripts/test_parity_harness.py::test_obs_scratch_buffer_is_zeroed_before_reuse`, `test_observation_builder.py::test_valid_config_initializes`, `test_ez_metric_game_state_verrou.py::test_verrou_game_state_observation_builder`, `test_model_value_per_figurine.py` (2)). **Mutation rouge constatée, `__pycache__` purgé** : `max_floor_height` forcé à 0.0 → `test_move_ascent_13_06.py::test_the_agent_observes_that_its_models_are_elevated` rouge, ce qui prouve que la branche réécrite est bien celle du chemin de production. `grep _plunging_fire_height` → 0 hit restant. **69 tests verts** sur les sept fichiers concernés (harnais de parité, contrat d'observation, verrou EZ, valeur par figurine, montée 13.06, Plunging Fire tir, contrat de déploiement) — 43 sur les six premiers seuls. `obs_size` inchangé : aucune conséquence sur les modèles.

Correctifs hors chantier (2026-09-09, suite 28) : `worktree-fix-suite-unitaire-rouge` — **les 18 rouges de `tests/unit/` sont fermés ; les 12 de la classe `game_rules` le sont par le MOTEUR (suite 27, livrée en parallèle), les 6 autres ici, et aucun défaut de moteur ne se cache derrière ces 6.** Les rouges annoncés comme « subsistants » par les suites 25 et 26 en font partie et ne subsistent plus. Quatre causes distinctes, aucune commune. (1) `tests/unit/engine/test_move_commit_floor_orientation.py` déclarait une unité sans `UNIT_KEYWORDS`, même défaut que la suite 26 sur un troisième fichier — la clé est exigée par `def _model_multilevel_reachable_field` (`engine/phase_handlers/movement_handlers.py`), qui la passe à `def validate_floor_placement` (`engine/terrain_utils.py`) ; `INFANTRY` et non la liste vide, sinon la fixture perd le droit 13.06 de finir son move à l'étage et le test ne mesure plus l'orientation qu'il vise. (2) Le couple de déclaration de montée 13.06 (`def ascent_declaration_reset_state`, `engine/phase_handlers/movement_handlers.py`) était posé au reset sans entrer dans le socle `def turn_state_invariants` (`tests/_state_invariants.py`) — importé et non recopié, comme son jumeau de vol. **L'ajout est inerte pour la cinquantaine de fixtures qui étalent le socle**, et c'est vérifié plutôt que supposé : les trois lecteurs du couple passent par `.get`/`setdefault`, donc « clé absente » et « set vide » sont indiscernables. (3) Magic de save **TL04 → TL05**, TL04 refusé explicitement avec son motif, pour ces deux clés plus `_charge_engage_memo`. **Aucune exception n'est faite au prétexte que les lecteurs d'aujourd'hui tolèrent l'absence en `.get`** : le contrat porte sur l'ENSEMBLE des clés du reset, et un lecteur qui passerait à `require_key` ne réécrirait pas ce format. L'entrée TL05 est **écrite comme une union** de l'entrée TL04 — un `frozenset`, donc impossible à élargir par ce partage, ce qui est l'interdit que rappelle le fichier — et des trois clés ajoutées : la ligne dit ce que le format ajoute, au lieu de noyer trois clés dans une seconde copie de cent-vingt. (4) Six références documentaires violaient la convention §5 (nom de fichier nu, ancre de ligne) : cinq dans ce fichier, une dans `Documentation/Roadmap/infra.md` — celle-ci **déjà périmée**, ses lignes citées étant la signature de `def _finish_charge_after_placement` (`engine/w40k_core.py`) et non la capture du segment `[MODELS:]` dont parlait la phrase. **LES DEUX DERNIERS ROUGES SONT DES ÉPISODES DE RÉFÉRENCE PÉRIMÉS, PAS DES RÉGRESSIONS, et la distinction a été MESURÉE.** `tests/unit/engine/test_charge_memo_steplog_footprint.py` ne voyait plus aucune charge à sa graine 42 — sonde sur huit graines à 150 pas : **trois en produisent** (0 → 3 charges committées, 11 → 2, 3 → 1), donc la charge fonctionne et c'est le tirage qui a bougé. `tests/unit/engine/test_reserves_metrics.py` ne voyait plus aucun tir à sa graine 2 — re-mesure sur ses trois graines : **0 → 22 tirs, 1 → 0, 2 → 0**, exactement l'inverse du classement du 2026-09-06. **La cause commune de ces deux dérives est nommée** : ces harnais jouent en actions légales tirées au sort, et l'ouverture de la verticalité au move ajoute un point de décision, donc tout le tirage se décale. La graine y est un **paramètre de mesure, pas une constante du verrou**, et les deux fichiers le disent maintenant, avec la mesure qui l'établit. Chiffres périmés d'`infra.md` (402 lignes, 302 segments `[MODELS:]`, mesurés à la graine 42) remis sur la mesure réelle de la graine 0 ; `Documentation/Chantiers/Replay.md` suit le bump de magic dans la même livraison. **Les six fixtures `game_rules` d'abord complétées ici ont été RETIRÉES** : la suite 27 ferme la même classe de rouges par le moteur, sous la décision explicite « zéro fixture touchée » — vérifié, les **93 tests des cinq fichiers concernés sont verts sans aucune modification de fixture**, y compris les 7 de `test_faction_abilities.py` que la suite 27 n'avait pas listés. **786 tests verts** sur les fichiers touchés, leurs voisins de save et un échantillon de 23 consommateurs du socle, mesurés sur l'état FUSIONNÉ (suites 27 et `worktree-fix-ascent-level-cache` comprises) ; `/code-review` : 0 finding. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 29) : `worktree-aligne-lignee-obs-size` — **quatre affirmations chiffrées fausses laissées par les trois chantiers d'observation des 2026-09-08 et 09, qu'aucun contrôle automatique ne voyait ; un contrôle les voit désormais.** Les six `observation_params.justification` (empreinte RIGOUREUSEMENT identique, md5 vérifié avant et après : 9104 → 9490 caractères) annonçaient `VALEUR COURANTE : 16971` et une grille `32x32x11` pendant que l'`obs_size` du MÊME bloc déclarait 17055 et que `GRID_CHANNELS` (`engine/spatial_grid.py`) valait 12 — la contradiction était **interne à un même paragraphe**, dont la dernière phrase disait déjà « 16971 -> 17055 » et « GRID_CHANNELS 11 -> 12 ». Elles renvoyaient en outre à `Documentation/Archives/docs/AI_TRAINING.md` comme « référence publiée », document **absorbé** (bandeau « ne plus le citer ») dont `VALUE_CHECKS` ne surveille rien : le renvoi nomme maintenant `Documentation/Reference/training/entrainement.md`, seul document où `obs_size` est confronté à la source. **La lignée a été MESURÉE et non recopiée** — worktree détaché, `ObservationBuilder.SQUAD_OBS_SIZE_TARGET` calculé à chaque commit : 16791 → **16811** (`d617a5d2`, OC live + secured, +20) → **16971** (`7c0c004c`, mots-clés de catégorie, +160 = 5 bits × 32 entités) → **17055** (`32789d87`, verticalité, +84) ; la branche `c4dd34ac` seule donnait 16895, c'est le merge qui produit 17055. Le corps de la justification **sautait le maillon des mots-clés** et enchaînait le 2026-09-08 sur la verticalité : l'événement +160 y est ajouté, sans quoi la lignée corrigée n'aurait été justifiée nulle part. Côté documents, l'historique d'`observation_et_actions.md` s'arrêtait à 16791 alors que la même page annonçait déjà la grille `(12, 32, 32)` (récupère les trois maillons avec leurs champs), et l'exemple de `training_config` d'`entrainement.md` portait `"obs_size": 16791` — **hors de portée du motif de `claim_obs_size`, qui exige des backticks et ne voit donc pas un bloc fencé**, ce qui explique trois livraisons vertes sur une valeur fausse. **Nouveau contrôle `check_config_justification`** (`scripts/check_doc_references.py`) : il confronte la valeur courante ET le dernier maillon de la lignée à l'`obs_size` du bloc qui les porte, et la grille annoncée à `engine/spatial_grid.py` — aucune passe ne regardait jusqu'ici **à l'intérieur d'un bloc de config**, `VALUE_CHECKS` ne couvrant que des `.md`. Il rend **21 confirmées** (7 profils résolus × 3 — `x1_lineage` hérite de la justification par `extends`, le JSON brut n'en porte que 6), et un motif qui ne retrouve plus sa phrase est une **ASSERTION ORPHELINE**, jamais un succès. **Deux mutations rouges constatées, `__pycache__` purgé** : défauts réintroduits dans la config → 14 contradictions sur 21 ; comparaison neutralisée dans le code → 3 tests rouges. **6 tests ajoutés** (`tests/unit/scripts/test_check_doc_references.py`), dont le verrou bout en bout sur la config réelle et la garde anti-vert-vacant sur énumération vide ; **177 verts** sur ce fichier, `tests/unit/scripts/test_agent_config_guard.py` et `tests/unit/ai/test_metrics_doc.py` — `justification` est dans ses `DOC_KEYS`, donc une modification purement textuelle y est verte par construction. `PYTHONPATH=/home/greg/40k python3 scripts/check_doc_references.py` **exit 0** sur l'état fusionné. pyright vert sur les deux `.py`. `obs_size` n'a PAS changé (17055 avant comme après) : **aucune conséquence sur les modèles**, aucun `--new` imposé.

Correctifs hors chantier (2026-09-09, suite 30) : `worktree-fix-entree-format-figee` — **réécrire l'entrée d'un format de save ANCIEN ne faisait rougir aucun contrôle.** Trouvé par `/code-review` sur la suite 27, et **la moitié du défaut a été fermée entre-temps par `02077af3`** (`_TL05_KEYS` rendue indépendante de `_TL04_KEYS`), qui supprime la propagation par union : une clé déposée dans l'entrée figée ne remonte plus dans l'entrée courante. **L'autre moitié restait ouverte** : `test_reset_keys_match_the_current_save_format` ne lit QUE l'entrée courante, et depuis que TL05 est littérale, `_TL04_KEYS` n'est plus lue par aucun contrôle — la réécrire est silencieuse, alors qu'elle décrit un format déjà sur le disque des joueurs. Parade : `_FROZEN_FINGERPRINTS` épingle le COMPTE et l'EMPREINTE (sha256 des clés triées, 16 hexa, recalculable par `_fingerprint` trois lignes plus haut) de chaque entrée qui n'est plus la magic courante — `W40KTL04` : 128 clés / `4f1bc5601c046cb5`. Second contrôle `test_every_frozen_entry_is_pinned` : au bump suivant, TL05 devient un fait historique à son tour et doit être épinglée dans le même geste, sans quoi le trou se rouvre sous un autre nom. **Mutation rouge constatée, `__pycache__` purgé** : `_charge_engage_memo` ajouté à `_TL04_KEYS` → **un seul rouge, le nouveau** (129 clés / `05fad960f3836a49`), les quatorze autres verts — c'est la mesure qui prouve que le littéral seul ne voyait rien. 25 tests verts sur le fichier et ses voisins de save (déballage restreint, clés statiques, sauvegardes PvP), pyright vert. Docstring du fichier et `Documentation/Chantiers/Replay.md` d'équerre : l'interdit « n'élargis pas l'entrée existante » n'est plus une consigne, c'est un contrôle. Le commentaire d'en-tête du tableau, resté orphelin après `02077af3` (il annonçait « les trois clés que le reset publie en plus » juste avant l'entrée qui ne les contient pas), est recadré dans le même geste.

Correctifs hors chantier (2026-09-09, suite 31) : `worktree-obs-size-source-unique` — **`obs_size` n'est plus déclaré nulle part : la valeur calculée est la seule source, et la suite 29 — livrée le même jour — en est la démonstration.** Elle avait construit `check_config_justification` pour confronter la prose d'`observation_params.justification` à l'`obs_size` du bloc qui la porte, après que la contradiction eut franchi trois livraisons. Le défaut était d'un cran en amont : la config recopiait à la main une valeur que le moteur confrontait ensuite à `ObservationBuilder.SQUAD_OBS_SIZE_TARGET` — **celle-là même qui la détermine**. Une boucle fermée n'établit rien ; sa seule issue possible était de retarder sur sa source, et elle l'a fait. Les six blocs `observation_params` sont supprimés, avec les quatre sites de plomberie qui les portaient (`engine/w40k_core.py` : extraction du profil, injection dans `self.config`, branche « reconstruire un `training_config` depuis `observation_params` », et le bloc d'espace d'observation ; `engine/observation_builder.py` : le `raise` du constructeur), plus les **deux** sites PvP/PvE d'`api_server.py` — les rater aurait fait tomber le serveur au démarrage pendant que l'entraînement, lui, aurait continué. **La branche `Box(obs_size)` du pipeline mono-figurine était MORTE** : le `raise` qui la précédait rendait toujours vraie la condition qui la gardait. **AUCUN RETRAIN** : le contenu de l'observation ne bouge pas, `SQUAD_OBS_SIZE_TARGET` vaut 17055 avant comme après, les `.zip` existants restent valables — c'est ce qui distingue cette livraison de tous les autres chantiers d'observation. **Ce que le contrôle supprimé apportait est repris par deux mécanismes distincts, et aucun ne remplace l'autre** : SB3 protège la MACHINE (`load` → `check_for_correct_spaces`) en comparant le **Dict entier**, donc il voit aussi une DISPOSITION changée à taille égale, ce qu'un total scalaire ne savait pas faire ; le garde-fou de `tests/unit/engine/test_deployment_observation_contract.py` protège l'HUMAIN contre un retrain non prévu, et devient **le seul point d'acquittement** — son message ne renvoie plus à aucune config à éditer. **Critère de clôture MESURÉ** : champ factice ajouté à `UNIT_BIN_FIELDS`, `__pycache__` purgé → **un seul échec, le garde-fou** (17055 → 17087, avec l'ancienne valeur, la nouvelle et le `--new`), les autres tests verts **sans qu'aucune config n'ait à être éditée** ; le grep de la clé sur `config/` ne rend plus rien. **La lignée a désormais UN domicile** — la chaîne « Historique de `obs_size` » d'`observation_et_actions.md` — au lieu de trois dont aucun n'était sous contrôle : la `justification` de la config disparaît avec son bloc, et la docstring du garde-fou passe de 115 à 38 lignes en renvoyant au document. `claim_obs_size_history` confronte son **dernier maillon** à la source calculée, ce qu'aucune passe ne faisait — `claim_obs_size` ne regardait que la valeur en gras d'`entrainement.md`. **Mutation rouge constatée** : dernier maillon figé à 16971 → « VALEUR PÉRIMÉE — la source dit 17055 », exactement la panne où l'historique s'était arrêté à 16791. `expected_obs_size()` lisait les profils d'agent, donc une valeur recopiée : elle dérive maintenant de `SQUAD_OBS_SIZE_TARGET`. **`check_config_justification` et ses 6 tests sont retirés** : leur sujet n'existe plus, et les garder ferait lever le script sur un profil valide. **Suivant le précédent d'`action_space_size`** (même défaut, même espace de noms, déjà fermé ainsi), la clé est **laissée dans les fixtures de test** : une config volontairement périmée prouve que le code ne la lit plus, une fixture nettoyée ne prouve rien — c'est le matériau des deux tests de non-lecture ajoutés. Documents remis d'équerre : `entrainement.md` (bandeau, ligne de tableau, exemple de config, erreur commune), `observation_et_actions.md`, `capacites.md` (dont le renvoi pointait `config/agents/ArmageddonAgent/`, **chemin inexistant**), `tour_de_jeu.md`, `armes.md`, `configuration.md`, et `ai/curriculum.py` dont la liste de clés structurelles nommait encore `obs_size`. **`Bot_refactor.md` §E garde son `obs_size` de `policy.yml`** : c'est le relevé daté d'un artefact figé, dont tout l'objet est de **diverger** de la valeur du jour — la précision de sa source est ajoutée. Le script de contrôle documentaire rend **exit 0** sur l'état fusionné. **432 tests verts** sur les fichiers touchés et les candidats du chemin supprimé ; contrôle de types vert sur les dix fichiers modifiés ; `/code-review` : 2 findings, **tous deux traités** (borne de fin de l'extracteur, jumeaux documentaires du fragment `observation_params`). La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 32) : `worktree-resolveur-doc-index-git` — **le contrôle documentaire déclarait introuvable un fichier qui existe, et c'est le DOCUMENT qui se faisait corriger.** `SEARCH_DIRS` (`scripts/check_doc_references.py`) énumère à la main les répertoires où chercher un nom de fichier NU. Mesuré sur le dépôt : **44 dossiers porteurs de `.py`/`.ts` en étaient absents, soit 435 fichiers** — tout `frontend/src/` (roster, utils, components, hooks), `tests/unit/scripts`, `tests/unit/services`, `tests/integration/pvp`. La veille, la suite 27 avait cité `test_parity_harness.py` dans ce même index : le contrôle a rendu FICHIER INTROUVABLE et le renvoi a été réécrit en chemin complet pour le satisfaire, alors que le fichier était là et ses 5 tests verts. **Un contrôle qu'on apaise en éditant le document qu'il contrôle ne contrôle plus rien.** Correction : `resolve` gagne une passe d'INDEX, `tracked_paths_by_basename`, dérivée du listing suivi — la même doctrine que `tracked_suffixes`, dont la liste écrite à la main ignorait `.rs`, `.sql`, `.html` et `.css`. **Placée en DERNIER RECOURS, après le voisin, après `SEARCH_DIRS` et après les trois arbres parcourus** : elle ne peut que transformer un « introuvable » en trouvé, jamais déplacer une résolution existante. `SEARCH_DIRS` survit mais change de rôle, et son commentaire le dit : **table de PRIORITÉ** qui départage les **72 basenames** que plusieurs fichiers portent, plus la liste des lieux où un fichier a le droit de vivre. **L'identité reste protégée comme avant** : `is_ambiguous` est inchangé, et les quatre sites qui appellent `resolve` le consultent déjà avant de confronter un symbole ou de propager l'atteignabilité — un homonyme reste compté INVÉRIFIABLE, jamais cassé. `tracked_basenames` dérive désormais de l'index au lieu de recompter le listing, pour que le découpage des chemins — le piège que sa propre docstring documente — n'ait qu'une implémentation. **Non-régression PROUVÉE par comparaison, pas déduite** : la version d'avant et celle d'après ont tourné sur le MÊME arbre, sorties identiques au caractère près (100 lignes, `exit 0` des deux côtés) — aucun verdict du corpus ne bouge. **Preuve positive par test** : un fichier par dossier non déclaré, soit les 44, doivent se résoudre ; rouge constaté avant le correctif (`episode.py`, dossier `check`). Deux verrous l'encadrent — la priorité des dossiers déclarés sur l'index pour les homonymes, et le refus de résoudre un nom que le dépôt ne porte pas, sans quoi la passe deviendrait un tampon — chacun avec sa garde d'énumération non vide. **177 tests verts** sur `test_check_doc_references.py`, 8 sur les consommateurs du chargeur de script ; contrôle de types propre sur le script (l'import `pytest` non résolu apparaît aussi sur des fichiers non touchés, il est environnemental). L'instrument de comparaison a été supprimé. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 33) : `worktree-verrou-mots-cles-categorie` — **les tests des mots-clés de catégorie parcouraient une table RECOPIÉE dans le fichier de test, donc ils prouvaient que chaque bit DÉCLARÉ s'allume, jamais que chaque mot-clé CONSOMMÉ par le moteur possède un bit.** Un mot-clé ajouté côté moteur n'aurait fait échouer aucun test et l'agent aurait subi une catégorie qu'il ne perçoit pas — le motif exact déjà fermé côté règles d'unité par `test_every_registered_obs_id_is_in_the_vocabulary`. Deux tests **dérivent** désormais les mots-clés des sources du moteur : `_HIDEABLE_KEYWORDS` et `_FLOOR_CAPABLE_KEYWORDS` (`engine/game_state.py`, 13.06 et 13.08/13.09), `_MONSTER_OR_VEHICLE_KEYWORDS` (`shared_utils`, 10.06) et `ANTI_RULE_IDS` (`attack_sequence`, 24.03), plus les littéraux passés à `_unit_has_keyword` (6 sites, tous « fly »), lus **par AST et non par regex** — un `grep` ne distingue pas un argument d'une mention en commentaire. La source 10.06 était un **littéral dans le corps** de `_model_is_monster_or_vehicle`, donc invisible à toute dérivation : elle est extraite en constante, comportement identique vérifié sur les tests 10.06. **La portée est DÉCLARÉE, jamais supposée totale** : trois comparaisons écrites sur place lui échappent (`shared_utils` ~5805 et ~11137, `fight_handlers` ~4706), toutes sur `monster`/`vehicle`, deux mots-clés qui ont leur bit ; les deux commentaires et le docstring les nomment, avec ce qu'il faut faire pour y entrer. **`BEASTS`/`SWARM` restent sans bit et l'exclusion est DATÉE au lieu d'être affirmée** : mesuré sur les **42** datasheets résolues depuis `config/agents/*/rosters/**` ET `config/agents/_p2_rosters/**`, zéro porteur et **zéro écart entre `hideable` et `INFANTRY`** — un bit dédié serait colonne pour colonne la copie de `kw_infantry`, pour 32 scalaires et un retrain `--new`. Le second test tombe le jour où un roster joué en porte un ; ce roster étant alors nouveau, le bit se paie dans un retrain déjà nécessaire. **`/code-review` : 5 findings, 4 traités, 1 écarté avec raison.** Le balayage manquait `_p2_rosters` (pas de segment `rosters` dans son chemin, que compose `def _max_count_for_ref` d'`ai/train.py`) alors que les bits sont écrits pour les **20 slots ennemis** autant que pour les alliés ; et 112 des 404 entrées de `models` sont des objets de placement dont `str()` rendait la repr, silencieusement ignorée — les deux corrigés, **33 → 42 datasheets résolues**. La dérivation `[ANTI-X]` filtrait sur `startswith` là où `def _anti_crit_wound_threshold` d'`attack_sequence.py` découpe inconditionnellement : le préfixe est **asserté**. Finding écarté : les rosters `CoreAgent` dits « retirés », ce qui n'est pas prouvé — les inclure est le choix conservateur, mais le message d'erreur cesse d'imposer une seule issue et nomme aussi le retrait de l'unité. **Rectif du 2026-09-10 (suite 54)** : ces rosters ont depuis été supprimés du dépôt, le corpus passe donc de **42 à 23** datasheets résolues — la garde `>= 20` du test tient, et « zéro porteur de BEASTS/SWARM » reste vrai sur le corpus réduit. **Quatre mutations rouges constatées, `__pycache__` purgé** : « titanic » dans `_FLOOR_CAPABLE_KEYWORDS`, `FenrisianWolf` (BEASTS) dans un roster d'agent, `_unit_has_keyword(unit, "aeldari")` dans `charge_handlers`, et `RipperSwarm` placé **uniquement** dans un `models` dict de `_p2_rosters` — cette dernière atteinte par les deux corrections à la fois. Restaurations vérifiées par diff vide. **103 tests verts** sur le fichier, les consommateurs 10.06, la conformité BEASTS 13.06 et les voisins d'observation. **`obs_size` inchangé (17055) : aucun retrain imposé**, ce qui est tout l'objet de la décision. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 34) : `worktree-ingress-candidats-observation` — **une escouade qui arrive de réserves choisissait son point d'entrée entre sept boîtes noires, et ces sept choix étaient scorés par la convolution des cellules de mouvement.** Deux couches du même défaut, celui que §0.40 point 3 et §0.44 avaient fermé pour le seul déploiement. (1) `def _encode_deployment_candidates` (`engine/observation_builder.py`) sortait dès que la phase n'était pas `deployment`, alors que le masque ouvre les MÊMES ids 4-10 à une escouade en réserves pendant la phase de MOUVEMENT — l'ingress move (20.04) est une mise en place (03.02), pas un mouvement. Reproduit par exécution avant correction sur un épisode réel : masque ouvert sur `[4,5,6,7,8,9,10]`, `ingress_slot_candidates` rendant les sept mêmes slots, et `deploy_cand_bin[:, present].sum() == 0.0`. (2) `def _deploy_logits` (`ai/pointer_policy.py`) ne routait vers la tête pointeur que si le bit `phase_deployment` valait 1 : au même instant il valait `0.0`, donc les logits des ids 4-11 sortaient de la conv 1×1, aux cellules (0, 4..11) de la fenêtre égocentrique. **Le routage lit désormais la PRÉSENCE de candidats** (`deploy_cand_bin[..., -1]`, le masque même de l'encodeur) et non plus la phase : la question posée est celle que le masque tranche, sans nommer la phase qui l'ouvre. L'index de drapeau publié par l'extracteur, devenu sans consommateur, est supprimé avec ses deux tests de contrat. **L'observation RELIT ce que le masque a posé, elle ne recopie pas son prédicat** : `INGRESS_OPEN_SLOTS_KEY` est effacé à l'entrée de la construction du masque et écrit au seul endroit qui le sait, la branche d'ingress — sans quoi un choix d'activation dont la tête de pool est en réserves ferait décrire des candidats qu'aucune action n'ouvre. **La variante de scoring allégée est supprimée, et le chiffre qui la justifiait était périmé** : elle sautait les deux colonnes d'exposition au motif de 2,98 s sur le pool ; mesuré après la vectorisation batch de §0.64, **0,120 s pour les 17 609 cases d'un pool de bord et 0,471 s pour les 59 050 cases d'un pool Deep Strike**, à 9 sources — 6 fois moins sur un pool plus grand, et mémoïsé par (escouade, tour, état des unités posées). L'ingress est donc le jumeau exact du déploiement que sa docstring annonçait : mêmes colonnes, même tri, mêmes clés — ce dernier point étant ce que l'observation lit par `require_key`. **Une conclusion intermédiaire a été RÉTRACTÉE sur mesure** : la collision des slots 6 et 10 avait été attribuée à l'absence d'exposition, or le DÉPLOIEMENT la présente aussi — `(216,296)` pour les deux, `los=(0,0)` sur les sept candidats — parce que les deux branches de `def _deployment_slot_order` ne diffèrent que par `(-los, -potential_los)`, constantes tant qu'aucun ennemi n'est posé. Le défaut est réel, préexistant et **hors périmètre** (le corriger change l'ordre des slots du déploiement) : il part en SUITE avec ses ancres. **L'ancre de la grille égocentrique n'est pas touchée, et c'est une mesure qui l'a tranché** : la fenêtre fait 110×110 subhex (180×180 en Deep Strike) sur un plateau 220×300 et les candidats tombent aux bords ; couverture mesurée **1/7 et 2/7 pour la zone de déploiement contre 0/7 et 1/7 pour le barycentre du pool** — aucune ancre ne couvre le pool, l'existante est la moins mauvaise et la seule stable. Rouge prouvé par mutation aux trois endroits, `__pycache__` purgé : exposition remplacée par zéro → rouge sur `mc2` ; routage reconditionné à la phase → rouge sur le seul test d'ingress, 36 autres verts ; garde de phase remise → rouge sur les deux terrains. **258 tests verts** sur les cinq suites touchées et les suites de scoring de déploiement, plus les jumelles ingress/réserves. **`obs_size` inchangé : aucun champ ajouté ni retiré**, donc aucun `--new` imposé par la forme — mais le bloc de candidats devient non nul en phase de mouvement et le routage change, donc ce que le modèle courant a appris sur les ids 4-10 en mouvement est caduc. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 35) : `worktree-safe-rear-strategie-distincte` — **deux des sept stratégies de mise en place posaient systématiquement le même hexe : l'agent avait sept boutons dont deux indiscernables, sans que rien ne lève.** C'est le défaut que la suite 34 avait mesuré et laissé hors de son périmètre. Cause : dans `def _deployment_slot_order` (`engine/action_decoder.py`), « sûr » (+2) et « arrière-garde » (+6) portaient le MÊME tuple de tri à l'exception de `(-los, -potential_los)` en tête du premier — or ces deux colonnes valent zéro partout tant qu'aucun ennemi n'est posé, comme `nearest_ally` tant qu'aucun allié ne l'est (`def _deployment_score_columns` la met à zéro sur toute la zone dans ce cas). Les deux tris coïncidaient donc exactement dans le cas le plus courant. **Mesure avant/après, `board/44x60x5`, les deux scénarios d'entraînement** : avant, `armageddon1` → `{(215,295): [6,10]}` et `armageddon2` → `{(109,215): [5,9], (11,295): [6,10]}` ; après, `armageddon1` → aucune et `armageddon2` → `{(109,215): [5,9]}` seule. **Deux pistes ont été écartées SUR MESURE avant celle retenue, et non par préférence** : le recul seul (`-progress`) ne sépare rien, l'hexe le plus reculé d'une zone étant aussi le plus éloigné des ennemis — les deux tris désignaient encore `(216,296)` ; et un critère de cohésion en tête ne sépare pas davantage, `nearest_ally` étant précisément l'une des colonnes constantes. L'objectif est le seul critère du registre non colinéaire à l'éloignement. L'arrière-garde trie désormais par objectif PUIS par recul — ce que son commentaire promettait déjà en mots (« loin des ennemis, tout en gardant un objectif proche ») sans que l'ordre des clés le rende — le recul en second empêchant qu'elle double « pression sur objectif » (+1), qui trie le même objectif par progression AVANT ; un test le verrouille. **La paire 5/9 subsistante n'est PAS le même défaut, et la distinction est mesurée et non supposée** : leurs ordres complets sur les 15 721 hexes de la zone DIFFÈRENT, seul le premier choix coïncide, l'hexe le plus proche d'un objectif étant aussi le plus central sur ce terrain — une convergence géométrique légitime de deux intentions distinctes. Elle n'est pas traitée, et le test dit pourquoi. **Un verrou écrit puis RETIRÉ parce qu'il était faux** : « les sept stratégies posent sept hexes distincts » exige une propriété que la conception ne garantit pas et tombait sur cette convergence ; l'invariant retenu est l'égalité des ORDRES COMPLETS, vérifiée en isolation sur des valeurs aléatoires et sur la zone réelle des deux scénarios. L'en-tête de `tests/unit/engine/test_deployment_slot_order_strategies.py`, qui annonçait « progress absent de +6 » et « +6 primary = nearest_enemy max », suit dans la même livraison. Rouge prouvé par mutation, `__pycache__` purgé : ancien tuple réintroduit → **7 rouges**, dont les deux scénarios réels ; restauré → vert. **74 tests verts** sur les deux suites touchées, les caches de déploiement et le déploiement automatique ; réserves stratégiques et contrat d'observation verts également. **`obs_size` inchangé**, mais l'ordre des slots de déploiement change : ce que le modèle courant a appris sur les ids 4-10 est caduc — coût déjà engagé par la suite 34, donc payé une seule fois tant qu'aucun ré-entraînement n'a été relancé. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 36) : `worktree-observateur-split-fire` — **pendant un tir fractionné, l'observation rendue à l'agent décrivait une AUTRE escouade que celle qui tire, dans les deux sous-états.** Mesuré sur le chemin de production (`step_with_mask`) : l'observation était identique scalaire pour scalaire à celle de l'escouade d'identifiant le plus bas, et différente de celle de la tireuse. Cause : le masque du split-fire rend un pool VIDE (`action_decoder`, branche 1c), et `_build_observation_and_mask` n'avait de retour anticipé d'observateur que pour trois mécanismes sur cinq — il tombait donc sur le repli `_first_squad_on_board`. Le défaut est un JUMEAU DIVERGENT et non un oubli isolé : la table `_PLAYER_CHOICE_MECHANISMS` d'`ai/env_wrappers.py` listait bien les deux sous-états du split-fire depuis leur livraison, mais le moteur ne peut pas importer `ai`, donc il gardait sa propre liste écrite à la main. La table est déplacée dans `engine/action_decoder.py` (`PLAYER_CHOICE_MECHANISMS`, ordre et lecteurs inchangés), enrichie de l'escouade OBSERVATRICE de chaque point d'arrêt, et lue par les TROIS consommateurs — le prédicat de pause et le tirage du bot (`env_wrappers`) plus la sélection d'observateur (`w40k_core`). Deux points d'arrêt restent sans observateur, volontairement et documenté sur le champ : l'Oath (ne porte sur aucune escouade amie, repli délibéré) et la décision agent (règle propre, cas d'armée `waaagh_call`). Coexistence de deux arrêts sur des escouades différentes : `RuntimeError` explicite, ce que les trois branches d'origine se contentaient d'affirmer en commentaire. ⚠️ **`obs_size` inchangé, aucun ré-entraînement forcé** — mais un modèle existant a appris sur l'observation erronée : la distribution qu'il voit pendant un split-fire change, à mesurer par sonde holdout, pas à supposer. Impact hors entraînement : le PvE décide sur la même construction (``def make_ai_decision` (engine/pve_controller.py)`). Verrous : deux tests moteur (un par sous-état) avec la tireuse d'identifiant NON minimal — sinon le repli tombe juste par hasard et le test est vert sans rien verrouiller — et copie profonde des observations, le tampon étant partagé. Les deux ont été repassés au ROUGE par neutralisation des deux entrées split-fire du registre. `/code-review` : aucun finding ; commentaire de `_maybe_drain_forced_wait` (« six états » → huit) corrigé dans la même livraison. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 37) : `worktree-pointeur-emplacements-armes` — **les deux familles d'actions de choix d'arme étaient scorées par des couches denses indexées sur une position dont le contenu change en cours de partie.** `collect_weapon_profiles` (`engine/observation_weapon_profiles.py`) trie les profils par nombre de PORTEURS VIVANTS décroissant : mesuré par exécution sur une escouade de 3 porteurs de bolter et 2 de lascannon, l'emplacement 0 porte le bolter à effectif plein et le lascannon après deux pertes de bolter — les DEUX emplacements changent d'arme sans qu'aucune action soit jouée. `fight_weapon_net` et `shoot_weapon_sel_net` apprenaient donc un RANG, et d'autant plus mal que les pertes s'accumulent en fin de partie. Elles sont remplacées par `fight_weapon_query_net` et `shoot_weapon_sel_query_net`, deux requêtes qui pointent les embeddings de profil de l'unité ACTIVE, exposés par emplacement en queue du vecteur de features (`active_weapon_embeddings_slice`, ajoutée EN DERNIER — un ajout en tête aurait décalé les six tranches existantes en silence). **Aucun paramètre n'est ajouté à l'extracteur** : les embeddings existaient déjà, produits par le `weapon_encoder` partagé pour toutes les entités, et `_aggregate_subentities` les JETAIT après agrégation ; seule leur voie de lecture change. Deux points sans lesquels le branchement serait faux : les emplacements VIDES sont annulés par le masque de profil (sans quoi ils sortiraient le BIAIS de l'encodeur, indistinguable d'une arme réelle), et `_point` lit désormais `d` sur l'embedding qu'on lui donne, ces lignes étant en `weapon_dim` (32) et non `entity_dim` (64) — valeur inchangée au bit près pour les six familles d'entités. **L'annulation porte sur la SEULE ligne exposée, et c'est une mesure qui l'a tranché** : l'appliquer au rang complet, comme la première écriture le faisait, couvrait les quatre rangs de sous-entités — armes et types, les deux camps — soit **384 Mo d'activations retenues en plus à `batch_size 4080`** (formes réelles : 12+20 entités × 20 profils × 32, et 12+20 × 6 types × 16) contre **10 Mo** pour la ligne réellement lue, sur une machine dont le pic mesuré est déjà de 8,89 Go pour 8,19 Go de VRAM. Le masque n'est pas re-dérivé au site d'annulation : `_encode_units` rend celui qui a servi à l'encodage. Neutralité sur l'agrégat vérifiée par exécution et non par lecture (`_masked_mean_max` masque déjà mean et max, ensemble vide compris). `obs_size` ne bouge pas, `TOTAL_ACTION_SIZE` ni `DENSE_LOGIT_COUNT` non plus (les deux comptes étaient déjà soustraits d'`action_net`). Verrous : le logit d'un emplacement SUIT le profil qui l'occupe — à latent GELÉ, échanger deux profils échange leurs logits, ce qu'une tête dense ne peut pas produire — plus l'assemblage aux bons ids, la nullité des emplacements vides, la ligne lue (l'unité ACTIVE et non une autre alliée) et le miroir action ↔ observation des deux familles, ordre `[tir | mêlée]` vérifié sur l'ÉMETTEUR. Cinq mutations rejouées ROUGE, `__pycache__` purgé : embedding de rang au lieu du contenu (2 rouges), masque de profil retiré à ses deux emplacements successifs (2 chaque fois), ligne alliée 1 au lieu de 0 (2), bloc de mêlée pris sur le bloc de tir (2). **85 tests verts** (2 skips : checkpoint épinglé absent) sur les fichiers touchés et les consommateurs de la policy, plus **154** sur les contrats d'armes, de split-fire et d'espace d'action. `/code-review` passé sans finding ; c'est lui qui a relevé le coût du masquage uniforme, corrigé avant commit. pyright inchangé (la seule erreur du fichier, `obs["deploy_cand_bin"]` sous `PyTorchObs`, préexiste et n'est pas touchée). ⚠️ **Changement d'architecture de la policy : tout modèle antérieur est incompatible** — sans effet aujourd'hui (aucune archive n'est chargeable sur le schéma courant), mais le bénéfice n'est pas mesurable avant le prochain `--new` : les tests prouvent l'alignement et la propagation, pas le gain d'apprentissage.

Correctifs hors chantier (2026-09-09, suite 38) : `worktree-arme-conditionnee-cible` — **le choix d'arme se faisait sans voir contre qui.** Suite directe de la 37, qui avait rendu les emplacements adressables sans changer ce que la requête regarde : les deux requêtes ne dépendaient que du latent du tronc, où les 20 ennemis n'arrivent qu'agrégés (`enemies_agg`, moyenne + max). Or choisir une arme, c'est confronter S/PA/D à E/Sv/PV (04.01, 05.02, 05.04) — une compatibilité par PAIRE (arme, ennemi), non reconstructible depuis cette agrégation. En mêlée le manque est nu : `squad_fight` a DÉJÀ joué le `FIGHT_SLOT` avant d'armer `pending_fight_weapon_select`, la cible est donc désignée et marquée (`fight_target_selected`, posé par `_encode_unit_entity` dans `engine/observation_builder.py` sur les seules entités ENNEMIES), et la tête l'ignorait. `_weapon_target_bonus` (`ai/pointer_policy.py`) ajoute au logit de chaque emplacement la compatibilité `(w_j · P e_i) / sqrt(d_w)` — même forme et même diviseur qu'un logit de pointeur, puisque c'en est algébriquement le second terme : `(W_q l + P e) · w_j`. **Une seule mécanique, deux réductions, et c'est l'état qui les sépare** : en MÊLÉE la somme pondérée par `fight_target_selected` extrait la ligne désignée (zéro ligne marquée hors du point d'arrêt, donc terme nul, donc aucune branche ni garde de phase) ; au TIR le split-fire choisit l'arme AVANT la cible (`pending_weapon` vaut `None` quand le masque ouvre ces slots), la question devient « cette arme a-t-elle une bonne cible disponible » et la réduction est un `max` sur les ennemis PRÉSENTS. **Le masque du `max` n'est pas décoratif, et la première rédaction de sa justification était fausse** — relevée par `/code-review` : un slot absent a bien un embedding nul (`_encode_masked`), mais la projection qui le lit porte un BIAIS, donc sa compatibilité vaut `(w_j · b) / sqrt(d_w)`, arbitraire et non nulle (mesuré : norme L1 de `P(0)` à 1,90 à l'initialisation). Sans masque, des slots vides disputeraient le `max` aux vraies cibles. Vérifié sur un cas construit à la main, projection identité, où l'écart entre masqué et non masqué est `-sqrt(d)` contre `+sqrt(d)`, plus un contrôle qui MESURE le biais de la projection réelle au lieu de le supposer. **Écarté et pourquoi** : conditionner la requête de tir sur un résumé agrégé des ennemis aurait été REDONDANT — le tronc porte déjà `enemies_agg` — donc des paramètres pour une information déjà présente. Coût : 2 x (`entity_dim` x `weapon_dim`), soit 4 160 paramètres, et une matrice arme x ennemi de 3,3 Mo à `batch_size 4080`. `obs_size`, `TOTAL_ACTION_SIZE` et `DENSE_LOGIT_COUNT` inchangés — l'information était déjà dans l'observation, seule sa voie de lecture change. Verrous : les deux réductions sur cas construit (masque du `max`, extraction de la ligne marquée, zéro quand rien n'est désigné), le déplacement des logits de mêlée quand la cible DÉSIGNÉE change à latent gelé — plus sa moitié inverse, terme exactement nul hors du point d'arrêt —, la localité des quatre familles indexées par slot ennemi reformulée (les blocs d'arme lisent désormais les ennemis, c'est un couplage voulu et non indexé par slot), les emplacements d'arme VIDES qui ne bougent pas, et le gradient non nul des deux projections. Trois mutations rejouées ROUGE, `__pycache__` purgé : `max` non masqué (2 rouges), terme de cible retiré de l'assemblage mêlée (4), projection de mêlée utilisée au tir (3). **87 tests verts** (2 skips) sur les fichiers touchés et les consommateurs de la policy. **pyright passe de 1 erreur à 0** sur `ai/pointer_policy.py` : la lecture de clés d'observation est désormais précédée d'un contrôle explicite du type de l'espace, qui narrow aussi la lecture préexistante de `deploy_cand_bin`. ⚠️ **Changement d'architecture de la policy, à livrer avant le prochain `--new`** ; comme pour la suite 37, les tests prouvent l'alignement et la propagation, pas le gain d'apprentissage.

Correctifs hors chantier (2026-09-09, suite 39) : `worktree-retrait-intentions-de-zone` — **la famille d'actions « intentions de zone » est retirée : 15 actions qui n'avaient plus aucun effet sur la partie, et coûtaient 1,6 à 5,2 steps par épisode.** Leur unique conséquence était la prime `zone_intent_shaping`, débranchée le 2026-08-11 sur mesure (part des déclarations payées 0.269 contre 0.355 pour la même politique tirant au hasard). Mesure du coût du statu quo avant retrait, sur TensorBoard : `o_intent_zone_steps` à 1,63 par épisode sur le run P1 (intent effondré, 98,6 % INVADE, information mutuelle 0,000 bit, alignement 0,478 pour une référence hasard de 0,477) et 5,21 sur `x1_long` du 2026-09-09 (pour 123 steps d'épisode). **Vérifié sur les PDF et non sur les commentaires du code** : `16 Actions.pdf` décrit les Actions réelles (STARTS/UNITS/USE LIMIT/COMPLETES/EFFECT, venues des mission packs) — les intentions n'en sont pas ; `08 Command phase.pdf` énumère cinq étapes que `command_phase_start` implémente toutes ; ni `14 Objectives.pdf` ni `26 Primary_missions.pdf` ne demandent de déclaration. **Les 15 ids restent RÉSERVÉS** (`BASE_ZONE_INTENT`, 1296-1310) parce que `15 Stratagems.pdf` place une vraie décision joueur dans cette phase — INSANE BRAVERY 15.04, « just before you make a battle-shock roll » — et qu'aucun stratagème n'est implémenté : les CP s'accumulent sans puits. `TOTAL_ACTION_SIZE` (1389) et `obs_size` (18269) sont donc INCHANGÉS, aucun retrain n'est imposé par la forme. **La phase de commandement ne rend plus la main du tout** : `command_phase_resume` ne distingue plus l'agent du bot et enchaîne sur le mouvement, ce qui supprime aussi le `wait` de sortie. **Deux effets de bord traités dans la même livraison.** (1) **Bug préexistant révélé par le `raise` qui remplace le fourre-tout** `return "zone_intent"` de `def action_family` : `COHERENCY_SLOTS` (P3-0) et `SHOOT_WEAPON_SEL_SLOTS` (P3-8) n'avaient AUCUNE branche et étaient comptées comme des intentions dans `action_family_counts` depuis leur livraison — deux branches et deux familles ajoutées, et l'énumération de `test_action_family_usage` complétée (elle ne pouvait pas le voir : elle vérifiait que chaque famille DÉCLARÉE est atteinte, or aucune des deux ne l'était). (2) **Format de save bumpé TL05 → TL06**, imposé par le contrat de `test_save_format_key_contract` : cinq clés quittent le reset (`zone_intents`, `zone_intent_free_steps_remaining`, `unit_zone_assignments`, `_zone_intent_declarations`, `_pending_zone_shaping`). **C'est le premier bump causé par un RETRAIT** ; TL05 est épinglée dans `_FROZEN_FINGERPRINTS` (131 clés / `bc1d5f0c7f07dc36`) et rejetée avec son motif. Conséquence mesurée : une seule save existe sur le disque (`logs/pvp_saves/__working__.pkl`, le tampon de travail PvP), elle devient illisible. Neuf courbes retirées (`o_intent_zone_steps`, `p_intent_control_dependency`, sept `combat/intent_*`) ; les lettres des survivantes ne sont PAS redécalées, pour garder les runs archivés comparables. Rouge prouvé par mutation aux deux endroits qui portent le comportement (reprise de phase, masque de commandement), `__pycache__` purgé entre les deux ; **351 tests verts** sur les 15 fichiers touchés, dont 8 verrous neufs (`tests/unit/engine/test_command_phase_has_no_agent_action.py`). Trois fichiers de test supprimés avec leur sujet. ⚠️ **Le bump TL05 → TL06 a été ANNULÉ le même jour, sur décision de l'utilisateur (option C).** La doctrine du bump protège d'un état AMPUTÉ — une save écrite avant l'ajout d'une clé que le moteur lira ensuite. Sur un RETRAIT elle ne protège de rien : une row TL05 restaurée rend cinq clés que plus personne ne lit, inoffensives. Les cinq clés restent donc PUBLIÉES par le reset, inertes et documentées comme telles, `_MAGIC` revient à `W40KTL05` et l'entrée TL06 du contrat est retirée — même arbitrage que pour les 15 ids d'action : on condamne, on ne supprime pas ce qui invaliderait ce qui est déjà écrit ailleurs. Vérifié sur le disque : la seule save existante (`logs/pvp_saves/__working__.pkl`, 2026-09-09 17:46) porte l'en-tête `W40KTL05` et redevient lisible ; aucune save TL06 n'a été écrite entre le bump et son annulation. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 40) : `worktree-reactive-move-engagement-et-refus` — **le mouvement réactif ignorait la condition d'engagement de sa propre datasheet, offrant un désengagement gratuit.** Texte officiel (« Skulking Horrors », fourni par l'utilisateur — l'ability n'est dans AUCUN PDF de `Documentation/40k_rules/`, recherche des 5 termes sur les 35 fichiers, 0 hit, instrument validé par témoins positifs et couverture d'extraction mesurée) : « Once per turn, when an enemy unit ends a Normal, Advance or Fall Back move within 9" of this unit, **if this unit is not within Engagement Range of one or more enemy units**, it can make a Normal move of up to D6". » Les six filtres d'éligibilité de `maybe_resolve_reactive_move` (`engine/phase_handlers/shared_utils.py`) portaient la vie, le camp, la présence sur table, le « déjà réagi », la possession de la règle et les 9 pouces — **jamais l'engagement**. Le pool BFS n'écarte que les cases d'ARRIVÉE adjacentes à un ennemi, jamais la position de DÉPART : un porteur au contact réagissait et sortait du corps à corps **sans les contraintes du Fall Back**. Le défaut a été MESURÉ avant correctif, pas déduit : réactif engagé en (10,10) à distance 1 d'un ennemi, déplacé en (12,9) à distance 2, `applied=1`. Les DEUX sites d'appel de production (`movement_handlers.py`, `shooting_handlers.py`) appellent la fenêtre sans garde amont — vérifié, pas supposé. Fix : `unit_within_engagement_zone_footprints`, primitive canonique EZ déjà utilisée par les jumeaux fight/charge, placée après le filtre de rayon. **Livré avec le retrait de `reactive_move` au `FenrisianWolf`** : l'utilisateur confirme que l'unité ne porte pas cette règle, et le nom d'ability « Predator Instinct » n'existe nulle part ailleurs dans le dépôt — le Termagant en est le SEUL porteur. Miroir `unit_matrix.json` corrigé à la ligne concernée et non régénéré : le générateur plante sur le répertoire `ork` qu'il ne connaît pas, **défaut préexistant et indépendant** (le miroir date du 2026-03-27 et ne couvre aucune unité Ork). Verrous : un test d'éligibilité repassé au ROUGE par retrait de la porte (`assert True is False`), `__pycache__` purgé, **plus un contrôle jumeau hors zone d'engagement** — sans lui, le `triggered=False` pourrait venir de la scène et resterait vert si la porte devenait inerte. `/code-review` : 2 findings, **tous deux traités dans la livraison** — la description de `config/unit_rules.json` promettait un déclenchement inconditionnel (remplacée par le texte VERBATIM de la datasheet, qui devient la seule copie fidèle dans le dépôt) et la spec d'éligibilité de `Documentation/Reference/jeu/regles_unites.md` ignorait la porte, ainsi que le refus pour incohérence de formation livré le 2026-08-21. **350 tests verts** sur les fichiers touchés et leurs dépendants (réactif, obs des règles d'unité, câblage `RULES_STATUS`, consommateurs de `unit_rules.json`, intégration PvP `test_command.py`), pyright sans erreur nouvelle, biome propre. ⚠️ **Le volet REFUS n'est PAS livré** : `reactive_decision_mode` vaut toujours `"auto"` en dur, et la branche `"state"` comme `decline_reactive_move` restent sans producteur — arbitrage de périmètre rendu à l'utilisateur, le coût réel étant une modale PvP à créer (aucune UI réactive n'existe côté PvP, seulement du replay). Sans effet sur l'entraînement : le Termagant n'est dans aucun roster d'`ArmageddonAgent_x1`. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-09, suite 41) : `worktree-static-keys-live-wins` — **le retrait d'une clé devenue STATIQUE laissait la save écraser la valeur vivante, et le seul rempart était un geste manuel que rien ne garantissait.** `rebuild_game_state` (`services/game_snapshots.py`) ré-attachait les clés de `_GS_STATIC_KEYS` depuis le moteur vivant **puis** faisait `update(captured["game_state"])` : une row écrite quand la clé était encore mutable la réinjectait avec sa valeur — celle d'une AUTRE partie — par-dessus la valeur vivante, sans que rien ne lève. Le lecteur ne voyait pas une clé manquante, il lisait une mauvaise valeur. **Le défaut n'a jamais été déclenché** (aucune clé n'a encore migré) et il était documenté en prose dans `services/game_saves.py` depuis la revue du 2026-09-09, avec pour parade le bump de magic. **Ce bump ne couvrait pas le cas, et c'est mesuré et non supposé** : le seul contrôle qui voit une migration, `test_save_format_key_contract`, ne regarde que les clés posées par le RESET, alors que `_deploy_pool_set_cache` et `_los_blocking_grids_cache` naissent paresseusement — une clé peut devenir statique sans jamais avoir été dans sa portée. **La règle passe donc de la prose au code** : les clés statiques sont filtrées de la partie capturée, une clé statique vient TOUJOURS du moteur vivant. Une clé statique que le live n'a pas encore (cache paresseux) reste absente et se recalcule, seul comportement juste pour un cache ; l'exception assumée reste la clause de roster (`uses_codex_detachment`, `army_faction`), capturée hors `game_state` précisément parce que `config` est statique. **Le jumeau en mémoire portait la même faute et il est SUPPRIMÉ plutôt que corrigé deux fois** : `GameSnapshotStore.build_game_state` recopiait le corps de la reconstruction, il délègue maintenant à `rebuild_game_state` — deux exemplaires d'une même règle, c'est ainsi qu'elle a divergé. **Rouge prouvé par mutation, `__pycache__` purgé** : ancien `update` réintroduit → **3 rouges sur 3 nouveaux tests**, dont celui du jumeau mémoire ; restauré → 6 verts. Les trois verrous se CONSTRUISENT (rétro-datage d'une row par réinsertion d'une clé statique) puisque aucune save réelle ne porte le cas, et chacun garde son anti-vert-vacant : la clé doit être dans `_GS_STATIC_KEYS` et absente de la capture courante. **Trois documents remis d'équerre dans la même livraison**, la livraison rendant fausse la consigne qu'ils portaient : le bloc `_MAGIC` de `game_saves.py`, la docstring et le message d'assertion de `test_save_format_key_contract.py`, et `Documentation/Chantiers/Replay.md`. **Le bump reste obligatoire** pour une clé qui quitte le reset tout en restant créée paresseusement ET lue. **40 tests verts** sur les fichiers touchés et leurs voisins de save (clés statiques, contrat de format, détachement codex, sauvegardes PvP en intégration), pyright vert sur les deux `.py`. `/code-review` : aucun finding ; une inexactitude de commentaire signalée hors finding est corrigée dans la livraison. Aucun effet sur l'entraînement ni sur `obs_size`. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-10, suite 42) : `worktree-fix-pytest-back-16-fails` — **16 tests rouges de `tests/unit/`, sept causes, dont deux régressions livrées la veille et jamais vues parce que leur chantier n'avait relancé que ses propres fichiers.** (1) `test_deployment_clearance_parity.py` (4 rouges) : la livraison 20.01 « déclaration des réserves avant le déploiement » arrête le moteur sur une décision agent AVANT la première pose, donc le masque du step 0 ne porte que des `CHOICE_*` ; le test exigeait un slot de pose et tombait sur `aucune action de déploiement dans le masque (step 0)`. Il solde désormais l'étape par `settle_reserves_declarations`, le helper que ce même chantier avait livré pour les autres tests. Sa plage de slots passe de `range(4, 9)` — littéral qui laissait `centre_hub` et `safe_rear` hors de la vérification de parité — à `DEPLOY_SLOT_BASE` + `DEPLOY_STRATEGY_COUNT`. (2) `test_charge_declaration_distances.py` : le gate d'éligibilité 11.02.1 (« within 12" of one or more enemy units ») n'était appliqué qu'en métrique euclidienne ; le chemin **hex/gym**, celui de l'entraînement, n'avait pour éligibilité que le BFS de 12" finissant en zone d'engagement, qui accepte un ennemi **au-delà** de 12" (mesuré : 12,13" et 12,89"). L'escouade déclarait donc une charge sans ennemi déclarable et la ligne de journal portait `charge_nearest_enemy_inches: None`. Le gate devient commun aux deux métriques via `charge_target_within_max_distance`, source unique de cette borne et primitive dont `charge_target_edge_distance_subhex` est la version chiffrée — l'invariant « ligne de charge ⟹ distance de déclaration non nulle » devient structurel. ⚠️ **Le comportement de charge change en x1** : des charges au-delà de 12" que le BFS autorisait sont maintenant refusées à la déclaration. (3) `ai/training_contract.py` : `agent_reward_table` ne vérifiait que la PRÉSENCE de la clé d'agent, jamais son contenu — `build_contract` écrivait sans un mot un contrat que sa propre relecture déclare abîmé, et l'erreur ne sortait qu'au run SUIVANT en accusant le fichier de contrat au lieu de la table de récompense. Le refus est posé à l'écriture, pendant de celui de `_exige_sections`, dont la docstring argumentait cette garantie sans qu'aucun code ne la tienne. (4) `test_attached_units_keywords_19_03.py` et `test_attached_units_legality_19_01.py` (3 rouges) : leurs scénarios ne déclaraient qu'un seul joueur, donnée invalide — l'observation exige `value_at_start` non nul pour les deux camps et REFUSE explicitement une armée de valeur nulle. L'adversaire obligatoire est ajouté, aucune assertion n'est touchée. (5) `metriques.md` : la note de retrait des intentions de zone citait `` `combat/intent_*` `` en backticks, forme que `_doc_tags()` compte comme un tag vivant ; les sept courbes sont désormais nommées sans préfixe, comme toutes les autres courbes retirées du document. (6) et (7) `ROADMAP_INDEX.md` : la ligne 137 de la livraison 20.01 attribuait quatre symboles de `deployment_handlers.py` à un JSON de scénario, et la section hygiène franchissait le seuil de 20 lignes ✅ — 26 correctifs antérieurs au 2026-09-08 archivés dans `archives/doc.md#hygiene-correctifs-ponctuels`, compteur 21 → 19, aucune perte vérifiée par script. **Preuve rouge/vert par mutation pour chaque correction d'invariant**, `__pycache__` purgé. **781 tests verts** sur les fichiers touchés et leurs jumeaux (charge, déploiement, réserves 20.01, contrat d'entraînement, contrôle documentaire). `/code-review` : 1 finding, **fermé le 2026-09-10** (`worktree-contrat-base-actions`) — le refus d'écriture du contrat ne testait que la sous-table VIDE alors que son message annonce `base_actions` ; une sous-table non vide amputée de cette section écrivait donc un contrat, et le run mourait en cours d'épisode dans `RewardCalculator._get_unit_reward_config` (`engine/reward_calculator.py`). `agent_reward_table` exige désormais `base_actions`, le cas fréquent étant le RETRAIT d'une clé et non la table vide. Le même trou un cran plus bas est fermé dans la foulée : `base_actions` présente mais VIDE laissait passer l'écriture (l'empreinte porte un chemin, donc `_exige_sections` ne voyait rien) et le run mourait au premier tir dans `RewardMapper.get_shooting_priority_reward` (`ai/reward_mapper.py`). Rouge prouvé avant chaque fix, 99 tests verts (contrat + prologue de run). `/code-review` de la livraison : aucun finding. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-10, suite 43) : `worktree-retour-d-win-rate` — **`00_critical/d_win_rate` est rendue au tableau de bord critique, deux jours après son retrait de la suite 18.** La courbe est écrite depuis `def log_episode_end` (`ai/metrics_tracker.py`), sous la même garde `winner is not None` que sa jumelle `game_critical/win_rate` — et non depuis `log_critical_dashboard`, où elle vivait avant le retrait : ce dashboard tourne à CHAQUE fin d'épisode, donc un épisode sans vainqueur y poserait un point que la jumelle n'a pas, et les deux séries divergeraient en abscisse pour tout le reste du run. `e_episode_reward_smooth` a suivi le même jour, cf. suite 44. Verrou : `test_le_win_rate_dentrainement_a_sa_jumelle_00_critical` (`tests/unit/ai/test_metrics_single_writer.py`), qui compare les deux séries point par point sur un montage portant un épisode sans vainqueur — c'est lui qui sépare les deux sites d'écriture ; rouge prouvé par mutation, `__pycache__` purgé. Doc d'équerre : ligne de tableau et bloc « Organisation » de `metriques.md`, note des lettres libres (qui annonçait encore `l` libre alors que `l_approx_kl_max` existe depuis la suite 18), `entrainement.md` et `Documentation/Roadmap/training.md`. 80 tests verts sur les cinq fichiers de métriques. **Mesure jointe, faite pour ce prompt** : sur le run de lignée en cours (`x1_lineage`, repris à l'épisode 50 001, à 53 001 au moment de la mesure), les ~110 tags `bot_eval/*`, `00_critical/{0_gap_*,a,b,o_robust_current_score}`, `forcing/*` et `perf/{d,e}_bot_eval_*` sont absents parce qu'AUCUNE évaluation de bots n'a encore eu lieu — `bot_eval_freq` vaut 10 000, héritée de `x1_long`, donc la première tombe à 60 000. `02_combat/a_value_trade_ratio` y porte 2 502 points. Rien d'autre que `d_win_rate` n'avait disparu du code.

Correctifs hors chantier (2026-09-10, suite 44) : `worktree-retour-e-episode-reward-smooth` — **`00_critical/e_episode_reward_smooth` est rendue au tableau de bord, et l'inventaire complet de ce qui a réellement disparu des courbes est établi par mesure.** La courbe est écrite depuis `def log_episode_end` (`ai/metrics_tracker.py`), juste après l'append de `all_episode_rewards` : contrairement à `d_win_rate`, elle n'a **aucune jumelle** — `game_critical/episode_reward` porte la valeur BRUTE d'un épisode, jamais son lissage —, donc le retrait de la suite 18 avait supprimé la MESURE et pas seulement un nom. Elle n'est pas branchée sous la garde `winner is not None` de sa voisine : une reward est acquise quelle que soit l'issue, et le verrou `test_la_reward_lissee_du_dashboard_critique_couvre_tous_les_episodes` (`tests/unit/ai/test_metrics_single_writer.py`) le prouve par un épisode sans vainqueur, dont le point doit exister ; ses valeurs séparent aussi la moyenne de fenêtre de la moyenne cumulative. Rouge prouvé par mutation, `__pycache__` purgé ; 81 verts sur les cinq fichiers de métriques. **INVENTAIRE MESURÉ, run P1-OLD (419 tags) contre le run du 2026-09-09/10 (405 tags)** — 34 tags absents, trois causes et aucune perte en cours de run : **16 RENOMMAGES** du décalage de lettres de la suite 18 (`m_immediate_reward_ratio` → `n_`, `p_reward_deploy_*` → `q_`, `q_obj_held_diff_deploy_*` → `r_`, `r_win_rate_deploy_*` → `s_`, `s_deploy_active_share` → `t_`, `t_truncated_episodes` → `u_`), où la mesure existe sous un autre nom et où c'est le NOM CONNU DE L'UTILISATEUR qui a disparu ; **14 RETRAITS réels**, dont les 4 de `d`/`e` refermés par les suites 43 et 44 et les 10 tags d'intentions de zone (`n_intent_zone_steps`, `o_intent_control_dependency`, `actions/share_zone_intent`, 7 × `combat/intent_*`) partis le 2026-09-09 avec la famille d'actions elle-même — irrécupérables, la mesure n'a plus d'objet ; **4 propres à l'étape** (`03_selfplay/P0`, `pool_eval/vs_p0` et leurs doublons), absents d'un départ à froid qui n'a pas de pool, et de retour dès l'étape de lignée. **Aucune courbe ne meurt en cours de run** : sur les 405 tags, toutes les courbes par-épisode portent leurs points de 500 à 50 000 sans trou ; les deux seules exceptions sont `bot_eval/training_combined` (sonde d'apprentissage 1 éval sur N, `ai/training_callbacks.py`, 1 point ici comme sur P1-OLD) et le blob `custom_scalars__config__`. Les ~110 tags `bot_eval/*` absents du run de lignée en cours ne sont pas perdus non plus : `bot_eval_freq` vaut 10 000, la reprise est partie de l'épisode 50 001, la première éval tombe à 60 000.
Correctifs hors chantier (2026-09-10, suite 45) : `worktree-fix-renvois-roadmap-index` — **les deux rouges de `tests/unit/scripts/test_check_doc_references.py` venaient de trois renvois écrits dans ROADMAP_INDEX.md lui-même, tous posés par les suites 42 à 44.** (1) La ligne de la suite 43 nommait le fichier sujet de l'entraînement par un chemin préfixé du seul dossier parent, forme qu'aucune des deux conventions du dépôt ne résout — ni relative au document, ni relative à la racine. Le chemin complet depuis la racine est écrit, et non le nom nu, parce que `archives/` porte un fichier de même basename. (2) et (3) La ligne de la suite 42 refermait son propre `/code-review` en citant deux numéros de LIGNE, ce que §5 interdit : le premier doublait `RewardCalculator._get_unit_reward_config` (`engine/reward_calculator.py`), déjà nommé dans la même phrase, et disparaît ; le second est remplacé par le symbole où le run mourait, `RewardMapper.get_shooting_priority_reward` (`ai/reward_mapper.py`). **Aucun code touché : la documentation était fausse, pas le contrôle** — les deux tests rouges sont le même défaut vu deux fois, une fois par la liste d'entrée et une fois par le code de sortie du script. `scripts/check_doc_references.py` en sortie 0, 179 tests verts sur son fichier de contrôle. La vérification large reste à l'utilisateur.

Correctifs hors chantier (2026-09-10, suite 46) : `worktree-fix-pyright-10-erreurs` — **les 10 erreurs de `pyright.log`, trois fichiers, trois causes, aucune dans le moteur — et une seule était un vrai défaut de code.** (1) `scripts/split_assigned_policy_probe.py` lisait `dist.distribution.probs` sur ce que `get_distribution` DÉCLARE rendre, une `MaskableDistribution` : cet attribut n'existe que sur la variante catégorielle, la variante multi-catégorielle portant à la place une LISTE de facteurs sous `distributions`. La sonde compare des probabilités indexées par id d'action ; sur une liste de facteurs, son TVD mesurerait autre chose que le choix d'action, et rien dans la sonde n'énonçait ce contrat — tenu par construction, `PointerMaskablePolicy` refusant déjà tout espace d'action non `Discrete`. La lecture devient une fonction nommée, `_action_probs`, qui refuse par `TypeError` toute autre distribution ; l'extraction la rend testable sans construire de modèle ni doubler la classe. **Rouge prouvé par mutation** (garde-fou neutralisé → `AttributeError` sur la multi-catégorielle), `__pycache__` purgé, et **l'atteinte du chemin réel est mesurée et non déduite** : `_Policy.distribution` exécutée sur un vrai moteur d'audit rend un vecteur de 1 389 probabilités sommant à 1,0 — la sonde lancée sur 2 graines n'avait, elle, mesuré AUCUN point d'arrêt informatif et n'aurait rien prouvé. (2) `tests/integration/pvp/test_deploy.py` : deux tests de la déclaration 20.01 indexaient la question rendue par `_pending_declaration` sans vérifier qu'elle est publiée, alors que cette fonction rend `None` quand l'étape est close ; l'étape close aurait fait tomber le test sur une erreur d'indexation au lieu de son assertion. Le contrôle qui manquait est celui que le premier test de la même classe posait déjà. (3) `tests/unit/engine/test_command_phase_has_no_agent_action.py` passait des ids d'unité en chaîne à l'helper `_unit` de `tests/unit/engine/test_action_decoder.py`, qui les déclare `int` et dont l'autre appelant passe des entiers ; `_build_gs` normalise ensuite par `str(...)`, donc aucun comportement ne change. **41 tests verts** sur les fichiers touchés (sonde, phase de commandement, déploiement PvP), pyright en 0 erreur sur les quatre. Pas de `/code-review` : aucun fichier de `engine/`, `ai/`, `services/` ni `frontend/src/` n'a bougé. Les autres journaux fournis étaient déjà propres — biome sur 353 fichiers, `tsc` vide, 132 tests front verts ; `pytest_back.log` était tronqué à 34 %, la suite large restant à l'utilisateur.

Correctifs hors chantier (2026-09-10, suite 47) : `worktree-commentaire-combi-blast` — **la mesure d'homogénéité des groupes combi portait sur `[CLOSE-QUARTERS]` seul et se lisait comme valant pour toutes les règles.** Le commentaire de `_pick_one_profile_per_weapon_group` (`engine/phase_handlers/shared_utils.py`) affirmait « 14 groupes combi, 0 heterogene, 0 en melee » sans borner sa portée, alors que `_shoot_engagement_blocks_target` décide de l'éligibilité contre une cible engagée en lisant `[CLOSE-QUARTERS]` **et** `[BLAST]`. Re-mesure du 2026-09-10 sur les 6 armureries (`frontend/src/roster/*/armory.ts`, 231 armes, parsées par `ArmoryParser._parse_armory_file`) : la mesure d'origine est EXACTE pour `[CLOSE-QUARTERS]` (14 groupes à au moins 2 profils, 0 hétérogène, 0 en mêlée), mais **2 groupes sur 14 sont hétérogènes en `[BLAST]`** — `ballistus_missile_launcher` et `cyclone_missile_launcher`, dont le profil Frag porte la règle là où le Krak ne la porte pas. Les deux profils d'un combi ne sont donc pas toujours également éligibles : contre une cible engagée, le Frag est refusé là où le Krak passe. Sans effet sur le groupage, qui ne voit que des candidats déjà filtrés arme par arme par ce même gate. **Commentaire seul, aucun comportement touché** — contrôlé par `git diff -U0` : 0 ligne non-commentaire. 62 tests verts (déclaration de tir, sélection d'arme par défaut, tir sur cible engagée 04.02).

Correctifs hors chantier (2026-09-10, suite 48) : `worktree-obs-combi-exclusivite` — **l'observation présentait les deux profils d'une même arme physique comme deux armes indépendantes, et l'agent sur-comptait son propre volume de feu.** Mesuré sur le chemin de production (`build_armageddon_engine`, comparaison entre la somme des profils que l'obs présente et le volume exclusif réel calculé par `_weapon_group_key`) : **6 couples escouade × registre sur 20** sur-comptent, de +10,5 % à +26,1 % en volume, +5,3 % à +28,6 % en dés d'attaque — **uniquement au tir, jamais en mêlée**, ce que confirme le comptage des groupes combi du registre d'unités (12 au tir, 0 en mêlée). **Aucune action fausse n'en découlait** : le masque et la résolution appliquent déjà l'exclusivité (`_pick_one_profile_per_weapon_group`), le défaut était dans l'entrée de la politique et de la valeur. **L'information n'était pas dérivable** : `profile_identity` (`engine/observation_weapon_profiles.py`) n'inclut pas `COMBI_WEAPON`, et ni le bloc TYPES ni le bloc « mes figurines » (`engine/observation_entities.py`) ne relient un profil à une figurine — un Intercessor portant bolt_rifle + bolt_pistol, deux armes physiques distinctes qui tirent toutes les deux (04.01 verbatim, « You can select one or more ranged weapons that model has »), se présentait dans l'obs exactement comme un combi à deux profils. ⚠️ **Le texte de la règle « Multiple Weapon Profiles » est ABSENT du corpus** : recherche sur les 35 PDF de `Documentation/40k_rules/`, 1 seul hit, la ligne de renvoi du SEE ALSO de `04 Making attacks.pdf`, sans texte développé. L'exclusivité reste donc portée par le moteur et n'a PAS été modifiée — la livraison l'expose, elle ne la définit pas. Arbitrage rendu à l'utilisateur et tranché : marqueur de groupe encodé comme `obs_id` RÉSERVÉ dans les slots d'ids déjà existants (`WEAPON_RULE_ID_SLOTS`), registre en Python et plage disjointe des ids de règles — **`obs_size` reste 18269**, zéro paramètre ajouté (la table d'embedding est pré-dimensionnée à `OBS_ID_VOCAB_SIZE` et ne s'ajuste pas au vocabulaire), **aucun `config/*.json` touché**. Les deux options écartées : un scalaire continu dédié coûtait +640 dimensions, un `--new` obligatoire, et encodait un nominal dans un canal continu ; un simple bit « je suis exclusif » est insuffisant, l'armée de démo portant 3 groupes combi distincts. Plage dimensionnée sur la borne STRUCTURELLE (un groupe marqué occupe au moins 2 des `K_WEAPONS` slots) et non sur le maximum observé, qui vaut 2 sur les 18 rosters d'agent et d'adversaire. Marge de slots vérifiée : 4 règles au maximum sur les 231 armes, marqueur compris 5 sur 6, débordement toujours levé par `_fill_id_slots`. 141 tests verts, 5 mutations rouges puis vertes. **Aucun gain de win-rate n'est revendiqué** : il ne se prouvera que par une mesure holdout.

Correctifs hors chantier (2026-09-10, suite 49) : `worktree-oc-par-figurine` — **le contrôle d'objectif multipliait un OC d'ESCOUADE par un nombre de figurines au lieu de sommer l'OC de CHAQUE figurine.** Règle lue avant correction, `Documentation/40k_rules/14 Objectives.pdf` §14.02 verbatim : « add together the OC characteristics of all the models in that player's army that are within range of that objective » ; et `Documentation/40k_rules/02 Datasheets.pdf` §02.02 : « Objective Control (OC): How effectively a model can control an objective » — l'OC est une caractéristique de FIGURINE, portée par `models_cache`. Le produit n'est juste que sur une escouade homogène ; dès qu'un personnage est attaché (19.01), les deux profils d'OC coexistent et il est faux **dans les deux sens**. Mesuré : 5 Boyz OC 2 + BannerNob OC 6 rendait 12 contre 16 attendus ; 5 Intercessor OC 2 + Ancient OC 1 sous « Relic Banner: This unit has +1 OC » rendait 18 contre 17. `unit_effective_oc` devient `unit_oc_bonus` — seul le BONUS reste porté par l'unité, l'OC de base se lit sur la figurine — et `iter_living_model_footprints` se dédouble en `iter_living_models_with_footprints`, qui rend l'identifiant avec l'empreinte. La règle 01.07 reste au niveau de l'unité, comme le veut son encadré (« the OC characteristic of all of its models is '-' »). ⚠️ **CHANGEMENT DE COMPORTEMENT DANS TOUTES LES PARTIES** : sur les deux rosters de la démo, les QUATRE escouades de bloc sont hétérogènes en OC — Boyz 24 → 22 et 24 → 27, Intercessor 12 → 11 et 21 → 19 ; le contrôle d'objectif, les points de mission primaires et le reward d'objectif changent donc, et **un ré-entraînement `--new` avec re-mesure du win-rate holdout est requis**. Le jumeau `_enemy_threat_order` (`engine/phase_handlers/shared_utils.py`) sommait déjà par figurine : c'est le contrôle qui divergeait, pas lui ; il reste volontairement différent (tri de menace sur la datasheet, sans bonus ni battle-shock). 93 tests verts sur les fichiers touchés, 5 rouges par mutation, domaine de l'OC dans `models_cache` vérifié à l'exécution (toutes les figurines en entier, aucune clé manquante — le `require_key` lève au lieu de retomber sur un défaut).

Correctifs hors chantier (2026-09-10, suite 50) : `worktree-expected-damage-cible-effective` — **le reward et les bots estimaient les dégâts contre une cible non protégée : l'asymétrie était dans une seule fonction.** `expected_damage` (`engine/utils/expected_damage.py`) appliquait le Waaagh! de l'ATTAQUANT (+1 STR, +1 NB) tout en lisant les caractéristiques défensives BRUTES de la cible, alors que le moteur résout la blessure contre la T de 19.02 (« use the highest T characteristic of the bodyguard models in that unit ») et la sauvegarde contre l'invulnérable effective. Mesuré sur le vrai chemin (5 Boyz + BannerNob attaché, joueur 2, Waaagh! appelé, bolt rifle) : 0,37037 avec ET sans Waaagh!, contre 0,29630 avec les caractéristiques effectives — **surestimation de 25,0 %, présente dès le tour 1**, le BannerNob conférant la 5+ invulnérable en permanence. Chemin de production : `ai/reward_mapper.py` appelle `expected_damage`, et `engine/reward_calculator.py` appelle le mapper à chaque décision de tir, de charge et de combat. Le correctif introduit `effective_defensive_profile`, source unique du (T, Sv, InSv) effectif, et y branche aussi les deux heuristiques de choix d'arme qui notaient contre la T de la figurine échantillon. **Le vrai travail était le cache** : `_wdc_def_key` était posé UNE FOIS par `stamp_weapon_keys` au reset, donc la table pré-calculée que lisent tous les bots ne pouvait structurellement pas voir un effet qui s'active en cours de partie — clé mesurée à (5, 5, 7) alors que l'InSv effective valait 5. La clé défensive est supprimée du tampon et calculée à la LECTURE : changement de FORME du cache, pas correctif de ligne. Coût mesuré : `squad_expected_damage` passe d'environ 5 µs à 19,1 µs par appel (13,0 µs d'oracle), et `build_best_weapon_cache` retombe à 130 µs par reset, sa dimension cible ayant disparu. ⚠️ **Le reward change** : ré-entraînement requis, au même titre que la suite 49. 568 tests verts, 3 mutations rouges puis vertes, pyright en 0 erreur sur les quatre fichiers moteur et services touchés.

Correctifs hors chantier (2026-09-10, suite 51) : `worktree-table-degats-armes-absentes` — **57 armes sur 231 n'existaient pas dans la table de dégâts, et un trou de DONNÉES y valait zéro dégât en silence : les bots tenaient toute escouade ork pour incapable de tirer.** Ligne fautive, `engine/weapon_damage_cache.py` : la boucle de `build_best_weapon_cache` faisait `continue` sur une sous-table absente, si bien que `lookup_best_weapon` rendait `(-1, 0.0)` — violation T1. Mesuré avant correction, clé `weapon_off_key` testée contre `load_weapon_damage_table` : **ork 18 absentes sur 21, spaceMarine 24 sur 117, tyranid 10 sur 36, aeldari 3 sur 28, adeptusCustodes 1 sur 10, chaos 1 sur 19** — et sur le roster de démo, TOUTES les armes de tir de TOUTES les unités ork sauf Gretchin. **QUATRE causes distinctes, chacune rattachée aux armes qu'elle explique** : (1) `FACTIONS` écrite en dur dans `scripts/weapon_damage_builder.py` n'énumérait que 5 des 6 armureries — les 18 armes ork ; (2) la clé offensive ignorait `[TORRENT]` 24.37, dont les armes portent `ATK: 7` : la formule de touche rendait 0, le builder ne gardant que les dégâts non nuls, le profil n'était jamais écrit — 12 armes, dont les lance-flammes Space Marines ; (3) la table datait du 2026-03-31 quand les armureries dataient du 2026-08-28 — 27 armes ajoutées depuis ; (4) **racine de la cause 3 : le constructeur était INEXÉCUTABLE**, son chargement de module par chemin de fichier étant mort le jour où `engine/combat_utils.py` a importé `engine.game_utils`, ce qui re-entrait dans `engine/__init__` — plus personne ne pouvait régénérer la table, et rien ne le disait. Après correction : **0 arme absente sur les 231**, six armureries, vérifié indépendamment ; `squad_expected_damage` de 5 Boyz sur 5 Intercessors passe de **0,0 à 0,5556** au tir, la mêlée et le sens inverse restant identiques. **La table RÉTRÉCIT de 4,8 %** (1 415 999 → 1 348 336 octets) : les plages défensives sont désormais dimensionnées sur la mesure des 179 datasheets (T 2→11 bonus Waaagh! compris, ARMOR_SAVE 2→7, INVUL_SAVE effective 4→7) au lieu d'un maximum théorique qui payait 156 profils jamais portés. La levée sur profil absent est posée à la LECTURE et non à la construction, parce que 59 fichiers de test patchent `load_weapon_damage_table` en dictionnaire vide. 249 tests verts, 5 mutations rouges puis vertes — dont une **passée verte au premier essai** et corrigée : le test des plages appliquait `toughness_bonus_while_waaagh` au seul porteur au lieu de le conférer à l'unité attachée (19.04), donc il ne verrouillait rien. ⚠️ **Le modèle de dégâts des bots change dans toutes les parties** : ré-entraînement requis, au même titre que les suites 49 et 50.

Correctifs hors chantier (2026-09-10, suite 52) : `worktree-marqueur-combi-tronque` — **un groupe combi coupé par la troncature des profils laissait un marqueur sans partenaire dans l'observation.** Suite directe de la suite 48. `collect_weapon_profiles` ordonne les profils par porteurs DÉCROISSANTS et ne garde donc pas ensemble les deux profils d'une même arme physique : `encode_squad_weapon_profiles` numérotait les marqueurs sur la liste COMPLÈTE, puis jetait les profils au-delà de `k_slots`. Reproduit à l'exécution avant correction — 12 profils de tir distincts, groupe `plasma` porté par 3 figurines au slot 0 et par 1 au rang 11 : le slot 0 sortait avec `COMBI_GROUP_0` et son unique partenaire était hors des 10 slots, l'observation affirmant « je suis exclusif » sans dire avec qui — exactement le symbole muet que l'invariant « un groupe à profil unique n'est pas marqué » refuse ailleurs. **Portée mesurée : aucun roster actuel n'atteint le cas** — 4 profils de tir au maximum par datasheet (179 scannées via `ai/unit_registry`), 6 au maximum par escouade sur `build_armageddon_engine(seed=0)`, contre `K_WEAPONS_RANGED = 10` slots ; c'est donc un verrou de spécification sur le chemin défensif de la troncature, au même titre que le test de troncature déjà présent. Arbitrage rendu à l'utilisateur et tranché (option A) : les marqueurs sont numérotés sur les profils RÉELLEMENT OBSERVÉS (`profiles[:k_slots]`), et c'est la règle déjà testée « moins de 2 occurrences, pas de marqueur » qui retire l'orphelin — aucun cas particulier ajouté. Les deux options écartées : faire lever la troncature contredit §11, qui l'autorise du moment qu'elle est loguée ; garder le marqueur solitaire revient à demander à l'agent d'apprendre à ignorer un symbole. Conséquence assumée et documentée : le débordement des 10 marqueurs (`COMBI_GROUP_MARKER_COUNT`) devient inatteignable depuis ce chemin (au plus `k_slots // 2` groupes marqués par registre) et reste le contrat de `assign_combi_group_markers` pour un appelant direct — le `raise` et son test sont conservés. **`obs_size` inchangé (18269), aucun `config/*.json` touché, aucun comportement modifié sur les rosters actuels.** 272 tests verts sur les 8 fichiers qui importent le module, mutation rouge puis verte (`__pycache__` purgé).

Correctifs hors chantier (2026-09-10, suite 53) : `worktree-journalisation-decisions-agent` — **aucune décision d'agent ne laissait de trace : impossible de savoir si l'agent déclare ses réserves 20.01 ou décline systématiquement.** Défaut mesuré à l'ouverture : `grep -rn "decision" ai/step_logger.py` rendait **0 hit**, `reserves_declaration|CHOICE_0|agent_decision` sur `ai/analyzer.py` et `ai/analyzer_core.py` rendait **0 hit** dans les deux, et la branche qui applique la déclaration (`engine/w40k_core.py`) construisait un payload `agent_decision` sans aucun `append_action_log`. Seul `rule_choice` était journalisé, par un appel écrit à la main. Le relevé est désormais posé à un site UNIQUE, `_record_agent_decision_action_log`, appelé par un mince `_handle_agent_decision_action` qui encadre l'ancien corps renommé `_dispatch_agent_decision_action` — l'encadrement est nécessaire parce que l'application de la décision l'EFFACE, et qu'elle n'existe donc plus au retour. Le libellé passe par `format_agent_decision_message` (`engine/action_log_utils.py`), constructeur unique partagé par le Game Log PvP et `step.log`. `LOG_GRAMMAR_VERSION` passe de 7 à 8. **`rule_choice` est gardé À PART, non absorbé**, sur trois raisons vérifiées : sa ligne nomme la RÈGLE retenue, information que le site commun n'a pas ; elle est écrite par trois chemins dont deux ne passent jamais par une décision d'agent (prompt humain PvP, `pve_controller`) ; et `ai/analyzer_core.py` la parse déjà pour `rule_choice_selection_usage`. **`agent_decision` est NON-incrémentant**, aligné sur ce que fait déjà le moteur — mesuré : `charge_placement` produit une ligne `charge`, `allocation_model` des lignes `shoot`/`combat`, `move_after_shooting` la sienne, toutes incrémentantes ; un relevé incrémentant aurait compté deux steps pour un seul `step()`. L'analyzer rend en section 1.7 le compte par type et par candidat, puis le taux de déclaration : `20.01 strategic-reserves declaration rate: 1/4 = 25.0% (CHOICE_0 = declared)`. 520 tests verts, pyright en 0 erreur sur les sept fichiers, rouge par mutation prouvé des deux côtés (journalisation neutralisée → 2 rouges sur 4 ; branche de lecture de l'analyzer neutralisée → 5 rouges sur 6, le 6e vérifiant précisément l'absence de taux sur un journal sans décision). 📝 **Compteurs fin d'épisode — constat mesuré (rectif doc 2026-09-10)** : `Steps=` et `Total=` sont corrects — faux diagnostic corrigé. Mesuré sur trois épisodes complets : écart `Total=` vs steps gym de −129/320 (reserves_20_fixture1 seed 0), −40/80 et −21/63 (smoke_t5_bare MELEE seeds 1 et 8) ; décisions d'agent en expliquent 25, 1 et 0. Cause dominante : `generic_handlers.end_activation` incrémente `episode_steps` une seconde fois en fin d'activation (arg2==1), ce qui fait valoir `Total=` environ deux fois le nombre de steps gym. la branche `EPISODE END` de `analyzer_core.run` n'extrait de cette ligne que `Duration=`, `Winner=` et `Method=` — ni `Steps=` ni `Total=` ne sont lus ; les longueurs d'épisode de l'analyzer viennent de `state.episode_actions`, incrémenté dans `analyzer_core.run` sous la garde `step_inc`. Ces types de décision n'ont jamais produit de ligne incrémentante. Rectifié : docstring de `log_episode_end` et légende des champs dans les docs de référence.

Correctifs hors chantier (2026-09-10, suite 54) : `worktree-refus-du-move-reactif` — **le mouvement réactif était devenu une décision de joueur sans que rien n'en tire les conséquences hors de la fenêtre elle-même.** `reactive_decision_mode` passe à `"state"` aux deux sites de `engine/w40k_core.py`, et `decline_reactive_move` — formalisé depuis l'origine et sans producteur — a enfin le sien. La fenêtre devient REPRENABLE : son curseur vit dans `game_state` (`PENDING_REACTIVE_MOVE_KEY`, `drive_reactive_move_window`) au lieu de variables locales, et le jet de D6 est persisté — une suspension entre le jet et le choix le re-tirerait, et le joueur choisirait sur une portée qui n'est plus celle affichée. **Trois conséquences que rien ne portait**, chacune mesurée : la phase ne peut plus se terminer sous une décision armée (`_defer_phase_advance_while_reacting`, posé avant les DEUX boucles de cascade — sur un `move_after_shooting` en DERNIÈRE activation du pool, la cascade traversait shoot → charge → fight → tour suivant → command → move en une seule action et `command_phase_start` purgeait la fenêtre sous la question) ; toute autre action est barrée pendant l'attente par un succès INERTE à la forme du payload d'attente d'allocation manuelle (`_reject_action_while_reactive_move_pending`, au lieu de laisser un second mouvement lever `RuntimeError[reactive_move.reentrance]` sur un clic banal) ; le siège PvE est tranché sur-le-champ (`_resolve_reactive_move_decision_for_ai_seats`), sans quoi la partie se bloquait, `execute_ai_turn` refusant `not_ai_player_turn` pour une décision posée pendant le tour de l'adversaire. Le bot répond `CHOICE_0` (`ai/env_wrappers.py`), MÊME exception que `move_after_shooting` : `CHOICE_0` EST la destination de l'ancienne heuristique `_select_reactive_destination`, donc l'adversaire de référence ne bouge pas. **`obs_size` inchangé (18269)** : le type `reactive_move` est ajouté EN FIN de `AGENT_DECISION_TYPE_IDS` — jamais inséré, un type déclaré gardant son index — soit 12 types pour 16 colonnes réservées ; c'était le seul conflit du merge, `main` ayant ajouté `reserves_declaration` au même endroit, et les deux sont conservés. **Le refus ne consomme PAS la capacité du tour** (`units_reacted_this_enemy_turn` n'est alimenté que sur le chemin du mouvement appliqué) : la question revient à chaque mouvement ennemi finissant à portée dans le même tour. ⚠️ Lecture ASSUMÉE, tranchée par l'utilisateur : le corpus ne définit pas le « Once per turn » de la datasheet — « once per » n'apparaît que deux fois dans les 27 PDF de `Documentation/40k_rules/`, « once per battle » (15 Stratagems) et « USE LIMIT: Once per turn » (16 Actions, limite portant sur l'accomplissement) — donc la lecture est portée par un test et par un commentaire, pas par la règle. Front : le panneau n'est posé qu'à un siège HUMAIN, verrouillé par deux cas dont le cas humain sert de contre-épreuve. **Exposition entraînement : NULLE, mesuré** — le seul porteur est `Termagant` (1 datasheet sur 179), absent des rosters d'`ArmageddonAgent_x1`, et le seul scénario d'entraînement qui le porte appartient à `CoreAgent`, dont le training config a été supprimé le 2026-07-19 : `load_agent_training_config('CoreAgent')` lève `FileNotFoundError`, et la découverte de scénarios (glob NON récursif) ne trouverait rien dans son `scenarios/training/`. **Aucun gain de win-rate n'est revendiqué.** 45 tests moteur + 4 vitest, 3 mutations rouge→vert (`__pycache__` purgé) ; 215 tests verts sur les deux côtés du conflit de merge et sur les sept fichiers de test ajoutés par `main` depuis la base de la branche. ⚠️ **NON VALIDÉ EN NAVIGATEUR** : le merge a été demandé avant l'essai PvP, les services tournant depuis `main` et non depuis un worktree — reste à voir en partie le panneau chez le bon joueur, la phase qui n'avance pas sous la question, et la question qui revient après un refus.

Correctifs hors chantier (2026-09-10, suite 55) : `worktree-verrous-journalisation-decisions` — **deux verrous ajoutés à la journalisation des décisions de la suite 53, et un trou annoncé qui n'en était pas un.** Le prompt d'ouverture demandait un test de CONTRAT paramétré sur les onze `AGENT_DECISION_TYPE_IDS` et un test de NON-CONTOURNEMENT fondé sur « `consume_pending_agent_decision` est l'effaceur unique » : **les deux prémisses sont fausses, vérifié avant d'écrire.** Trois sites appellent `clear_pending_agent_decision` hors de `consume` (`engine/w40k_core.py` branches `mortal_wounds_target` et `rule_choice`, `engine/game_state.py` purge de command phase), et `_resolve_faction_decisions_for_ai_seats` applique `returned_models_profile`, `returned_models_placement` et `waaagh_call` DIRECTEMENT sans relevé — sièges bot/PvE hors gym, dont les choix viennent des heuristiques `_select_ai_*`, où les journaliser fausserait le taux mesuré. Le test paramétré, lui, est **infaisable et vide** : les branches du dispatch exigent un état riche (contexte de tir, plan de charge, file 20.01, `_pending_exhortation_fight`, `pending_rule_choice_queue`, fenêtre réactive), et neutraliser le dispatch pour les atteindre rendrait le test vert pour n'importe quel type, y compris un type non branché. **Le mode de panne « un type de plus non journalisé » n'existe pas structurellement** : la garde `NotImplementedError` du dispatch force toute branche nouvelle sous l'encadrement qui relève, et `ai/analyzer.py` compte les types par `defaultdict` sans en câbler aucun — le douzième type (`reactive_move`, suite 54) est arrivé journalisé sans qu'une ligne de relevé ait été écrite pour lui. **Ce qui manquait vraiment**, et qui est livré : le cas d'un type qui produit AUSSI une ligne d'effet. `reserves_declaration`, seul type couvert de bout en bout, n'en produit aucune — sur lui, « la ligne d'effet compte le step » et « le relevé ne le recompte pas » sont indiscernables. `test_le_dispatcher_de_decision_route_bien_le_type` vérifie désormais les DEUX entrées d'`action_logs` (`move_after_shooting` incrémentante, `agent_decision` non incrémentante, toutes deux dans la liste blanche `_STEP_LOG_TYPE_MAP`), soit l'invariant que le commentaire de `_STEP_LOG_NON_INCREMENTING_TYPES` affirmait sans test. Second verrou : `test_l_encadrement_du_releve_n_a_qu_un_appelant`, analyse statique AST sur `engine/`, `ai/` et `services/`, qui exige que `_dispatch_agent_decision_action` ET `_record_agent_decision_action_log` n'aient qu'un appelant, `_handle_agent_decision_action` — gardiennage contre un chemin qui appliquerait sans relevé, valeur annoncée comme telle dans son docstring et pas davantage. ⚠️ La branche « qui écrirait son propre relevé à côté » était annoncée ici et dans le docstring alors que ce test ne la voyait pas : elle n'appelle ni l'application ni le relevé, donc elle passe — corrigé en suite 56. 68 tests verts, 3 mutations rouge→vert (`__pycache__` purgé à chaque fois), dont deux invisibles à l'exécution (`if False:`) que seul le test statique voit.

Correctifs hors chantier (2026-09-10, suite 56) : `worktree-verrou-journal-decisions-review` — **les deux sondes statiques de la suite 55 étaient contournables ; les deux contournements sont mesurés puis barrés.** Premier trou : le visiteur AST n'inspectait que `Call.func`, si bien qu'une référence NON APPELÉE au symbole gardé passait — mutation appliquée, `applique = self._dispatch_agent_decision_action` puis `applique(action)` est un second chemin d'application sans relevé, et les deux paramétrages restaient VERTS. Le contrôle porte désormais sur `visit_Attribute` : une référence liée ici et appelée ailleurs est un appel, différé. Second trou, celui que le docstring de la suite 55 annonçait à tort : une branche qui poste elle-même `{"type": "agent_decision", ...}` n'appelle NI l'application NI le relevé, donc ne peut pas être vue par un verrou d'appelant — mutation appliquée, verte elle aussi. `test_aucune_entree_agent_decision_n_est_ecrite_hors_du_site_unique` sonde le LITTÉRAL (et non l'appel à `append_action_log`, le dictionnaire pouvant être construit à distance de son émission) et exige un producteur unique, `_record_agent_decision_action_log`. Ce que cette seconde entrée casserait concrètement : `models_segment` est posé VIDE au site unique pour que `_build_step_log_details` n'aille pas lire les positions LIVE ; construite à la main, la clé manque, et une décision 20.01 — jouée AVANT toute mise en place — sort avec ses socles à (-1,-1). **Un troisième finding écarté**, et la raison est un choix : `self._pile[-1]` attribue l'appel à la fonction la plus INTERNE, donc une closure définie dans `_handle_agent_decision_action` ferait rougir la sonde alors que l'invariant tient. Attribuer à `_pile[0]` supprimerait ce faux rouge, mais rendrait invisible la closure définie au bon endroit puis RETOURNÉE et appelée ailleurs : sur une sonde de contournement, le faux rouge lisible vaut mieux que le vrai trou silencieux. 7 tests verts, 2 mutations rouge→vert, chacune vérifiée VERTE sur la sonde d'avant (`__pycache__` purgé).

Correctifs hors chantier (2026-09-10, suite 54) : `worktree-purge-coreagent` — **`config/agents/CoreAgent/` et `config/agents/_p2_rosters/150pts/` sont supprimés (24 fichiers).** L'agent `CoreAgent` n'a plus ni training config ni rewards config depuis `20a2d479` (2026-07-19), donc `load_agent_training_config` lève `FileNotFoundError` et aucun run ne démarre sur cet agent. Ce qui restait était le banc `training_benchmark` : 4 scénarios que le glob de `_gather_scenario_files_in_dir` ne voyait pas (il ne descend pas dans les sous-dossiers de `scenarios/training/`), mais qui restaient jouables par chemin explicite — `def _load_units_from_roster_refs` dérive l'agent du CHEMIN du scénario et non de `--agent`, et `ai/train.py` accepte un chemin de scénario explicite passé à `--scenario`. **Ce banc est abandonné sur décision utilisateur** : sa suppression rend orphelin tout `config/agents/_p2_rosters/150pts/`, qu'aucun autre scénario ne référence, d'où sa suppression dans le même geste. **Conséquence mesurée et acceptée** : le corpus de `test_no_played_roster_unit_carries_an_excluded_keyword` passe de **42 à 23** datasheets résolues (19 n'étaient citées que par ce banc — `Carnifex`, `Genestealer`, `Hormagaunt`, `Zoanthrope`, `DreadnoughtBallistus`… ), la garde `>= 20` du test tient et il reste vert ; les datasheets elles-mêmes vivent dans `frontend/src/roster/**/units/*.ts` et ne sont pas touchées. `def main` de `scripts/roster_aggregate_rankings.py` avait `--matchup-dir` par défaut sur `config/agents/CoreAgent/rosters/150pts/matchups`, un dossier absent du dépôt : l'argument devient **requis** (T1), verrouillé par un test neuf, rouge constaté en remettant le défaut. Docs d'équerre : `README.md` et `entrainement.md` cessaient de proposer `--agent CoreAgent`, `lecons_de_methode.md` disait qu'un scénario du dépôt renseignait `agent_roster_seed` (**plus aucun** ne le fait), `mcts_adversaire.md` renvoyait à la config effective de `CoreAgent`. **`ai/models/CoreAgent/` est laissé intact** (5 fichiers, 19 Mo) : `CLAUDE.md` interdit toute suppression automatique sous `ai/models/`.


---

## Soutien — backlog hors jalons

### Prêt à démarrer

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| moteur+training | **Stratagèmes réactifs** — Fire Overwatch §15.08 + Heroic Intervention §15.11 ; **slots obs réservés avant R1** (`"charged"` UNIT_BIN + 2 types AGENT_DECISION) livrés 2026-08-25, implémentation J4 | [moteur.md#reactive-stratagems](moteur.md#reactive-stratagems) | 🚫 |
| front | Validations navigateur en attente | [front.md#validations-nav](front.md#validations-nav) | ⚡ |

### À cadrer avant d'ouvrir

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| moteur | LoS 3D : tir à travers un mur depuis un étage (signalé 2026-08-11, jamais cadré) | [moteur.md#los-mur-etage](moteur.md#los-mur-etage) | 🚫 |
| bot+training | Chantier récompense distinct (relevé du chantier panel) | [bot.md#recompense](bot.md#recompense) | 🚫 |

### Lourds — re-cadrer avant toute reprise

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| moteur | Preview de tir sans deepcopy | [moteur.md#preview-tir](moteur.md#preview-tir) | 🚫 |
| infra | **Accélération entraînement RL** — Phase 0 ✅ + Phase 1 ✅ **mesurée machine au repos 2026-08-26** (1.1–1.9 livrés ; **1.7 ✅ buffers numpy livrés (2026-08-26)** ; gain réel sur la QUEUE — P99 −44 %, wall −32 %, médiane −7 % sur 3+3 répétitions) ; bench_env_step ✅ **reproductible (2026-08-26)** ; **Phase 2 ✅ complète (2.1–2.4, 2026-08-26)** (GPU buffer, GPU metrics, inline masks ; +13–16 % fps mesuré run réel) ; **Phase 4 : 4.1 ✅ livré (2026-08-27)** — gate d'étape parallélisé, décision B actée et implémentée (B1–B7, unification du harnais checkpoint dans le harnais bot, parité confirmée 8-ép + tests) ; **4.2 ✅ livré (2026-08-27)** — pool persistant + jeton version `(model_path, mtime)`, rechargement seulement sur changement, `create_checkpoint_eval_pool`, `ExploiterProbeCallback._on_training_start/end` (**ce couple de hooks retiré le 2026-09-04 : il recréait le pool à chaque tranche de `learn()`, la persistance annoncée n'existait pas — cf. `worktree-pool-sondes-persistant`**) ; **4.3 ✅** `bot_eval_n_workers` → 2 (n=6 plus lent que n=2, F_6=1 293 s vs F_2=58 s, 2026-08-27) ; **Phase 3 ✅ livrée (2026-08-28)** — collecte distribuée Option A : workers déroulent 340 steps en autonome avec policy CPU gelée, retournent trajectoire, learner update-only ; 4 bugs review corrigés (double norm, raw_gc corrompu, ordre bootstrap, num_timesteps) ; 34+5 tests verts ; **gain time/fps mesuré : médiane 487 fps (3 reps x1_debug, machine au repos, 2026-08-28) vs 229 fps Phase 2 → +113 % (×2,1)** ; **correctifs Phase 3 (2026-08-28)** : durées épisodes distribuées injectées depuis les workers (cross-traj sentinel -1.0, reset exclu), crash action_dist workers corrigé (PointerMaskablePolicy recrée action_dist absent), 5 bugs timing/guard/stale corrigés, simplification harnais ; 35 tests verts ; **correctifs Phase 3 (2026-08-29)** : fix-pyright-check-ai-rules (20 erreurs pyright + terme interdit), fix-tests-back (8 tests rouges ai corrigés), phase3-reward-fix (normalisation rewards cold-start + diag/ TF), fix-ret_var-rescaling (value_loss gonflé — clip×rescale 15×), diag-logprob-drift (instrumentation drift log_prob/ratio mb0 — `diag/logprob_drift_mean_abs_mb0` + `diag/ratio_mb0_mean`, exp(mean) pour éviter débordement float32 et biais Jensen) ; 27 tests verts ; **retrait du re-scaling du critique (2026-08-29)** — deux approches successives du même défaut cold-start : d'abord une garde `count > 1.0` (test-cold-start-value-scale, `689729a0`), puis la suppression complète du facteur `sqrt(old_ret_var)/sqrt(new_ret_var)` sur `values`, `last_values` et le bootstrap (phase3-supprimer-rescaling-values, `ed091ab6`), qui rend la garde caduque : au rollout 1 d'un run `--new` le facteur valait 0,060 et écrasait les prédictions du critique de 17× (`value_loss` 15,1 et `explained_variance` −0,37 au rollout suivant), `ret_var=1.0` n'étant que la valeur d'initialisation de `RunningMeanStd` ; SB3 et le chemin stepwise ne rescalent jamais. Verrous durcis ensuite (audit-verrous-cold-start) : assertion exacte `returns[-1] = r_last + gamma*last_value` au lieu d'un seuil sur `returns.mean()` qui laissait passer un `last_values ×0.5` (vérifié vert), et couverture du chemin de clipping (`raw_reward=150`, le cas que le `ret_var` gelé saturait à 10,0) ; un test comparant deux scalaires calculés sur place a été supprimé, prouvé vert face au défaut réintroduit ; 31 tests verts. **ROOT CAUSE du non-apprentissage trouvée et corrigée (2026-08-29)** : aliasing des buffers scratch d'observation — le worker distribué stockait les obs PAR RÉFÉRENCE pendant 340 steps alors que le moteur les sert dans des buffers réutilisés (`observation_builder`, contrat « ne jamais stocker au-delà du step courant ») → buffer du learner = état final répliqué (tout sauf `global_cont`), d'où `diag/ratio_mb0` 0,92-0,95 (attendu 1,0), `explained_variance` figée à ~0, critique effondré sur une constante ; le chemin stepwise était protégé par accident (pickle du pipe) ; présent depuis `4b2fe20d` (commit initial Phase 3) — le retrait du re-scaling était justifié mais n'était pas la cause. Fix : copie profonde dans `normalize_obs_with_snapshot` + `terminal_observation` (2 sites), verrous rouge/vert `test_obs_stored_are_copies_not_scratch_refs` + `test_terminal_observation_is_copied_before_reset` ; 33 tests verts ; **jumeau `n_envs = 1` corrigé (2026-08-29)** — `DummyVecEnv` (SB3 2.9) pose `terminal_observation = obs` puis `reset()` puis `deepcopy`, donc le bootstrap `TimeLimit.truncated` évaluait l'obs INITIALE de l'épisode suivant (mesuré chaîne de production : 28/28 clés identiques au post-reset avant fix, 4/28 différentes après) ; copie à la sortie de `BotControlledEnv.step` et `SelfPlayWrapper.step`, 2 verrous rouge/vert. **Validation en cours : run `--etape P2` du 2026-08-29, critère `ratio_mb0 = 1,0` exactement + `explained_variance` croissante dès les premiers rollouts** | [infra.md#perf-entrainement](infra.md#perf-entrainement) | ⚡ |
| infra | Noyau natif BFS move/empreintes — pool de move = 29 % d'une partie d'évaluation ; **`geodesic_move_reach` sorti de la cible le 2026-09-08** (20,4 % → 5,2 % du step en Python pur : index entiers de `hex_index_table` + expansion par couches, puis carte d'obstacles `BlockedBitmap` mémoïsée ; périmètre obstacles + BFS 8,8 % → 7,0 % du wall, mesuré 3 fois une variante par processus, machine au repos). Cibles restantes : `erode_move_pool_by_squad_block`, `hex_line_iter` (gain Python facile pris le 2026-09-08, −8,0 % sur la boucle ; restait 9,22 % de temps propre **avant `BlockedBitmap`**, part à re-mesurer), `arm_charge_placement_decision` (13,59 % re-mesuré après `BlockedBitmap` ; mémo inter-intentions livré le 2026-09-09, −18,4 % de calculs d'engagement, le reste est du volume de recherche). **Mesurer au py-spy, pas au cProfile : facteur 2,5 de déformation** | [infra.md#noyau-natif](infra.md#noyau-natif) | ⚡ |
| infra | Migration PostgreSQL | [infra.md#postgresql](infra.md#postgresql) | ⚡ |
| infra | **Recherche (MCTS) — sièges B et C, entraînement** : distillation Expert Iteration dans PPO (étape de curriculum `P11`), puis champion qui cherche dans le pool (`P12`, optionnel). L'usage « adversaire MCTS indépendant » de `mcts_adversaire.md` est REJETÉ sur mesure (2026-09-08). Ne s'ouvre qu'après le siège A et son gate G1 — même document, `Documentation/Chantiers/backlog/mcts.md` | [infra.md#mcts](infra.md#mcts) | 🚫 |
| bot | Tranches 2-3 benchmark — schedule P0→P10 + exploiters : code et tests livrés 2026-08-22 (`--etape`, `curriculum.json`, pool figé par-env, `ExploiterProbeCallback` sondage synchrone + `validate_exploiter_protocol` + `exploiter_config`, 24 tests verrou) ; restent les 14 runs (~260 h) | [bot.md#league](bot.md#league) | 🚫 |

---

## Hygiène documentaire

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| doc | 🟠 **Refonte Documentation/** — P1–P3 livrées (2026-08-27) ; P4 consolidation : moteur+backlog (16→9 docs, 20 sources archivées), training (7→5 docs, 7 sources archivées) et **jeu+outils** (jeu 3→3 noms d'objet, outils 7→4 dont fusion 5→1 `outils_conformite`, 11 sources archivées) livrés 2026-08-28 ; **v11** (5 renommages d'objet, strates §9.4 purges) et **infra** (3 renommages d'objet, corps re-vérifiés, gardes re-pointées) livrés 2026-08-28 — P4 complète, les 5 lots livrés ; correctif de suivi : slot `m` du dashboard de `metriques.md` réaligné sur `m_immediate_reward_ratio_mean` (l'ancien tag n'est plus émis ; ce tag est lui-même devenu `n_immediate_reward_ratio_mean` le 2026-09-08, cf. suite 18) ; **correctif de suivi 2026-08-28** : 2 tests rouges de vérification documentaire recalés sur les noms P4 (V11_phaseA → decisions_du_joueur, V11_agent_rework → index_v11), lot P4 v11 finalisé (sweep 5 refs code, strate périmée §9.4bis + T6, tables correspondance 5 docs) | [doc.md#refonte](doc.md#refonte) | ⚡ |
