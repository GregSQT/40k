# Roadmap Index — Ordre global du travail

> **Source unique de priorité.** Ce fichier tranche l'ordre, les fichiers sujets tranchent le
> contenu. Historique d'un sujet : `archives/<sujet>.md`. `archives/ROADMAP.md` est un FOSSILE
> gelé (l'ancien fichier monolithique) : ne pas le lire en session, ne plus le mettre à jour.
>
> **Règles d'arbitrage :** Code > décision datée > priorité ici > tout autre doc.
> Conflit résiduel → demander à l'utilisateur.
>
> **Discipline.** Ouvrir = ajouter une ligne ici D'ABORD. Livrer = marquer ✅ ici + vider/archiver
> dans le fichier sujet, dans la même livraison.
>
> **Outillage.** `python3 scripts/check_doc_references.py` contrôle ce fichier, les fichiers
> sujets et les deux contrats permanents (renvois, liens, valeurs recopiées, ancres, sortes),
> et vérifie qu'aucun chantier ouvert de `Documentation/Chantiers/backlog/` n'est devenu
> inatteignable depuis ce fichier — un document que plus aucun fichier sujet ne cite n'est plus
> priorisé, il est seulement stocké.
> La porte de fusion `scripts/check_roadmap_declared.py` (hook `prepare-commit-msg`, versionné
> dans `.githooks/`) : une fusion dans `main` est refusée quand **2** chantiers ont été livrés
> sans que ce fichier bouge. Se débloquer : écrire la ligne du chantier puis `git add` (l'index
> vaut déclaration) ; fusion hors chantier : `ROADMAP_GATE=off git commit`.
>
> **Contrats permanents** (jamais archivés, hors roadmap) :
> `Documentation/Chantiers/Replay.md` (contrat `step.log`, pipeline replay) et
> `Documentation/Chantiers/analyzer_couverture.md` (matrice règle → contrôle → champs de
> log) — relus à chaque livraison qui touche le journal.
>
> **Exceptions actées** (numérotées ici et nulle part ailleurs) : `Bot_refactor.md` vit à la racine de `Documentation/Chantiers/` au lieu de `backlog/` (chantier vivant, chemin demandé) ; `archives/v11.md` porte l'historique du programme V11 entier et sert d'archive au sujet `v11_chemin_critique.md`.
>
> **Pendant entraînement.** `⚡` = peut démarrer sans interrompre un run (ne touche pas
> `config/**/*.json`, ne modifie pas le moteur). `🚫` = ne pas démarrer : risque de biaiser le
> run ou conflit direct avec le processus d'entraînement.

---

## Direction — le fil

**CAP : la démo de financement** — un agent RL crédible sur les deux rosters retenus (Space
Marines / Orks, décision 2026-07-19), prouvé par une mesure quantitative (win-rate `x1_long`
contre le panel) et une validation qualitative par un joueur externe.

Tout chantier sert un jalon ci-dessous, ou attend. Les jalons sont séquentiels ; le soutien
(analyzer, security, infra, hygiène doc) avance en parallèle quand un jalon le réclame.

| Jalon | Contenu | Critère de sortie |
|---|---|---|
| **J1 — Pipeline prouvé, ligne de base** ✅ | Run `x1_long --new` terminé le 2026-08-20 ; ligne de base REJOUÉE le 2026-08-21 sur `robust_0.8463` (post-rupture §12.15) : combined agent `0,8567`, pire bot `attrition = 0,810` ; reference bots mesurés bot-contre-bot (balanced `0,168`, denial `0,155`, reactive `0,139`) ; `benchmark_floor` remis à `0,90` le 2026-08-20 (le `0,049` mélangeait les sémantiques) — gate RETIRÉ depuis, le 2026-08-22 | Critères de [training.md#run-verif](training.md#run-verif) verts ; ligne de base panel rejouée ✅ |
| **J2 — Le gym décide tout** ✅ | P3-5, P3-6, P3-8, P4, P5 livrés ; `TOTAL_ACTION_SIZE` 1159→1389, `obs_size` 16671→16703 ; ré-entraînement `--new` nécessaire | Plus aucune décision de jeu jouée par une heuristique à la place de l'agent, hors optionnels statués par mesure de regret ✅ |
| **J3 — Mesure de référence** | Curriculum R1→R3 (URGENCE), puis `x1_long` (~6 h) | LE chiffre officiel du projet — solde §0.14, §0.67 et le critère T6 (via §10.6) |
| **J4 — Dépasser la mesure** | Capacités 06, É9 second scénario — priorisés selon ce que la mesure révèle | Win-rate au-dessus de la mesure de référence, reproductible |
| **J5 — Démo** | Validation qualitative §10.6 volet 2 (joueur externe), validations navigateur front soldées | Un externe joue contre l'agent et le trouve crédible ; le front tient la partie de bout en bout |

---

## ✅ URGENCE — Curriculum : adversaires et étalons (décision 2026-08-21)

**R0a/R0b livrés. R1→R3 absorbés par le curriculum `--etape` (décision 2026-08-30)** : le fix
aliasing obs Phase 3 (2026-08-29) invalide toute baseline antérieure ; le run `--etape P2`
post-fix tient lieu de R1 (ratio_mb0=1.0, EV→0.85). Le curriculum P0→P10 intègre les leviers
R2 (self-play) et R3 (récompense, si D.4 le justifie) séquentiellement — mesurer en standalone
n'apporterait que la décomposition du gain par levier. Fermé sans dette.
Détail : `Documentation/Chantiers/backlog/curriculum_adversaires_etalons.md`.

---

## J3 — Mesure de référence

| # | Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|---|
| 7 | training | Mesure de référence — `--test-only --step` sur champion final P10 (~8 min) | [v11_chemin_critique.md#mesure](v11_chemin_critique.md#mesure) | 🚫 |

---

## J4 — Capacités

Tâches J4 archivées → `archives/doc.md#hygiene-correctifs-ponctuels`

---

## Suspendus — ne pas commencer avant leur jalon

| Sujets | Chantier | Fichier | Jalon | ⚡/🚫 |
|---|---|---|---|---|
| moteur | **T7** Unification validation déploiement | [moteur.md#t7](moteur.md#t7) | Fix faux — re-analyser avant | 🚫 |
| moteur+training | **Force dispositions & missions** — home obj, VP scoring 4 missions, obs mission, reward conditionné, sampling, métriques | [missions.md](missions.md) | Terrains 4 missions créés par utilisateur | 🚫 |
| moteur | **Phase B** Observation des niveaux | [moteur.md#phase-b](moteur.md#phase-b) | Phase A' validée + LoS 3D complet | 🚫 |
| training+bot | **É9** Second siège + second scénario — levier `agent_seat_p2_ratio` livré 2026-08-28, plus deux correctifs de suivi le même jour : `_resolve_seat_p2_ratio` et `get_seat_stats` retournent `None` en modes de siège fixes au lieu d'un `0.5` interne trompeur (`ai/env_wrappers.py`, `ai/train.py`, tests rouge→vert). Le second SCÉNARIO reste ouvert | [training.md#e9](training.md#e9) | J4 — entraînement bot satisfaisant | 🚫 |
| training+bot | Validation qualitative §10.6 volet 2 | [bot.md#validation-externe](bot.md#validation-externe) | J5 — requis pour la démo | ⚡ |
| bot | MCTS à l'inférence §10.7 (plan B anti-coups-absurdes) | [bot.md#mcts-inference](bot.md#mcts-inference) | Après J3, seulement si la démo l'exige | 🚫 |

---

## ✅ Hygiène — correctifs ponctuels

Correctifs ponctuels livrés → `archives/doc.md#hygiene-correctifs-ponctuels`

Correctifs hors chantier (2026-09-03) : `worktree-fix-terrain-empty-string` (rejette chaîne vide dans accepts), `worktree-fix-biome-terrain-imports` (ordre imports Biome), `worktree-test-terrain-list-not-loaded` (trou test resolveSelectedTerrain liste non chargée + corrige accepts listLoaded), `worktree-fix-terrain-findings` (résolution conflit trim()), `worktree-fix-terrain-review-findings` (corrige 3 findings review terrain), `worktree-fix-terrain-review2` (guard availableTerrains + STORAGE_KEY test + trim test), `worktree-fix-terrain-simplify` (doublon import + trim redondant), `worktree-config-opus-default` (modèle par défaut Opus 5 + critères bannières CLAUDE.md), `worktree-suppress-objective-gamelog` (supprime le log des objectifs du game log PvP), `worktree-doc-hex-line-tiebreak-bias` (documente le biais de départage `hex_line` sur segments horizontaux), `worktree-cover-badge-per-model` + `worktree-cover-badge-ab-distinction` (badge de couvert par figurine — **ANNULÉS le 2026-09-03**, code restauré à l'identique de `9ec912e7` : le rendu obtenu en PvP était faux, et le badge unité-niveau d'origine fonctionnait. Cf. `archives/front.md` avant toute nouvelle tentative), `worktree-revert-wall-render` + `worktree-fix-walls-inconsistent` (rendu des murs — chantier `d3039f35`/`20e6ada5` (union d'hexagones par groupe) **ANNULÉ le 2026-09-03** : le mur devenait une chaîne d'hexagones en dents de scie d'une case d'épaisseur, illisible. Rendu par segments (capsule sur `start`/`end`) restauré à l'identique. Le second commit répare l'écrasement causé par `70cbdf26`, qui avait remis `services/api_server.py` et `BoardPvp.tsx` à leur état complet de `9ec912e7` — postérieur à `d3039f35` — et donc réintroduit le rendu par hexagones côté API pendant que `BoardDisplay.tsx` lisait des segments : `drawBoard` levait, plus aucun board), `worktree-fix-terrain-selection-crossed` (le terrain choisi dans le sélecteur n'était pas celui chargé : `default_for` de `terrain_list.json` et `terrain_ref` des scénarios avaient divergé, si bien qu'en `pvp_test` « Terrain 1 » chargeait `scenario_pvp_test.json` (terrain-mc2) et « Terrain 2 » chargeait `scenario_pvp_test_mc2.json` (terrain-mc1) — `pve` croisé de la même façon, et trois fichiers corrects inatteignables. Front et back résolvaient le MÊME fichier, donc rien ne plantait : seul le décor n'était pas celui demandé. Fix dans `default_for` seul ; le fichier sans suffixe reste le même dans les quatre modes, donc aucun plateau ne change — seule la sélection non-défaut passe de faux à juste. Le libellé par défaut devient mc2 en `pvp_test` et mc1 en `pve`, ce qui est désormais la vérité du fichier. Test paramétré rouge (4 combinaisons) → vert (7)), `worktree-fix-promote-run-state` + `worktree-fix-promote-run-state-t1` (bug curriculum : `episode_count_total` non propagé dans le run_state de l'étape promue — `promote_stage_model` copiait le run_state canonical à 0 ; fix : `save_run_state` explicite juste avant la promotion dans `_close_curriculum_stage`, levée si la clé est absente, test rouge→vert), `worktree-fix-deploy-ramp-warm-start` (bug curriculum : une étape en reprise à chaud redémarrait la rampe de déploiement à `active_ratio_start` (0.3) alors que le modèle repris l'avait parcourue jusqu'à 0.9 — la rampe est une FRACTION de la durée du run, donc reporter l'offset d'épisodes ne suffit pas (5 000 épisodes dans une rampe de 200 000 = 2,5 %). Mesuré : `active_ratio` à 0.315 à l'épisode 5000 d'un run de 200 000, soit ~69 % d'épisodes déployés par le moteur pendant que l'évaluation impose toujours le déploiement actif. Fix porté par le CODE et non par un override JSON (`deployment_mode_schedule` reste hors de `STAGE_HP_OVERRIDES_ALLOWED_TOP_KEYS`, et les étapes exploiteur n'ont droit à aucun override — E1/E2/E3 sont ainsi couvertes). La valeur atteint le moteur en DONNÉE (`parent_deploy_active_ratio_start` → `make_training_env` → `W40KEngine(training_deploy_active_ratio_start=)`) : la décoration du loader ne franchit pas la frontière `forkserver`, mesuré parent 0.9 / worker 0.3 puis worker 0.9 par argument. Tests rouge→vert des deux côtés), `worktree-fix-total-episodes-cross-boundary` (bug curriculum : `total_episodes` d'une étape override n'atteignait pas les workers forkserver — le worker relisait le JSON et ignorait la valeur de l'étape, causant un dénominateur de rampe erroné dès que l'étape déclare `total_episodes` différent du profil. Fix : passage en argument `total_episodes` → `make_training_env` → `W40KEngine(training_total_episodes=)`, miroir exact de `deploy_active_ratio_start`. Rouge→vert prouvé sur `test_parent_total_episodes_argument_overrides_json` ; test de traversée forkserver `test_total_episodes_cross_boundary.py` prouve que le worker lit bien le JSON sans fix)), `worktree-unify-deploy-ratio-arg` + `worktree-complete-cross-boundary-family` (les trois constructions directes de `W40KEngine` — branches mono-env — reposaient sur la décoration du loader, quand les branches vectorisées passaient la valeur en argument : même invariant, deux mécanismes choisis par `n_envs`, soit l'asymétrie qui avait laissé passer le no-op. Elles reçoivent désormais `training_deploy_active_ratio_start` ET `training_total_episodes` comme les autres. Ajoute un verrou STRUCTUREL paramétré sur la famille traversante : un test lit l'AST de `ai/train.py` et refuse tout site passant `training_episode_start_index` sans chaque membre — un test de comportement ne peut pas couvrir un site pas encore écrit, or le défaut d'origine était un site oublié. Ajouter un membre à la famille = ajouter une entrée dans `CROSS_BOUNDARY_FAMILY`. Rouge→vert prouvé membre par membre), `worktree-diag-cover-terrain` (les badges de couvert paraissaient inversés en PvP — mesuré : escouade 105 entièrement en terrain sans badge, escouade 102 toute badgée pour une seule figurine couverte. La règle 13.08 et son rendu étaient corrects ; c'est le PLATEAU qui n'était pas celui affiché, même croisement que `worktree-fix-terrain-selection-crossed`. Confirmé par mesure : sous `terrain-mc1` l'escouade 105 est 7/7 et la 102 1/3, soit exactement l'observation, alors que le moteur chargeait `terrain-mc2` et rendait 2/7 et 3/3. **Livraison réduite aux tests** : la correction avait été portée sur `terrain_ref` des scénarios alors que `worktree-fix-terrain-selection-crossed` avait déjà corrigé l'autre bout de la même relation, `default_for` ; les deux se composent et ré-inversent, ce qu'a détecté le test ajouté ici. Scénarios, `api_server.py` et `terrainSelection.ts` remis à leur état pré-merge. Reste durable : l'assertion qui manquait — le test existant vérifiait que deux terrains ne pointent pas le MÊME fichier, or une inversion en donne bien deux distincts, donc il restait vert ; ajout de « le scénario résolu déclare le terrain demandé » et d'un test d'existence couvrant les modes sans sélecteur, dont `pve_test`. À savoir pour la suite : les positions d'unités éditées le 2026-09-03 vivent dans `scenario_pvp_test.json`, qui est désormais le scénario **Terrain 2**, alors qu'elles ont été posées pour le Terrain 1). `worktree-fix-canonical-run-state-clobber` (régression du correctif run_state ci-dessus : `_close_curriculum_stage` posait INCONDITIONNELLEMENT le compte de fin de run sur le modèle canonique. En `save_best_robust` — x1_long/x5_long, les profils des étapes de curriculum — ce zip est l'instantané robuste pris plus tôt, dont `_copy_model_artifacts` avait déjà écrit un run_state cohérent avec ses poids ; l'écrasement le datait d'épisodes jamais joués, et `promote_stage_model` propageait l'écart à `model_<agent>_<etape>.zip`, l'étape suivante repartant sur un `episode_offset` gonflé. Le défaut d'origine était un run_state ABSENT et non une valeur à zéro : n'écrire que si le fichier manque couvre les deux cas. `require_key` reste inconditionnel (T1). Rouge→vert ; les 4 tests d'origine restent verts), `worktree-remove-startPvpGame` (supprime le chemin legacy `scenario_pvp.json` / `startPvpGame`), `worktree-simplify-ramp-log` (lecture de `ramp_log` dans `install_stage_config_overrides` simplifiée), `worktree-terrain-contract-test` (contrat `terrainSuffix` front/back — test paramétré vérifie la cohérence des suffixes), `worktree-fix-useengineapi-review` (4 bugs `useEngineAPI` : rejection non catchée `getMaxTurnsFromConfig` → spinner infini, 2 crashs `mergeGameState(undefined)` sur `skipData`/`decisionData.game_state` absents, console.warn diagnostique supprimé), `worktree-terrain-ref-demarrage` (**cause résiduelle du plateau affiché ≠ plateau chargé**, le symptôme que `worktree-diag-cover-terrain` et `worktree-fix-terrain-selection-crossed` avaient approché par l'autre bout : `terrain_ref` ne partait JAMAIS avec `POST /api/game/start`. La garde `terrainsForMode(mode).length > 0` (`useEngineAPI.ts`) est évaluée au MONTAGE, alors que la liste globale de `terrainSelection` est encore vide — le fetch `/api/config/terrain-list` n'a pas répondu. L'effet de démarrage gagnait donc toujours cette course, le serveur restait sur `DEFAULT_TERRAIN_BY_MODE[mode]` pendant que `useGameConfig`, qui lui ATTEND la liste, dessinait le terrain CHOISI. Le sélecteur de terrain n'avait aucun effet sur le moteur. Mesuré entre mc1 et mc2 : 972 hexes bloquants côté moteur que le plateau ne dessine pas (zones « désertes » où le déploiement est refusé) et 964 hexes dessinés en mur que le moteur laisse libres (murs « denses » traversés) — les deux symptômes signalés, dans les deux sens. À scénario ÉGAL, murs moteur et murs servis par `/api/config/board` sont identiques (996 = 996) : le défaut était le choix du scénario, pas son chargement. Fix : le hook reçoit la liste en option et attend, comme `useGameConfig` ; les trois consommateurs de la liste sont désormais gardés. Les 2 tests « terrain_ref envoyé au démarrage » ET les 8 tests de `useGameConfig.test.ts` étaient ROUGES sur main pour le même motif — leur montage ne fournissait pas la liste, donc aucune requête ne partait ; +1 test verrouille l'attente elle-même (rouge prouvé en neutralisant la garde). L'écran d'erreur terrain passe AVANT le voile de chargement : la partie attendant désormais la liste, un fetch en échec y restait bloqué sur « Starting W40K Engine Game... » sans jamais afficher la cause. Validation navigateur en attente, cf. `front.md#validations-nav`). `worktree-test-boardwithapi-guards` (test rendu `BoardWithAPI` — ordre écrans de garde `terrainListError` avant `loading` ; simplifie fixtures terrain + helper `applyGameState`/`formatApiError` ; supprime `startGameWithScenario` désynchronisé), `9c50e7f6` (stoppe ticker PIXI au repos — GPU discret libéré), `worktree-test-boardwithapi-guard-screens` (session parallèle — conflit add/add résolu en faveur de `worktree-test-boardwithapi-guards`), `worktree-fix-boardwithapi-test-fixtures` + `worktree-simplify-boardwithapi-test-satisfies` (fixtures `AuthSession` conformes puis `satisfies` sur les deux fixtures — `BoardWithAPI.test.tsx`, `useEngineAPI.test.ts`), `worktree-fix-logger-dump-wrappers` (**la capture des métriques PPO était fausse sur trois points, et les courbes de santé PPO de tous les runs antérieurs sont à relire en conséquence — cf. [training.md#courbes-ppo-reprise](training.md#courbes-ppo-reprise)**. (1) L'enveloppe posée sur `logger.dump` par `MetricsCollectionCallback._on_training_start` n'était JAMAIS retirée, alors que SB3 appaire `on_training_start`/`on_training_end` autour de chaque `learn()` et que la boucle budgétée en épisodes en enchaîne un par tranche de quatre updates. Sur un run neuf, SB3 reconstruit son logger à chaque `learn()` et l'enveloppe morte partait avec lui — ce qui a masqué le défaut ; en REPRISE, `model.set_logger` le rend persistant et les couches s'accumulaient. Mesuré sur le run P1 du 2026-09-03 : 41 756 points sur `training_critical/clip_fraction` pour 575 updates réels, contre 1 063 pour 1 063 sur un run neuf comparable — la fenêtre de vingt valeurs de `_calculate_smoothed_metric` couvrait vingt copies du même update, donc les quatre courbes `00_critical` de santé PPO n'étaient plus lissées et paraissaient 4× à 14× plus bruitées. `_on_training_end` la retire désormais, en visant le logger effectivement enveloppé. (2) La capture se déclenchait sur `if ntv:`, donc aussi sur le `logger.dump` que `_handle_episode_end` appelle à CHAQUE fin d'épisode et qui ne porte aucun update : norme du gradient recalculée sur tous les paramètres et `entropy_coef` republié par épisode — 101 415 points pour 1 063 updates sur le run neuf du 2026-08-29. `_PPO_KEYS`, déclaré depuis l'origine et jamais lu par personne, sert enfin de filtre. (3) `step_count` était posé APRÈS `log_training_metrics`, qui écrit chacun de ses scalaires à cette abscisse : toutes les courbes `training_critical/*` et `training_diagnostic/*` étaient décalées d'un dump. Mutation prouvée défaut par défaut, 8 tests. Effet nul sur le run P1 en cours, qui a chargé le code avant. Découvert par l'analyse du prétendu « bruit 4× à 14× » du warm start, qui n'existait pas : mesurées en brut, les deux séries sont aussi stables l'une que l'autre. Le pool de workers des sondes, recréé à chaque tranche de `learn()` pour le même motif, a été traité le 2026-09-04 par le correctif suivant), `worktree-pool-sondes-persistant` (**jumeau du défaut `logger.dump` ci-dessus, sur le même couple de hooks SB3** : `ExploiterProbeCallback` et `PoolEarlyStoppingCallback` créaient leur pool de workers d'évaluation dans `_on_training_start` et le fermaient dans `_on_training_end`, appairés par SB3 autour de CHAQUE `learn()`. La boucle budgétée en épisodes en enchaînant un par tranche de quatre updates, et une sonde tombant dans une tranche différente de la précédente, chaque sonde repayait le démarrage de ses workers — la persistance annoncée par les docstrings n'existait pas. Remplacé par `_EvalPoolOwnerMixin` : création paresseuse à la première sonde, fermeture par `shutdown_probe_eval_pools` dans le `finally` de la boucle `learn()`. Ce point de fermeture plutôt que `_close_curriculum_stage` ou un `atexit` : c'est le seul qui couvre TOUS les chemins de sortie — `_close_curriculum_stage` n'est atteint ni sur échec du run ni sur exception — et il libère les 4 workers AVANT l'évaluation finale, qui prend ses propres 16 et dont la coexistence est le régime à 47 Go que documente `validate_bot_eval_worker_params`. La fermeture sur l'exception qui remonte de `_probe`, livrée le même jour par `126ecf0c` et rencontrée en conflit de merge, est conservée telle quelle : le nouveau `finally` la couvrirait, mais plus tard, après que l'exception a traversé `close_all_training_envs` et ses 30 s par VecEnv — fenêtre pendant laquelle un SIGTERM rendrait les workers orphelins (PPID=1). Mesuré sur harnais identique, 3 sondes à travers des frontières de `learn()` : 2,46/2,04/2,03 s avant, 2,57/0,02/0,00 s après ; en sonde réelle s'y ajoute le chargement de l'archive adverse dans `_worker_ckpt_cache`, mesuré à 9,4 s pour un zip de 45 Mo, économisé lui aussi. Le rechargement du modèle P1 n'est PAS économisé — `_probe` sauvegarde dans un `mkstemp` neuf à chaque sonde, donc le jeton de version du worker change de toute façon. Comptage de processus `spawn_main` après la fermeture de fin de boucle : retour à la valeur de départ, 0 orphelin. Piège rencontré et consigné dans la docstring du mixin : les deux callbacks héritent `BaseCallback` AVANT le mixin, pour que `super().__init__(verbose)` l'atteigne ; un hook `_on_training_*` posé sur le mixin serait donc masqué par les stubs de `BaseCallback` et jamais appelé, sans erreur — la première mutation de contrôle, écrite sur le mixin, est passée VERTE pour cette raison et a dû être reportée sur la classe concrète. 3 mutations rouges prouvées, 66 tests verts), `worktree-simplify-dump-wrapper-fabriques` (nettoyage `/simplify` des deux correctifs ci-dessus : l'enveloppe `logger.dump` lisait `self._PPO_KEYS` dans son corps, donc capturait `self` — mesuré par `co_freevars` de l'enveloppe compilée : `('_model', '_original_dump', '_tracker', 'self')`. Elle vit le temps d'un `learn()` sur un logger que SB3 reconstruit à chaque tranche, d'où un cycle `callback → _dump_logger → logger → dump → callback` par logger, que le comptage de références ne casse pas et qui retient tracker et historiques d'épisodes jusqu'au gc générationnel. L'attribut est hissé en local avant la closure ; verrou `test_the_wrapper_does_not_capture_the_callback`, rouge prouvé. Les onze arguments d'`ExploiterProbeCallback` étaient par ailleurs retapés à l'identique dans trois blocs (deux dans `test_checkpoint_eval_parallel.py`, un dans `test_probe_eval_pool_lifetime.py`) et ceux de `PoolEarlyStoppingCallback` dans deux : fabriques `exploiter_probe_callback`/`pool_early_stopping_callback` dans `tests/unit/ai/_fabriques.py`, import différé pour ne pas tirer stable-baselines3 dans les tests d'analyzer. Le récit mesuré du défaut `logger.dump`, recopié à l'identique dans le docstring de module du test et dans la roadmap, ne vit plus que dans [training.md#courbes-ppo-reprise](training.md#courbes-ppo-reprise) ; les numéros de ligne de `sb3_contrib` ne subsistent qu'au site canonique, la docstring d'`_EvalPoolOwnerMixin`. Deux docstrings devenues fausses au merge du correctif précédent corrigées : « la même discipline que le pool des callbacks de sonde » et « le SEUL endroit qui ferme ce pool sur ce chemin ». Aucun changement de comportement, 76 tests verts), `worktree-ppo-dump-marker-canonique` (le prédicat de capture de l'enveloppe `logger.dump` énumérait à la main sept noms de métriques de santé PPO recopiant ce que `patched_ppo.train()` enregistre — un inventaire à garder en phase avec `train()`, et le troisième défaut du même chemin après l'enveloppe non retirée et le `if ntv:`. Remplacé par le marqueur canonique `train/n_updates`, que `PatchedMaskablePPO.train` (`ai/patched_ppo.py`) enregistre exactement une fois par update : il COMPTE les updates là où un nom de métrique compte les dumps qui en portent une. Vérifié plutôt que déduit : `PatchedMaskablePPO` est la seule classe PPO instanciée par `ai/train.py` et surcharge `train()`, donc le `train()` d'origine de SB3 ne s'exécute jamais ; `train/learning_rate` — la seule clé `train/` du lot que `patched_ppo` n'écrit pas lui-même — vient de `_update_learning_rate`, appelé en tête du MÊME `train()`, donc part dans le même `name_to_value` ; `exclude="tensorboard"` n'écarte rien de `name_to_value`, l'exclusion vivant dans `name_to_excluded` à part ; aucun autre producteur de clés `train/` dans le projet, donc le rétrécissement du prédicat ne perd aucune capture. Le verrou ajouté défend l'ÉTROITESSE du prédicat et non la capture : les six tests existants portent tous les noms de santé dans leur double, si bien qu'un retour à l'inventaire les laisserait verts — mesuré, cette mutation ne rougissait aucun d'eux. `test_the_predicate_is_the_marker_and_not_the_health_metrics` dumpe le double PRIVÉ de son seul marqueur et exige zéro capture ; rouge prouvé sur cette mutation, `__pycache__` purgé. 10 tests verts, review sans finding), `worktree-review-pool-sondes` (correctifs de review du pool des sondes, superposés à `probe-pool-workers-require-key` ci-dessus : la lecture de configuration du pool, partie avec `_on_training_start`, n'avait plus lieu qu'à la PREMIÈRE sonde. `bot_eval_n_workers_intermediate` est désormais exigée au démarrage par les deux sites qui construisent les sondes, mais `vec_normalize` et `vec_normalize_eval` restaient lues des heures plus tard, à la première sonde ; `prepare_probe_eval_pools`, appelée avant la boucle `learn()`, résout et mémorise TOUTE la configuration du pool sans démarrer de worker — la sonde ne relit plus rien, et le compte de workers résolu reste celui que `_probe` passe en `n_workers_override`. Plus `shutdown_probe_eval_pools` isolée par porteur — appelée depuis un `finally`, une fermeture qui lève y remplaçait l'exception en cours de propagation, Ctrl-C compris, et abandonnait les pools des porteurs suivants — `_eval_pool`/`_eval_n_workers` hissés en attributs de classe du mixin, et le commentaire de la boucle corrigé : `shutdown(wait=False)` DEMANDE la fermeture avant l'évaluation finale sans l'attendre, `wait=True` la garantirait au prix d'un Ctrl-C bloqué jusqu'à la fin de la sonde en vol. 4 défauts réintroduits rouges un par un, 58 tests verts. Cf. `Chantiers/backlog/perf_entrainement.md`).

---

## Soutien — backlog hors jalons

### Prêt à démarrer

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| moteur+training | **Stratagèmes réactifs** — Fire Overwatch §15.08 + Heroic Intervention §15.11 ; **slots obs réservés avant R1** (`"charged"` UNIT_BIN + 2 types AGENT_DECISION) livrés 2026-08-25, implémentation J4 | [moteur.md#reactive-stratagems](moteur.md#reactive-stratagems) | 🚫 |
| front | Validations navigateur en attente | [front.md#validations-nav](front.md#validations-nav) | ⚡ |

### À cadrer avant d'ouvrir

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| moteur | LoS 3D : tir à travers un mur depuis un étage (signalé 2026-08-11, jamais cadré) | [moteur.md#los-mur-etage](moteur.md#los-mur-etage) | 🚫 |
| bot+training | Chantier récompense distinct (relevé du chantier panel) | [bot.md#recompense](bot.md#recompense) | 🚫 |

### Lourds — re-cadrer avant toute reprise

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| moteur | Preview de tir sans deepcopy | [moteur.md#preview-tir](moteur.md#preview-tir) | 🚫 |
| infra | **Accélération entraînement RL** — Phase 0 ✅ + Phase 1 ✅ **mesurée machine au repos 2026-08-26** (1.1–1.9 livrés ; **1.7 ✅ buffers numpy livrés (2026-08-26)** ; gain réel sur la QUEUE — P99 −44 %, wall −32 %, médiane −7 % sur 3+3 répétitions) ; bench_env_step ✅ **reproductible (2026-08-26)** ; **Phase 2 ✅ complète (2.1–2.4, 2026-08-26)** (GPU buffer, GPU metrics, inline masks ; +13–16 % fps mesuré run réel) ; **Phase 4 : 4.1 ✅ livré (2026-08-27)** — gate d'étape parallélisé, décision B actée et implémentée (B1–B7, unification du harnais checkpoint dans le harnais bot, parité confirmée 8-ép + tests) ; **4.2 ✅ livré (2026-08-27)** — pool persistant + jeton version `(model_path, mtime)`, rechargement seulement sur changement, `create_checkpoint_eval_pool`, `ExploiterProbeCallback._on_training_start/end` (**ce couple de hooks retiré le 2026-09-04 : il recréait le pool à chaque tranche de `learn()`, la persistance annoncée n'existait pas — cf. `worktree-pool-sondes-persistant`**) ; **4.3 ✅** `bot_eval_n_workers` → 2 (n=6 plus lent que n=2, F_6=1 293 s vs F_2=58 s, 2026-08-27) ; **Phase 3 ✅ livrée (2026-08-28)** — collecte distribuée Option A : workers déroulent 340 steps en autonome avec policy CPU gelée, retournent trajectoire, learner update-only ; 4 bugs review corrigés (double norm, raw_gc corrompu, ordre bootstrap, num_timesteps) ; 34+5 tests verts ; **gain time/fps mesuré : médiane 487 fps (3 reps x1_debug, machine au repos, 2026-08-28) vs 229 fps Phase 2 → +113 % (×2,1)** ; **correctifs Phase 3 (2026-08-28)** : durées épisodes distribuées injectées depuis les workers (cross-traj sentinel -1.0, reset exclu), crash action_dist workers corrigé (PointerMaskablePolicy recrée action_dist absent), 5 bugs timing/guard/stale corrigés, simplification harnais ; 35 tests verts ; **correctifs Phase 3 (2026-08-29)** : fix-pyright-check-ai-rules (20 erreurs pyright + terme interdit), fix-tests-back (8 tests rouges ai corrigés), phase3-reward-fix (normalisation rewards cold-start + diag/ TF), fix-ret_var-rescaling (value_loss gonflé — clip×rescale 15×), diag-logprob-drift (instrumentation drift log_prob/ratio mb0 — `diag/logprob_drift_mean_abs_mb0` + `diag/ratio_mb0_mean`, exp(mean) pour éviter débordement float32 et biais Jensen) ; 27 tests verts ; **retrait du re-scaling du critique (2026-08-29)** — deux approches successives du même défaut cold-start : d'abord une garde `count > 1.0` (test-cold-start-value-scale, `689729a0`), puis la suppression complète du facteur `sqrt(old_ret_var)/sqrt(new_ret_var)` sur `values`, `last_values` et le bootstrap (phase3-supprimer-rescaling-values, `ed091ab6`), qui rend la garde caduque : au rollout 1 d'un run `--new` le facteur valait 0,060 et écrasait les prédictions du critique de 17× (`value_loss` 15,1 et `explained_variance` −0,37 au rollout suivant), `ret_var=1.0` n'étant que la valeur d'initialisation de `RunningMeanStd` ; SB3 et le chemin stepwise ne rescalent jamais. Verrous durcis ensuite (audit-verrous-cold-start) : assertion exacte `returns[-1] = r_last + gamma*last_value` au lieu d'un seuil sur `returns.mean()` qui laissait passer un `last_values ×0.5` (vérifié vert), et couverture du chemin de clipping (`raw_reward=150`, le cas que le `ret_var` gelé saturait à 10,0) ; un test comparant deux scalaires calculés sur place a été supprimé, prouvé vert face au défaut réintroduit ; 31 tests verts. **ROOT CAUSE du non-apprentissage trouvée et corrigée (2026-08-29)** : aliasing des buffers scratch d'observation — le worker distribué stockait les obs PAR RÉFÉRENCE pendant 340 steps alors que le moteur les sert dans des buffers réutilisés (`observation_builder`, contrat « ne jamais stocker au-delà du step courant ») → buffer du learner = état final répliqué (tout sauf `global_cont`), d'où `diag/ratio_mb0` 0,92-0,95 (attendu 1,0), `explained_variance` figée à ~0, critique effondré sur une constante ; le chemin stepwise était protégé par accident (pickle du pipe) ; présent depuis `4b2fe20d` (commit initial Phase 3) — le retrait du re-scaling était justifié mais n'était pas la cause. Fix : copie profonde dans `normalize_obs_with_snapshot` + `terminal_observation` (2 sites), verrous rouge/vert `test_obs_stored_are_copies_not_scratch_refs` + `test_terminal_observation_is_copied_before_reset` ; 33 tests verts ; **jumeau `n_envs = 1` corrigé (2026-08-29)** — `DummyVecEnv` (SB3 2.9) pose `terminal_observation = obs` puis `reset()` puis `deepcopy`, donc le bootstrap `TimeLimit.truncated` évaluait l'obs INITIALE de l'épisode suivant (mesuré chaîne de production : 28/28 clés identiques au post-reset avant fix, 4/28 différentes après) ; copie à la sortie de `BotControlledEnv.step` et `SelfPlayWrapper.step`, 2 verrous rouge/vert. **Validation en cours : run `--etape P2` du 2026-08-29, critère `ratio_mb0 = 1,0` exactement + `explained_variance` croissante dès les premiers rollouts** | [infra.md#perf-entrainement](infra.md#perf-entrainement) | ⚡ |
| infra | Noyau natif BFS move/empreintes — pool de move = 29 % d'une partie d'évaluation | [infra.md#noyau-natif](infra.md#noyau-natif) | ⚡ |
| infra | Migration PostgreSQL | [infra.md#postgresql](infra.md#postgresql) | ⚡ |
| infra | MCTS adversaire d'entraînement | [infra.md#mcts](infra.md#mcts) | 🚫 |
| bot | Tranches 2-3 benchmark — schedule P0→P10 + exploiters : code et tests livrés 2026-08-22 (`--etape`, `curriculum.json`, pool figé par-env, `ExploiterProbeCallback` sondage synchrone + `validate_exploiter_protocol` + `exploiter_config`, 24 tests verrou) ; restent les 14 runs (~260 h) | [bot.md#league](bot.md#league) | 🚫 |

---

## Hygiène documentaire

| Sujets | Chantier | Fichier | ⚡/🚫 |
|---|---|---|---|
| doc | 🟠 **Refonte Documentation/** — P1–P3 livrées (2026-08-27) ; P4 consolidation : moteur+backlog (16→9 docs, 20 sources archivées), training (7→5 docs, 7 sources archivées) et **jeu+outils** (jeu 3→3 noms d'objet, outils 7→4 dont fusion 5→1 `outils_conformite`, 11 sources archivées) livrés 2026-08-28 ; **v11** (5 renommages d'objet, strates §9.4 purges) et **infra** (3 renommages d'objet, corps re-vérifiés, gardes re-pointées) livrés 2026-08-28 — P4 complète, les 5 lots livrés ; correctif de suivi : slot `m` du dashboard de `metriques.md` réaligné sur `m_immediate_reward_ratio_mean` (l'ancien tag n'est plus émis) ; **correctif de suivi 2026-08-28** : 2 tests rouges de vérification documentaire recalés sur les noms P4 (V11_phaseA → decisions_du_joueur, V11_agent_rework → index_v11), lot P4 v11 finalisé (sweep 5 refs code, strate périmée §9.4bis + T6, tables correspondance 5 docs) | [doc.md#refonte](doc.md#refonte) | ⚡ |
