.git
.cursor
.vscode

__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/

.venv/
venv/

node_modules/
frontend/node_modules/
frontend/dist/

ai/models/

*.log
*.sqlite
*.sqlite3

# Base de comptes. Sans ces lignes, le `COPY . /app` du Dockerfile CUISAIT `config/users.db`
# (hashes de mots de passe) dans une couche de l'image : le montage compose la masque à
# l'exécution, mais elle reste lisible dans l'image pour qui l'obtient. Ajouté le 2026-08-10.
# Les TROIS lignes sont nécessaires : `*.db` ne couvre que la racine du contexte (Docker utilise
# filepath.Match, où `*` ne franchit pas un `/`), `**/*.db` couvre les sous-répertoires, et le
# chemin explicite garantit le cas qui compte même si les deux motifs changent de sémantique.
*.db
**/*.db
config/users.db

# Outillage de session Claude Code : contient les worktrees, soit des copies COMPLÈTES du dépôt
# (4,5 Go mesurés le 2026-08-10). Sans cette ligne, le contexte de build les embarque toutes.
.claude/

# Jamais exécuté par le conteneur. Leur exclusion rend aussi cohérent le périmètre bandit de
# scripts/security_check.sh, qui scanne « le Python que l'image embarque ».
tests/

agent-transcripts/
Documentation/TODO/
