# Vulnérabilités pip-audit acceptées SCIEMMENT sur la surface de production
# (requirements.runtime.txt). Lu par scripts/security_check.sh.
#
# Format : <ID>  # <justification>
# La justification est OBLIGATOIRE : une ligne sans `#` fait échouer le script.
# On liste des IDs, jamais des paquets : une NOUVELLE CVE sur un paquet déjà cité ici fera
# rougir le script au lieu de se glisser sous un blanc-seing.
#
# ÉTAT AU 2026-08-11 : AUCUNE EXCEPTION. Le fichier est vide, et c'est l'objectif à tenir.
#
# Il a porté 19 CVE torch entre le 2026-08-10 et le 2026-08-11, le temps de monter
# torch 2.5.1 -> 2.13.0 et sb3 2.6.0 -> 2.9.0. Elles ne sont pas « acceptées autrement » :
# elles n'existent plus sur la surface, et le mode `weights_only=True` de torch >= 2.6 a
# fermé le vecteur qu'elles visaient (cf. shared/torch_safe_globals.py).
#
# Avant d'ajouter une ligne ici, se demander si la montée de version n'est pas simplement
# faisable : c'est ce qu'on croyait coûteux et qui a pris une demi-journée, mesures comprises.
